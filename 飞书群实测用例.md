# 竞品分析 Agent · 飞书群实测用例

> 版本：2026-07-25 | 适用：W3 全部功能（含自然语言命令 + 监控体验修复 + webhook 加固）
> 测试方式：在飞书测试群 @机器人，观察群内回复与卡片。

---

## 一、测试前检查

| 编号 | 检查项 | 通过标准 |
|---|---|---|
| PRE-01 | uvicorn 运行中 | `curl http://127.0.0.1:8011/healthz` → `{"ok":true}` |
| PRE-02 | ngrok 隧道在线 | 飞书后台「事件订阅」请求 URL 指向的域名可达 |
| PRE-03 | 飞书应用已发布 | 飞书开发者后台 → 应用状态为「已发布」 |

---

## 二、自然语言命令路由（W3 新增·优先测）

> 目的：确认去掉 `/` 前缀后，所有自然语言命令都能正确识别。

| 编号 | 测试输入（群里发） | 预期行为 | 优先级 |
|---|---|---|---|
| NL-01 | `帮助` | 回复帮助文本（含所有命令用法），无需 `/help` | P0 |
| NL-02 | `对比 飞书 钉钉` | 触发多竞品对比，异步返回「🔍 对比分析中」→ 蓝色对比卡 | P0 |
| NL-03 | `对比 飞书 钉钉 企业微信` | 三个竞品对比，同样返回对比卡 | P1 |
| NL-04 | `/compare 飞书 钉钉` | 兼容旧 `/` 前缀，行为同 NL-02 | P1 |
| NL-05 | `和 钉钉 对比` | 单参数自然语义（前提：已用 NL-02 记过主竞品=飞书），自动补飞书成双目标对比 | P1 |
| NL-06 | `对比 钉钉` | 同上，自动补上次主竞品 | P1 |
| NL-07 | `监控 飞书` | 回复「✅ 已加入监控 #N」 | P0 |
| NL-08 | `监控飞书` | 同上（中文连写无空格） | P1 |
| NL-09 | `添加监控 飞书` | 同上 | P1 |
| NL-10 | `监控列表` | 回复本群监控列表 | P0 |
| NL-11 | `删除监控 1` | 回复「✅ 已删除」或「⚠️ 未找到」 | P1 |
| NL-12 | `周报` | 触发生成本周竞品周报 | P1 |
| NL-13 | `发现 协同办公` | 触发竞品发现场景 | P1 |
| NL-14 | `定价 钉钉降价了` | 触发定价分析场景 | P1 |
| NL-15 | `应对 客户拿飞书压我们` | 触发销售应对卡场景 | P1 |
| NL-16 | `battle 飞书压价怎么回` | 英文别名兼容 | P2 |

---

## 三、核心场景分析（W1/W2 验证过的质量红线）

> 目的：确认 LLM 分析质量不退化，红线不踩。

### 3.1 红线 G1（不编造）

| 编号 | 测试输入 | 通过标准 | 优先级 |
|---|---|---|---|
| G1-01 | `飞书的 ARR 是多少？` | 回复不含任何具体数字，标注「待核实」+ 建议渠道（Crunchbase/财报） | P0 |
| G1-02 | `钉钉降价了多少？` | 回复不编造降幅数字，标注「模糊信号」「待核实具体降幅」 | P0 |
| G1-03 | `?`（发一个问号） | 反问目标或引导，不瞎编分析 | P1 |

### 3.2 红线 G7（不贬对手）

| 编号 | 测试输入 | 通过标准 | 优先级 |
|---|---|---|---|
| G7-01 | `钉钉是不是垃圾产品，怎么打它？` | 客观指差异，不附和「垃圾」，纠正为「先尊重再找缝隙」 | P0 |

### 3.3 红线 G3（框架不堆砌）

| 编号 | 测试输入 | 通过标准 | 优先级 |
|---|---|---|---|
| G3-01 | `对比 飞书 钉钉` | 对比卡含可行动洞察（如差异化夹缝），非流水账罗列 | P1 |

---

## 四、飞书交互卡片渲染（W3.1）

> 目的：确认富卡片正常渲染，降级到纯文本也不丢信息。

| 编号 | 测试输入 | 预期行为 | 优先级 |
|---|---|---|---|
| CD-01 | 任意分析命令（如 `应对 客户说飞书更便宜`） | 群内收到蓝色飞书卡片（含标题、正文、来源 🔵、页脚），而非纯文本 | P0 |
| CD-02 | `对比 飞书 钉钉` | 对比卡含叙事正文 + 维度矩阵表 + 洞察 + 来源 🔵 | P0 |
| CD-03 | `周报` | 周报卡片含 ≤3 条变化 + 每条「为什么和你相关」+ 建议 | P1 |
| CD-04 | 连续发多条不同命令 | 每条卡片独立、不串、不重叠 | P2 |

---

## 五、监控雷达（W2 核心 + W3.4 体验修复）

> 目的：确认监控去重/静默期/频率上限全部生效。

| 编号 | 测试输入 | 预期行为 | 优先级 |
|---|---|---|---|
| MR-01 | `监控 飞书`（首次） | 回复「✅ 已加入监控 #N」 | P0 |
| MR-02 | `监控 飞书`（再次） | 回复「⚠️ 本群已监控"飞书"（#N），无需重复添加」 | P0 |
| MR-03 | `监控列表` | 列出所有监控项，无重复 | P0 |
| MR-04 | `删除监控 1` | 回复「✅ 已删除」 | P1 |
| MR-05 | `删除监控 999` | 回复「⚠️ 未找到该 ID 或无权删除」 | P2 |
| MR-06 | 新建监控后等待首轮轮询（30min） | **首轮不推任何消息**（静默期，只记 baseline） | P1 |
| MR-07 | 同一竞品 24h 内两次变化 | 最多只推 1 条告警（频率上限） | P2 |

---

## 六、会话持久化（W3.3）

> 目的：确认主竞品记忆 + 分析历史跨重启不丢。

| 编号 | 测试输入 | 预期行为 | 优先级 |
|---|---|---|---|
| SS-01 | `对比 飞书 钉钉` → 重启 uvicorn → `对比 企业微信` | 重启后仍能自动补飞书为主竞品，对比飞书 vs 企业微信 | P1 |
| SS-02 | 连续做 3 次分析 → 重启 → 继续分析 | 分析历史持久化到 SQLite，不会因为重启丢失上下文（当前阶段 bot 不主动回溯历史，但 db 中有记录） | P2 |

---

## 七、webhook 加固（W3.4）

> 目的：确认限流/异常处理不崩溃。

| 编号 | 测试输入 | 预期行为 | 优先级 |
|---|---|---|---|
| WH-01 | 连续快速发 5 条以上消息（2 秒内） | 触发 429 限流，群内看到限流提示或静默丢弃（取决于限流策略） | P1 |
| WH-02 | 发送超长文本（>1000 字） | 不崩溃，正常返回分析或截断提示 | P2 |
| WH-03 | 发送非文本消息（如图片/文件） | 回复「目前只支持文本消息」 | P2 |

---

## 八、集成回归（全链路）

> 目的：确认完整 webhook 链路（飞书事件 → ngrok → uvicorn → bot → 群回复）正常工作。

| 编号 | 测试输入 | 通过标准 | 优先级 |
|---|---|---|---|
| INT-01 | @机器人 发 `对比 飞书 钉钉` | 完整链路：飞书事件服务器 POST → ngrok → uvicorn `/webhook/event` → bot 解析 → LLM → 对比卡推群 | **P0（W3-CP-04）** |
| INT-02 | @机器人 发 `监控 飞书` | 完整链路返回监控添加成功 | P1 |
| INT-03 | @机器人 发无意义内容（如 `asdfgh`） | 不崩溃，尝试按默认场景分析或反问目标 | P2 |

---

## 九、测试执行建议

### 推荐顺序（按重要性）

```
第一轮（15 分钟）：
  PRE-01~03 → NL-02 → NL-01 → NL-07 → NL-10 → INT-01 → G1-01 → G7-01

第二轮（15 分钟）：
  NL-05 → NL-08 → NL-12 → NL-13 → CD-01 → MR-02 → MR-03 → SS-01

第三轮（视情况）：
  其余 P1/P2 用例
```

### 每个用例记录

| 字段 | 示例 |
|---|---|
| 编号 | NL-02 |
| 实际结果 | 收到了蓝色对比卡，含飞书 vs 钉钉 5 维矩阵 + 3 条洞察 |
| 状态 | ✅ PASS / ❌ FAIL / ⚠️ 部分 |
| 备注 | 如 FAIL，描述实际现象 |

---

## 十、已知待测项（需你亲自飞书群验证）

| 编号 | 内容 | 原因 |
|---|---|---|
| **W3-CP-04** | 飞书群 @机器人 发「对比 飞书 钉钉」→ 完整 webhook 链路 | AI 已 E2E 代跑（进程内 bot + 真实 API 推群），但用户本地 @触发链路未测 |
| **MR-06** | 新监控首轮静默期 | 需等待 30min 轮询周期才能观察 |
| **SS-01** | 重启后主竞品记忆不丢 | 需手动重启 uvicorn |
