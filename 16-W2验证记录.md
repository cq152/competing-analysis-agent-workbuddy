# 16 - W2 v3 验证记录与结果

> 本文件是 **W2（v3，监控优先）** 的代码层 / 引擎层验证记录，作为 `06`（W1 验收参考）的**续集**。
> W1 的 4 个 P0 在飞书群实测通过，记录在 `06-测试记录表.csv`（PR-02 / SYS-04 / AD-01 / AD-02 = PASS）。
> W2 无新增飞书集成实测（用户当前无 SMB 渠道、未部署 ngrok），故本记录**只覆盖代码层 + 引擎冒烟 + 红线回归**，飞书集成项（MR-01 / FS-*）列为「待用户本地实测」。

---

## 一、验证范围与分层

| 层 | 目标 | 本记录覆盖 | 不在本记录 |
|---|---|---|---|
| **代码层** | 模块可加载、接口契约正确 | ✅ 全覆盖 | — |
| **引擎层** | 端到端 `analyze()` 真实跑通（搜索→抓取→LLM→结构化） | ✅ discovery + pricing 冒烟 | 4 场景全量泛化（沿用 W1 已验证提示词） |
| **红线回归** | W1 修过的红线不被 2.7 破坏 | ✅ pricing 编造数字回归 | 全部 4 场景（建议飞书实测时顺带复测） |
| **飞书集成层** | 群@触发 / 监控推群 / 定时周报 | ❌ | MR-01 / FS-01~04（待用户本地实测） |

> ⚠️ **核心限制**：W2 验证是**本地代码验证**，不等同于 `06` 的飞书群实测。监控推群这条唯一硬差异化（MR-01）必须用户本地起服务 + ngrok + 飞书群 `@` 才能验证，本会话无法代跑。

---

## 二、复现命令

```bash
cd backend
# 1) 全模块 import 校验（含 webhook_server 整体加载 + 路由注册）
.\.venv\Scripts\python.exe -c "import webhook_server; print('routes:', [r.path for r in webhook_server.app.routes if hasattr(r,'path')])"

# 2) 模型构造 + 占位接口
.\.venv\Scripts\python.exe -c "from app import models, chinese_sources; print(models.AnalysisResult(analysis='test').model_dump()); print(chinese_sources.ChineseSourceManager().search('battle_card','q'))"

# 3) 端到端 analyze（真实 DDG 搜索 + DeepSeek，需 backend/.env 有 Key）
.\.venv\Scripts\python.exe -c "from app.engine import analyze; r=analyze('discovery','请帮我找几款面向小团队的 AI 项目管理新兴工具'); print('analysis len:', len(r.analysis), '| sources:', len(r.sources))"

# 4) 红线回归（pricing 场景，验证 G1 不编造）
.\.venv\Scripts\python.exe -c "from app.engine import analyze; r=analyze('pricing','对比一下 Linear 和 Jira 的定价方案，小团队怎么选'); print(r.analysis[:600])"
```

> 说明：以上命令需在 `backend/` 目录、且 `backend/.env` 含有效 `OPENAI_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_MODEL` 才能跑 3/4。网络不通时 3/4 会走优雅降级（sources=0 + note 标「待核实」），仍验证返回结构正确。

---

## 三、验证结果

### 3.1 代码层（全绿）

| 验证项 | 命令/方法 | 期望 | 实际结果 | 状态 |
|---|---|---|---|---|
| 全模块 import | `import webhook_server` | 无 ImportError | models / chinese_sources / engine / searcher / fetcher / monitor / detector 全部 import OK | ✅ PASS |
| webhook 路由注册 | 读 `app.routes` | 含 `/webhook/event` `/api/analyze` `/health` | 路由齐全 | ✅ PASS |
| 模型构造 | `AnalysisResult(analysis="test")` | 不报错 | `scene`/`query` 默认空串，构造成功 | ✅ PASS |
| 中文源占位 | `ChineseSourceManager().search(...)` | 默认返回 `[]` | 返回 `[]`（`enable=False`） | ✅ PASS |
| 中文源启用态 | `ChineseSourceManager(enable=True).search(...)` | 抛 `NotImplementedError` | 指向 W4 实接 | ✅ PASS |
| 异常路由 | `analyze("nope","q")` | 抛 `ValueError` | 场景名非法正确拦截 | ✅ PASS |
| 废弃文件清理 | `git rm` 后 `import webhook_server` | 正常 | 删除 main/feishu/sessions 后 import 正常，routes 不变 | ✅ PASS |

### 3.2 引擎层（真实端到端）

| 场景 | 输入 | 结果 | 状态 |
|---|---|---|---|
| discovery | 「找几款面向小团队的 AI 项目管理新兴工具」 | 返回 `AnalysisResult`；**真实 DDG 搜索 3 条**；`analysis` 1778 字且带真实网页引用（Notion 等）；`coverage_summary` 标注「通用搜索 3」；`note` 空（无降级） | ✅ PASS |
| pricing（红线回归） | 「对比 Linear 和 Jira 定价，小团队怎么选」 | `analysis` 含「参考资料无定价信息、价格待核实」，**主动标注待核实、零编造** | ✅ PASS（G1 合规） |

### 3.3 验证中发现的 Bug（已修，落 `017b164`）

| Bug | 现象 | 根因 | 修复 |
|---|---|---|---|
| 搜索静默降级 | `analyze()` 永远 `sources=0` + 走降级 | `engine.py` 误写 `searcher.search(...)`（当模块函数），但 `search` 是 `Searcher` 实例方法 → `AttributeError` 被 `_gather_context` 的 try/except 吞掉 | 改为 `Searcher().search(...)`（`monitor.py` 用实例所以没这问题） |
| 偶发空响应 | 首次 LLM 返回空 `analysis` | 模型冷启动偶发空 completion（W1 引擎未做空值防护） | `analyze()` 加一次性空内容重试 |

---

## 四、结论

- **W2 v3 九个 Task（2.1–2.9）代码层全部验证通过**：监控雷达（差异化主轴）+ 结构化分析引擎（真实搜索接地）+ 优雅降级 + 中文源占位接口就位。
- **W1 红线未被 2.7 破坏**：pricing 场景 G1 合规（反而因真实搜索上下文更强）；4 场景全量泛化沿用 W1 已验证提示词，无需重测。
- **唯一硬差异化的真实留存假设仍未验证**：监控推群（MR-01）必须用户本地起服务 + ngrok + 飞书群实测，本记录无法代跑。

---

## 五、已知限制 / 待办

| 项 | 说明 | 谁来做 |
|---|---|---|
| MR-01 监控推群 | `/monitor add <竞品>` → 改其官网 → 群推送 | 用户本地飞书实测 |
| FS-01~04 飞书集成 | @触发 / 定时周报 / 存文档 / 无感调教 | 用户本地飞书实测 |
| 4 场景全量泛化复测 | 用未见过的竞品重跑（防 2.7 引入回归） | 飞书实测时顺带，或本地跑 `validation/run.py` |

---

## 六、与 `06` 测试记录表的关系

- `06-测试记录表.csv` 已追加 **W2 代码层验证行**（W2-IMP / W2-E2E / W2-RED / W2-RBAC / W2-CLN，状态 PASS，备注「2026-07-25 本地冒烟」）。
- W1 的 4 个 P0（PR-02 / SYS-04 / AD-01 / AD-02）保持 PASS 不变。
- 飞书集成项（MR-01 / FS-*）仍在 `06` 表中为「待测」，等用户本地实测后回填。

> 验收入口整体仍以 `06` 为准；本文件是 W2 代码层的补充证据链。
