# 15 - 项目文件扫描与待确认/待更新清单

> 日期：2026-07-25 01:35 | 触发：用户暂停 W2 编码，要求"重新扫描项目文件，找出需要确认和更新的地方"
> 方法：重读 README / 09(v3) / 11 / 06 / 04 / 14 / engine.py / searcher.py / fetcher.py / requirements.txt / memory，交叉比对
> 结论：**当前执行态（代码 + 我的记忆 + 11 手册）停留在 v2.1 中间态，但项目权威文档已全部走到 v3。存在计划版本错位。**

---

## 一、🔴 最严重：计划版本错位（我在跑旧计划）

| 维度 | 我实际在做的（v2.1 / 11 手册） | 项目权威文档（v3 / 09 附录C） |
|---|---|---|
| W2 核心目标 | 多源搜索质量跃升（中文深掘=差异化） | **监控推送最小可用 + 引擎轻改造**（监控=唯一硬差异化） |
| 监控雷达 | W4 才做 | **提前到 W2 核心（Task 2.4）** |
| 变化检测 | W4 才做 | **提前到 W2 核心（Task 2.5）** |
| 中文数据源 | W2 必接（天眼查+site: 完整） | **降为占位接口（默认关闭，2.8）** |
| site: 中文搜索 | searcher 完整实现 | **砍掉（2.2 简化）** |
| 定价页提取 | fetcher 完整实现 | **砍掉，移 W4（2.3 简化）** |
| G6 红线 | 硬约束（阻塞 Gate） | **降级为目标（不阻塞，06 第 30 行已改）** |
| 验收门槛 | 覆盖率≥50% + G6 合规 | **覆盖率≥30% + 监控推群可用** |

**v3 证据链（自洽）**：`14` 对比实验（裸 DeepSeek 质量够用 → 唯一硬壁垒=监控推送）→ `13` 批判性审视（中文护城河技术不成立）→ `09` 附录C v3（落决策）→ `06` G6 降级标注 → `04` 定价 v2 → `README` §7 行 123 汇总。文档间一致。

**冲突点**：我的 `memory/2026-07-25.md` "方向决策"段写的是"否决砍中文数据源（正是差异化点）/ G6 部分采纳 / 监控留 W4"——与 v3 完全相反。该记忆段是**过时中间态**，需按 v3 更正。

---

## 二、🟠 中等：代码 over-build + 技术债

### 2.1 `searcher.py` 按 v2.1 over-build
- 我写了 `SOCIAL_DOMAINS`（小红书/知乎/抖音/黑猫/脉脉等）、`site_search()` 中文社媒分类、`_classify_source()`。
- v3 要求：**砍 site: 中文源，仅保留通用 DuckDuckGo 搜索**。
- 现状：通用搜索真实可用（ddgs lite backend 3 条），但中文社媒部分按 v3 是多余的。

### 2.2 `fetcher.py` over-build + 跑不通
- 我写了 `PRICING_PATTERNS` / `fetch_pricing_page()` / `_extract_pricing_data()`（定价页定向提取）。
- v3 要求：**砍定价页提取，移 W4**。
- **且当前 BROKEN**：trafilatura 2.x API 漂移——
  - `output_format="text"` → 报错，应改 `"txt"`
  - `trafilatura.extract_title()/extract_date()` → 已移除，应改 `trafilatura.extract_metadata(html)` 取 `.title`/`.date`
  - 当前 `fetch()` 真实抓取验证失败（0 页）。

### 2.3 TaskCreate 任务列表错位
- 我建的 9 个任务（2.1-2.9）基于 v2.1：2.4=中文源(完整)/2.5=招聘/2.6=场景搜索/2.7=模型/2.8=引擎/2.9=Bot。
- v3 重新编号：2.4=monitor / 2.5=detector / 2.6=models / 2.7=engine轻改 / 2.8=中文源占位 / 2.9=Bot+监控命令。
- 任务列表需按 v3 重建。

---

## 三、🟡 轻微：文档引用漂移

| 位置 | 问题 |
|---|---|
| `09` / `11` 技术选型段 | 写 `duckduckgo-search`，实际 `requirements.txt` 已改 `ddgs`、`searcher.py` 已用 `ddgs`（8.x 废弃，实测 lite backend 才通） |
| `config.py` | `search_engine` 字段定义了但 engine/searcher 都没消费（疑似死字段） |
| `README.md` §7 行 122 | 旧描述"下一步：W1–W4 正式开发"与行 123 v3 并存，易误导 |
| `11` 手册 | 无"已被 09 附录C v3 取代"横幅，后人易误用 |

---

## 四、⚠️ 待用户确认（4 项）

1. **是否正式以 v3 为执行标准？**
   权威文档已标 v3，但我的执行态/记忆/11 手册还停在 v2.1 中间态。确认后我全盘切 v3，不再参考 11 旧 Task 编号。

2. **已写的 searcher.py / fetcher.py 怎么处理？**
   - A（推荐）：按 v3 **简化**——searcher 砍 site: 中文源；fetcher 修 trafilatura API + 砍定价提取。代码立刻对齐 v3。
   - B：先留着，等 monitor/detector 做完再回头精简（但会产生"多余代码"期）。

3. **v3 第一优先级是否 = 监控雷达(2.4) + 变化检测(2.5)？**
   这俩是 v3 的核心差异化，且我完全没碰。确认后修完 fetcher 直接跳做这俩，而非继续旧顺序的"中文源"。

4. **记忆笔记"方向决策"段授权我直接按 v3 覆盖更正？**
   该段与 v3 矛盾，不更正会误导后续 session。建议直接以 v3 覆盖（中文源降占位 / G6降级 / 监控提前），并注明"build first 的本意=先做出监控这个真差异化，而非中文源"。

---

## 五、📝 待更新文件清单

| 文件 | 更新内容 | 优先级 |
|---|---|---|
| `memory/2026-07-25.md` | 更正"方向决策"段 → 以 v3 为准 | 🔴 高 |
| `11-W2执行手册.md` | 加 v3 横幅 + 重写 Task 清单匹配 09 附录C（砍 site:中文源/定价提取，加 monitor/detector，中文源降占位） | 🔴 高 |
| `backend/app/searcher.py` | 按 v3 简化：移除 site: 中文社媒分类/搜索 | 🟠 中 |
| `backend/app/fetcher.py` | 修 trafilatura API + 按 v3 移除定价页提取 | 🟠 中 |
| `09` / `11` 技术选型段 | `duckduckgo-search` → `ddgs` | 🟡 低 |
| `README.md` §7 行 122 | 旧描述标注已废弃/删除，避免与行 123 混淆 | 🟡 低 |
| TaskCreate 任务列表 | 按 v3 重建（2.4 monitor / 2.5 detector / 2.6 models / 2.7 engine / 2.8 占位 / 2.9 bot+监控命令） | 🔴 高 |

---

## 六、建议的下一步顺序（需用户先确认第四节）

```
1. 用户确认第四节 4 项 → 我按 v3 更正记忆 + 11 手册横幅
2. 修 fetcher.py（trafilatura API）→ 简化（砍定价提取）
3. 简化 searcher.py（砍 site: 中文源）
4. 【v3 核心】写 monitor.py（2.4）+ detector.py（2.5）→ 这是真差异化，优先验证
5. models.py（2.6）→ engine 轻改（2.7，中文源占位）→ chinese_sources 占位（2.8）→ bot 监控命令（2.9）
```

> 注：本文件为审计产物，不改动任何代码/计划，等用户拍板后执行更新。

---

## 七、扫描后补充发现（2026-07-25 01:40）

### 7.1 模型名漂移：`deepseek-chat` 已失效 → `deepseek-v4-pro`（🟡 低危，运行时无碍）
- 实测核对三处 env：
  - `backend/.env`（运行时实际加载，gitignore）：`OPENAI_MODEL=deepseek-v4-pro` ✅
  - `validation/.env`：`OPENAI_MODEL=deepseek-v4-pro` ✅
  - `backend/.env.example`（入库参考）：`OPENAI_MODEL=deepseek-chat` ❌ 陈旧
  - `backend/app/config.py` 第 24 行默认值：`model: str = Field(default="deepseek-chat", ...)` ❌ 死默认值
- **结论**：运行时因 `backend/.env` 覆盖了默认，engine 实际可调通；但若有人按 `.env.example` 新建环境或缺失 .env，会落到死的 `deepseek-chat` 直接挂。属配置漂移，非运行阻塞。
- 需更新：`.env.example` → `deepseek-v4-pro`；`config.py` 默认值 → `deepseek-v4-pro`。

### 7.2 记忆文件其实已含 v3（修正第一节表述）
- 重读 `memory/2026-07-25.md` 发现：第 86-92 行「计划 v3 落地」已完整记录 v3 五点决策，且第 88 行明确标注**覆盖**第 74-84 行「方向决策」的「监控暂按 W4 / 不砍中文源」。
- 故**计划版本错位不在记忆缺失 v3**，而在：① `11` 手册仍是 v2.1 ② 我写的代码按 v2.1 over-build ③ 第 74-84 行「方向决策」段与 v3 矛盾（虽有 88 行覆盖注，但该段本身仍易误导）。
- 第一节"记忆方向决策段过时"应降级为低优先级清理项（v3 已在 86-92 正确记录）。

### 7.3 修订后待更新文件表（合并 7.1）
| 文件 | 更新内容 | 优先级 |
|---|---|---|
| `memory/2026-07-25.md` 第 74-84 行 | 「方向决策」段与 v3 矛盾，加醒目"已废弃，以 86-92 行 v3 为准"横幅（已有 88 覆盖注，强化即可） | 🟡 低 |
| `backend/.env.example` | `OPENAI_MODEL=deepseek-chat` → `deepseek-v4-pro` | 🟡 低 |
| `backend/app/config.py` | 默认 `model` 值 `deepseek-chat` → `deepseek-v4-pro` | 🟡 低 |
| `11-W2执行手册.md` | 加 v3 横幅 + 重写 Task 清单（砍 site:中文源/定价提取，加 monitor/detector，中文源降占位） | 🔴 高 |
| `backend/app/searcher.py` | 按 v3 简化：移除 site: 中文社媒分类/搜索 | 🟠 中 |
| `backend/app/fetcher.py` | 修 trafilatura API + 按 v3 移除定价页提取 | 🟠 中 |
| `09`/`11` 技术选型段 | `duckduckgo-search` → `ddgs` | 🟡 低 |
| `README.md` §7 行 122 | 旧描述标注已废弃，避免与行 123 v3 混淆 | 🟡 低 |
| TaskCreate 任务列表 | 按 v3 重建（2.4 monitor / 2.5 detector / 2.6 models / 2.7 engine / 2.8 占位 / 2.9 bot+监控命令） | 🔴 高 |
