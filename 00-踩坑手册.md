# 00 - 踩坑手册 · Lessons Learned

> 从「竞品分析 Agent」项目中沉淀的**可复用踩坑记录**。
> 按统一结构整理：**触发条件 → 现象 → 根因 → 解法 → 可复用范围**。
> 下一个飞书 Bot / AI Agent / SaaS 项目直接复制，减少重复试错。

---

## 一、LLM / 提示词工程（⭐ 通用性最强，所有 AI 项目适用）

### 1.1 复读陷阱（R1）

| 项 | 内容 |
|---|---|
| **触发** | 用与 few-shot 示例**相同/高度相似**的输入跑测试 |
| **现象** | 输出结构 100% 正确、格式完美，但内容**几乎照抄示例**，无法证明模型真正具备分析能力 |
| **根因** | LLM 的 in-context learning 会优先匹配示例模式，相似输入 → 直接复用示例输出路径 |
| **解法** | 测试 query 必须**换行业、换竞品、换场景**，与 few-shot 示例无重叠；验证集和训练集（示例）必须隔离 |
| **防回归** | 把「测试必须用未见过的竞品」写进项目红线和 CI 流程 |
| **来源** | `06` §3.3 / 本项目 `validation/cases.json` 第一轮→第二轮 |

### 1.2 编造数字陷阱（R2）

| 项 | 内容 |
|---|---|
| **触发** | 用户输入模糊（如"听说降价了""ARR 是多少"），提示词未明确禁止编造 |
| **现象** | 模型凭空生成具体数字（如"原价 2,999→2,199 降幅 27%"），虽然标了"待核实"，但数字本身是编的 |
| **根因** | LLM 倾向于" helpful "——给一个具体数字比说"我不知道"看起来更有用；模糊输入时模型会自行脑补 |
| **解法** | 在提示词中加**硬约束红线**：「不确定的数据必须标注'待核实'并给出核实渠道，禁止编造任何具体数字」；对抗测试用追问法（"给我具体数字"）验证红线是否生效 |
| **防回归** | 每次改提示词后必须跑一次模糊输入 + 追问的对抗测试 |
| **来源** | `06` §3.3 / `validation/prompts/pricing.txt` |

### 1.3 轻蔑措辞陷阱（R3）

| 项 | 内容 |
|---|---|
| **触发** | Battle Card 场景中用户或模型需要对比竞品 |
| **现象** | 模型输出"开源是玩具""垃圾产品"等轻蔑措辞，虽然类比有力但不专业且可能冒犯客户 |
| **根因** | "带钩子/打比方"的要求容易激发模型的创意表达欲，滑向情绪化攻击 |
| **解法** | 红线收紧为「中性定位钩子」，明确禁止贬低类措辞；对抗测试用诱导输入（"XX 是不是垃圾？"）验证 |
| **防回归** | AD-02 对抗用例纳入每次回归测试 |
| **来源** | `06` §3.5 / `validation/prompts/battle_card.txt` |

### 1.4 「流畅但很浅」风险

| 项 | 内容 |
|---|---|
| **触发** | LLM 产出格式正确、语言流畅，但缺乏真实洞察 |
| **现象** | 报告看起来很专业（有结构、有标题、有建议），但泛到放在任何竞品上都成立——没有针对性 |
| **根因** | 提示词过于强调格式模板，模型优先满足形式要求而非深度分析 |
| **解法** | 先验证分析质量再投工程；验收标准必须有"因果链"要求（战略 A → 产品 B → 对你意味着 C）；用 live 原型跑测而非只审提示词 |
| **可复用** | 所有 AI 内容生成项目的 #1 风险 |
| **来源** | `06` §一 经验1 |

### 1.5 提示词版本化防漂移

| 项 | 内容 |
|---|---|
| **触发** | 提示词散落在多个地方（飞书 UI / 文档 / 代码 / 配置文件） |
| **现象** | 改了一处没同步另一处 → 飞线上的是旧版 → 测试通过但生产环境行为不一致 |
| **解法 | **单一真相源**：提示词只存在一个地方（本项目为 `validation/prompts/*.txt`），其余位置只消费不编辑；改完 commit + 跑回归 |
| **可复用** | 所有需要多端部署的 Agent 项目 |
| **来源** | `06` §一 经验3 / README §10 纪律 |

---

## 二、飞书开放平台集成（⭐ 飞书 Bot / 飞书应用项目适用）

### 2.1 页面迷宫：智能伙伴 ≠ 自定义智能体

| 项 | 内容 |
|---|---|
| **触发** | 用户在飞书 Aily 页面找不到"人设""发布模式""联网"等配置入口 |
| **现象** | 页面 Tab 是「个性化/产物/技能/模型/安全」，没有目标配置项 |
| **根因** | 飞书生态有两个近似产品：**智能伙伴（小满，个人 AI 助手）** vs **Aily 平台自定义智能体**。用户停在前者，后者才有完整配置能力 |
| **识别方法** | 看页面特征：①顶部有无「智能体」Tab + 右上角「+ 创建智能体」= 正确页；②左侧有无「新任务/自动化/智能体/项目」导航 = 正确页；③显示 SOUL.md / 「个性化」Tab = 错误页（智能伙伴） |
| **解法** | 文档必须配**截图对照表**，不能只给功能名；以用户实际截图反向校准文档 |
| **可复用** | 所有飞书 Aily / 智能体相关项目 |
| **来源** | memory 日记 / `aily/README.md` |

### 2.2 长连接单连接限制（F1）

| 项 | 内容 |
|---|---|
| **触发** | 使用 `lark.ws.Client` WebSocket 长连接模式接收消息 |
| **现象** | 开发期反复杀→起进程 → 新连接建成功但群里 @机器人没反应；旧连接僵尸导致事件路由到死实例 |
| **根因** | 飞书规定**同一应用只允许一个活跃长连接**；旧连接靠心跳超时才发现死亡，期间事件全部丢失 |
| **解法** | 方案 A：加单实例锁（原子 O_EXCL `.bot.lock`），启动前检查旧进程是否存活。方案 B（推荐）：**改用 HTTP Webhook 回调**，彻底绕过长连接限制 |
| **何时选 Webhook** | 需要频繁重启开发 / 多实例部署 / 不想管理连接生命周期 |
| **来源** | `08` 附录 C.1 / `backend/app/bot.py` 单实例锁 |

### 2.3 回复 API 字段校验失败（F2）

| 项 | 内容 |
|---|---|
| **触发** | 调用 `im.v1.message.create` 发送回复消息 |
| **现象** | `receive_id_type="message_id"`（引用回复）报错 `99992402 field validation failed` |
| **根因** | 飞书 message_id 引用回复有严格格式要求，某些场景下 chat_id 与 message_id 不匹配 |
| **解法** | 改用 `receive_id_type="chat_id"` 发送到群聊，以独立消息呈现（可见且不依赖 message_id 格式） |
| **可复用** | 所有飞书机器人回复消息场景 |
| **来源** | `08` 附录 C.1 / `backend/app/bot.py` _reply 方法 |

### 2.4 ngrok 代理变量冲突（F3）

| 项 | 内容 |
|---|---|
| **触发** | 系统环境设置了 `http_proxy` / `https_proxy` 变量 |
| **现象** | `ngrok http 8011` 启动失败，报 `ERR_NGROK_9009`（需付费功能） |
| **根因** | ngrok 走了系统代理服务器，被代理网关拦截为付费请求 |
| **解法** | 启动前清除代理变量：`env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY ngrok http 8011` |
| **可复用** | 所有使用 ngrok/cloudflared 内网穿透且环境有代理的场景 |
| **来源** | memory 日记 2026-07-24 |

### 2.5 权限静默缺失（F5）

| 项 | 内容 |
|---|---|
| **触发** | 应用已能收发群消息，但调用 `im.v1.message.list` 拉取群消息历史 |
| **现象** | API 返回 `230027 Lack of necessary permissions, need scope: im:message.group_msg` |
| **根因** | 收消息只需 `im:message` + `im:message:send_as_bot`，但**读群消息历史**需要额外权限 `im:message:group_msg`，不开也不报错（直到你调用才暴露） |
| **解法** | 飞书开发者后台 → 权限管理 → 搜索 `im:message` → 全部相关 scope 都开（尤其是带 `group_msg` 后缀的）；改完后**重新发布版本** |
| **可复用** | 所有需要读取群消息历史的飞书应用 |
| **来源** | 本次 P0 验收（2026-07-24 23:45） |

### 2.6 ListMessage API 参数陷阱（F6）

| 项 | 内容 |
|---|---|
| **触发** | 调用飞书 `im.v1.message.list` API 拉取群消息 |
| **现象** | `container_id_type="chat_id"` 报 `230001 invalid parameter` |
| **根因** | SDK 参数名与文档不完全一致；消息内容嵌套在 `MessageBody` 对象里而非直接在 `Message` 上 |
| **解法** | `container_id_type` 用 `"chat"`（不是 `"chat_id"`）；内容取 `msg.body.content`（JSON 字符串，需再 `json.loads` 取 `.text`）；API 返回**最旧在前**，要反转才是最新 |
| **可复用** | 所有使用 lark-oapi Python SDK 读消息的项目 |
| **来源** | 本次 P0 验收（2026-07-24 23:50） |

### 2.7 飞书操作文档原则

| 项 | 内容 |
|---|---|
| **原则** | 飞书类操作文档**必须配截图/对照表**，不能只给功能名 |
| **原因** | 飞书生态内同名/近似入口极多（智能伙伴 vs 自定义智能体 vs 智能体小队），UI 版本差异大，文字描述可能因版本不同失效 |
| **做法** | 给出页面特征对照表（"如果你看到 X = 你在正确的页面"），以用户实际截图反向校准文档 |
| **来源** | memory 日记多次迭代经验 |

---

## 三、工程运维（⭐ 后端服务 / Bot 项目适用）

### 3.1 后台进程回收风险

| 项 | 内容 |
|---|---|
| **触发** | uvicorn / ngrok 等长运行进程挂在本会话后台（`run_in_background`） |
| **现象** | 进程运行一段时间后被系统回收 → 异步回复丢失 → 用户以为"发了但没收到" |
| **根因** | 会话级后台进程的生命周期依赖会话存活，超时或资源压力下可能被清理 |
| **解法** | 生产/MVP 验证期：用户在自己终端独立启动进程，不依赖对话会话；开发调试期：注意检查后台 task 状态，发现异常及时重启 |
| **可复用** | 所有需要在对话会话中启动长运行服务的场景 |
| **来源** | memory 日记 / `_run_p0.py` 首次跑测 |

### 3.2 ngrok-free 域名临时性

| 项 | 内容 |
|---|---|
| **触发** | 使用 ngrok 免费版做内网穿透 |
| **现象** | 每次 ngrok 重启，公网域名都会变（如 `sitter-shrubbery-washout.ngrok-free.dev` 可能下次变成别的） |
| **影响** | 飞书后台填的 Webhook 回调地址失效 → 事件推送不到 |
| **解法** | 每次 ngrok 重启后：① 拿新域名（`curl localhost:4040/api/tunnels` 或看控制台输出）；② 飞书后台事件订阅 → 改 Webhook 地址 → 重新发布；③ 或升级 ngrok 付费版固定域名 |
| **可复用** | 所有 ngrok 免费版 + 飞书 Webhook 场景 |
| **来源** | README §7 衔接 SOP |

### 3.3 端口冲突

| 项 | 内容 |
|---|---|
| **触发** | 默认端口被本机其他服务占用 |
| **现象** | `uvicorn` 启动报 `Address already in use`（本项目 8000 端口被 IIS/金蝶 K3Cloud 占用） |
| **解法** | 用非常见端口（如 8011）；或在 `.env` 中配置 `WEBHOOK_PORT` 让用户自定义 |
| **可复用** | 所有本地开发服务 |
| **来源** | `07` 技术方案 / backend 实践 |

### 3.4 401 不一定是签名 bug

| 项 | 内容 |
|---|---|
| **触发** | Webhook 服务日志出现 `401 Unauthorized` |
| **现象** | 反复检查签名逻辑没问题，但仍有 401 |
| **根因** | 可能是**公网扫描器/爬虫**发的假请求（IP 段与飞书事件服务器不同），被验签逻辑拦截后正常返回 401 |
| **解法** | 看 IP 地址：飞书事件服务器 IP 段是固定的（如 `14.153.53.165`），其他 IP 的 401 可以忽略；生产环境可加 IP 白名单 |
| **可复用** | 所有公网暴露的 Webhook 接口 |
| **来源** | 本次 webhook 端到端验证 |

---

## 四、产品设计（⭐ SaaS / Bot 产品适用）

### 4.1 免费层钩子设计

| 项 | 内容 |
|---|---|
| **结论** | 用「**监控**」做免费层钩子，不用「限次分析」 |
| **理由** | 监控让人每天回来看（留存驱动），限次分析留住的是只看不动的用户 |
| **本项目实践** | "1 个竞品永久免费监控"作为钩子 |
| **可复用** | 所有 Freemium SaaS / Bot 产品 |
| **来源** | `06` §一 经验5 |

### 4.2 分发即护城河

| 项 | 内容 |
|---|---|
| **结论** | **飞书群 = 产品界面**，bot 进群 = 嵌入工作流 = 留存 |
| **理由** | 独立网站需要用户改变习惯来访问；嵌入已有工作流（群聊）零摩擦 |
| **反模式** | 先想功能后想分发，最后发现没人来用 |
| **可复用** | 所有平台型 Bot / 插件产品 |
| **来源** | `06` §一 经验6 |

### 4.3 变化检测 > 信息收集

| 项 | 内容 |
|---|---|
| **结论** | 架构围着「**发现变了什么 + 跟你相关吗**」转，不是围着「全面报告」转 |
| **理由** | SMB 用户没时间看 30 页报告；他们需要的是"这个变化对我有什么影响 + 我该做什么" |
| **落地** | 相关度评分决定推不推送，噪声不刷屏 |
| **可复用** | 所有情报/监控类产品 |
| **来源** | `06` §一 经验7 |

---

## 五、快速索引（按场景查）

| 你在做… | 先看这些坑 |
|---|---|
| 第一次做飞书 Bot | 2.1, 2.2, 2.3, 2.5, 2.7 |
| 写 LLM 提示词 | 1.1, 1.2, 1.3, 1.4, 1.5 |
| 做 AI Agent MVP 验证 | 1.1, 1.4, 4.1, 4.2 |
| 用 ngrok 内网穿透 | 2.4, 3.2 |
| 跑通 Webhook 全链路 | 2.2, 2.3, 3.1, 3.4 |
| 设计 SaaS 免费层 | 4.1, 4.2, 4.3 |

---

## 六、元信息

| 项 | 内容 |
|---|---|
| 沉淀自 | 竞品分析 Agent 项目（2026-07-24） |
| 覆盖阶段 | 从 0 到 MVP Gate 通过的全过程 |
| 更新方式 | 新坑发现后追加对应章节，保持结构一致 |
| 使用方式 | 下一个项目开始前通读一遍；遇到问题时按 §五 索引查找 |
