# 竞品雷达 SaaS 开发实施计划（总蓝图）

> 日期：2026-07-25 | 版本：v3.0（基于 `13` 批判性审视 + `14` 竞品对比实验修订；v2.1 内容保留作参考，开发以**附录C**为准） | 模式：**滚动计划**（细化当前阶段，后续阶段保持骨架）
>
> **项目全貌**：
> - **W1 ✅ 已完成** — 预研骨架 + MVP Gate（纯 LLM 引擎 + 飞书 Webhook 端到端 + 4 场景 P0 验收通过）
> - **W2–W4 🔲 本文档范围** — MVP 内测版开发（v3 调整：**监控提前到 W2** → 卡片 → 中文源增强）
> - **W5+ 📋 后续阶段** — 见 §附录B（用户验证 → 迭代 → 正式发布，不提前细化）

---

## W1 完成状态（基线）

| 交付物 | 文件 | 状态 |
|---|---|---|
| FastAPI 主入口 | `webhook_server.py` | ✅ 生产可用 |
| 分析引擎（纯 LLM） | `app/engine.py` | ✅ 可运行 |
| 消息处理/场景路由 | `app/bot.py` | ✅ 4 场景全通 |
| 配置管理 | `app/config.py` | ✅ .env 读取正常 |
| 飞书 Webhook 端到端 | ngrok + 事件订阅 | ✅ 全通 |
| **MVP Gate 验收** | 4 个 P0 场景飞书群实测 | ✅ **PASS（2026-07-24）** |

---

## W2–W4 正式开发（MVP 内测版）

## 0. 现状基线

### 已有代码（W1 预研骨架，可运行）

| 文件 | 职责 | 行数 | 状态 |
|---|---|---|---|
| `webhook_server.py` | FastAPI 主入口（Webhook + API + Health） | 163 | ✅ 生产可用 |
| `app/engine.py` | 分析引擎（纯 LLM，加载 prompts） | 48 | ⚠️ 需改造 |
| `app/bot.py` | 消息处理 / 场景路由 / 群白名单 / text 回复 | 236 | ⚠️ 需扩展 |
| `app/config.py` | .env 配置（LLM + 飞书凭证） | 15 | ⚠️ 需扩展 |
| `app/main.py` | 已废弃（被 webhook_server.py 取代） | 49 | 🗑️ 待清理 |
| `app/feishu.py` | 已废弃（stub） | 19 | 🗑️ 待清理 |
| `app/sessions.py` | 已废弃（仅被 main.py 引用） | 17 | 🗑️ 待清理 |

### 已验证能力
- 飞书 Webhook 端到端全通（接收→验签→场景路由→调 LLM→回复到群）
- 4 个 P0 场景通过飞书群实测验收（battle_card/pricing/weekly/discovery）
- `/api/analyze` 可被外部系统调用
- ngrok 内网穿透 + 飞书事件订阅已配置

### 提示词资产
- `validation/prompts/`：system.txt + 4 个场景提示词（battle_card/pricing/weekly/discovery）
- **唯一真相**，后端直接加载，不重复存放

### 数据源覆盖基线（审核前 vs 目标）

从 04/05/06 交叉提取，去重后应有 **21 个数据源**，当前覆盖率 ~10%（仅通用搜索+网页提取）。

**本计划目标：W2 结束时覆盖率达到 ~70%（14/21），满足 G1/G6 红线最低要求。**

---

## 1. 技术选型

### 1.1 搜索服务

| 选项 | 免费额度 | 延迟 | 中文质量 | 推荐度 |
|---|---|---|---|---|
| **DuckDuckGo Search** (duckduckgo-search) | 无限制 | ~1s | 中等 | ⭐⭐⭐ **MVP 通用搜索** |
| Google Custom Search API | 100次/天 | <1s | 高 | ⭐⭐ 额度太少 |
| SerpAPI | 100次/月免费 | ~2s | 高 | ⭐⭐ 付费才够用 |
| Bing Search API | 1000次/月免费 | ~1s | 高 | ⭐⭐⭐ 备选 |

**建议**：MVP 用 `duckduckgo-search`（零成本、无限制、pip 即装），生产换 SerpAPI/Bing。

### 1.2 网页内容提取

| 选项 | 特点 | 推荐度 |
|---|---|---|
| **trafilatura** | 专注正文提取，抗噪能力强，轻量 | ⭐⭐⭐ **推荐** |
| newspaper3k | 功能多但维护停滞 | ⭐⭐ |
| BeautifulSoup + 手写规则 | 完全可控但工作量大 | ⭐ |
| Jina Reader API | 免费托管服务，无需本地解析 | ⭐⭐⭐ **备选 fallback** |

**建议**：MVP 用 `trafilatura` 本地提取 + `Jina Reader` 作为 fallback。

### 1.3 中文数据源接入

| 数据源 | 接入方式 | 成本 | MVP 方案 | 对应红线 |
|---|---|---|---|---|
| **天眼查** | 开放平台 API | 免费（有额度限制） | `tianyancha` SDK 或直接 HTTP 调用 | **G1 核实渠道** |
| **site: 限定搜索** | DuckDuckGo site: 操作符 | 零成本 | 封装为 `chinese_sources.py` 的子模块 | **G6 中文优先** |
| **搜狗微信搜索** | 搜索引擎 | 免费 | 公众号文章检索 | G1 核实渠道 |
| **第三方数据 API**（小红书/抖音深度数据） | 第三方服务 | 低付费（~2元/页起） | **占位接口**，默认关闭，按需开启 | P0 场景增强 |
| **七麦数据** | 开放平台 API | 免费（基础版） | App 竞品时启用 | G1 核实渠道 |

**MVP 策略**：
1. **天眼查 API** = 必接（免费、结构化、G1 硬要求）
2. **site: 限定搜索** = 必接（零成本覆盖小红书/知乎/黑猫/公众号可索引内容）
3. **第三方 API** = 占位（接口预留，默认不启用，避免阻塞开发）
4. **七麦** = 按需接入（仅当竞品有 App 时）

### 1.4 新增依赖

```
# requirements.txt 新增
duckduckgo-search>=7.0    # 通用搜索引擎
trafilatura>=1.8         # 网页正文提取
httpx>=0.27              # 异步 HTTP（搜索/抓取用）
pydantic-settings>=2.0   # 配置管理增强
tianyancha>=1.0          # 天眼查 API 客户端（或手动封装 HTTP）
```

---

## 2. 目录结构（目标状态）

```
backend/
  app/
    __init__.py
    config.py              # [改] 扩展：搜索/抓取/中文数据源/日志配置
    engine.py              # [改] 核心改造：多源搜索 → 抓取 → 组装上下文 → LLM → 结构化结果
    bot.py                 # [改] 扩展：卡片回复、结构化结果展示
    searcher.py            # [新] 通用搜索层：DuckDuckGo + 结果去重排序
    fetcher.py             # [新] 抓取层：URL 正文提取、元数据抽取
    chinese_sources.py     # [新] 中文数据源层：天眼查 / site:搜索 / 第三方API占位
    recruitment_source.py  # [新] 招聘数据：招聘网站搜索 + 官网招聘页抓取
    scene_search.py        # [新] 场景感知搜索策略：4 场景差异化搜索编排
    models.py              # [新] 数据模型：AnalysisResult / Source / DataPoint / CompareResult
    card_renderer.py       # [新] 飞书卡片渲染：JSON Card 构造（W3 做）
    sessions.py            # [重写] 会话管理：SQLite 持久化 + 上下文窗口（W3 做）
    logger.py              # [新] 统一日志：结构化 JSON 日志 + 飞书错误上报
    monitor.py             # [新] 监控雷达 CRUD（W4 做）
    detector.py            # [新] 变化检测引擎（W4 做）
    scheduler.py           # [新] 定时任务 + 主动推送（W4 做）
    # 以下文件在 W2 完成后删除
    main.py                # 🗑️ 已废弃
    feishu.py              # 🗑️ 已废弃
  tests/
    __init__.py
    test_engine.py         # 引擎单元测试（mock 搜索/LLM）
    test_searcher.py       # 搜索层测试
    test_fetcher.py        # 抓取层测试
    test_chinese_sources.py # 中文数据源测试
    test_scene_search.py   # 场景感知搜索测试
    test_bot.py            # Bot 逻辑测试
  data/
    .gitkeep               # SQLite 数据库目录（gitignore）
  webhook_server.py        # [改] 加中间件（日志/限流/错误处理）
  requirements.txt         # [改] 新增依赖
  .env.example             # [改] 新增配置项
  README.md                # [改] 同步更新
```

---

## 3. 分周实施计划

### W2：多源搜索引擎接入 + 引擎改造（核心质量跃升）

**目标**：从「纯 LLM 编报告」升级为「基于真实多源数据做分析」，输出带来源引用的结果。
**关键变化（vs v1）**：W2 从 6 个 Task 扩展到 9 个，新增中文数据源层 / 招聘数据 / 场景感知搜索。

#### Task 2.1 — 基础设施加固（0.5d）

**改动文件**：`config.py`, `requirements.txt`, `.env.example`, 新建 `logger.py`

具体内容：
- [ ] `config.py` 加入搜索/抓取/中文数据源配置项：
  ```python
  # 搜索配置
  SEARCH_ENGINE: str = "duckduckgo"
  SEARCH_MAX_RESULTS: int = 5

  # 抓取配置
  FETCH_TIMEOUT: int = 10
  FETCH_MAX_URLS: int = 3

  # 中文数据源配置
  TIANYANCHA_API_KEY: str = ""          # 天眼查 API Key（空=不启用）
  ENABLE_THIRD_PARTY_API: bool = False   # 第三方数据 API（小红书/抖音等）
  QIMAI_API_KEY: str = ""                # 七麦 API Key（空=不启用）

  # 日志与数据库
  LOG_LEVEL: str = "INFO"
  DB_PATH: str = "data/bot.db"
  ```
- [ ] `requirements.txt` 新增 duckduckgo-search / trafilatura / httpx / pydantic-settings / tianyancha
- [ ] `.env.example` 同步新增配置项 + 注释
- [ ] 新建 `logger.py`：统一日志配置（JSON 格式、按级别分文件、飞书错误可单独 channel）

**验收**：`python -c "from app.config import settings; print(settings.search_max_results)"` 不报错；所有新增配置项可从 `.env` 读取

#### Task 2.2 — 通用搜索层 `searcher.py`（1d）

**新建文件**：`app/searcher.py`

具体内容：
```python
class SearchResult:
    url: str
    title: str
    snippet: str      # 搜索引擎返回的摘要
    source: str        # 来源域名（如 crunchbase.com, github.com）
    source_type: str   # "general_web" | "social" | "official" | "commercial"

class Searcher:
    def search(query: str, max_results: int = 5) -> list[SearchResult]
    def site_search(query: str, site: str, max_results: int = 5) -> list[SearchResult]
    # 内部实现：
    #   1. 构建搜索关键词
    #   2. 调用 DuckDuckGo 搜索
    #   3. 结果去重（按 URL domain 聚类）
    #   4. 按相关性排序（标题匹配 > snippet 匹配）
    #   5. 返回 SearchResult 列表
```

**验收**：
```python
from app.searcher import Searcher
results = Searcher().search("Notion 竞品分析")
assert len(results) > 0
assert all(r.url and r.title for r in results)
# site: 限定搜索
zhihu_results = Searcher().site_search("Notion 评价", "zhihu.com")
assert all("zhihu.com" in r.url for r in zhihu_results)
```

#### Task 2.3 — 抓取层 `fetcher.py`（1d）

**新建文件**：`app/fetcher.py`

具体内容：
```python
class FetchedPage:
    url: str
    title: str
    text: str           # 正文（trafilatura 提取）
    publish_date: str | None
    word_count: int
    source_type: str    # 标注来源类型

class Fetcher:
    def fetch(urls: list[str], max_pages: int = 3) -> list[FetchedPage]
    def fetch_pricing_page(url: str) -> FetchedPage | None  # 定价页专用提取
    # 内部实现：
    #   1. 并发 HTTP GET（httpx 异步，超时控制）
    #   2. trafilatura 提取正文
    #   3. 过滤低质量页面（<100 字、非中文/英文、404）
    #   4. 返回 FetchedPage 列表
```

**定价页定向抓取**（04 P0 第1条 + pricing 场景核心需求）：
- 识别 URL 包含 `/pricing`/`/price`/`/plans`/`/费用`/`/价格` 等路径
- 提取表格化定价信息（套餐名/价格/功能对比）
- 输出结构化 `{plan_name, price, period, features}` 而非纯文本

**验收**：
```python
from app.fetcher import Fetcher
pages = Fetcher().fetch(["https://www.notion.so/product"])
assert len(pages) > 0
assert pages[0].word_count > 100
```

#### Task 2.4 — 【新增】中文数据源层 `chinese_sources.py`（1d）

**新建文件**：`app/chinese_sources.py`

> **这是本次修订的核心新增任务。对应 10 审核报告的「缺失 1 + 缺失 2」。**
> **目的：满足 G6 红线（中文互联网优先）和 G1 红线（不编造的核实渠道）。**

具体内容：

```python
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from enum import Enum

class SourceType(Enum):
    GENERAL_WEB = "general_web"        # 通用网页
    SOCIAL_XIAOHONGSHU = "xiaohongshu"  # 小红书
    SOCIAL_DOUYIN = "douyin"            # 抖音
    SOCIAL_ZHIHU = "zhihu"              # 知乎
    SOCIAL_WEIBO = "weibo"              # 微博
    WEIXIN_GZH = "weixin_gzh"           # 微信公众号
    COMPLAINT_HEIMA = "heima"           # 黑猫投诉
    COMMERCIAL_TIANYANCHA = "tianyancha" # 天眼查
    COMMERCIAL_QIMAI = "qimai"          # 七麦数据
    RECRUITMENT = "recruitment"         # 招聘数据
    OFFICIAL_PRICING = "official_pricing" # 官网价格页
    OFFICIAL_CHANGELOG = "official_changelog" # 官网更新日志

@dataclass
class DataPoint:
    """统一数据点格式——所有数据源的输出归一化"""
    title: str
    content: str                        # 正文/摘要/结构化数据
    url: str | None = None
    source_type: SourceType = SourceType.GENERAL_WEB
    source_name: str = ""               # 显示名称：「小红书」「天眼查」等
    published_at: str | None = None     # 发布时间
    confidence: str = "medium"          # high / medium / low
    metadata: dict = field(default_factory=dict)  # 扩展字段

class DataSource(ABC):
    """数据源统一抽象"""
    @abstractmethod
    def name(self) -> str: ...

    @abstractmethod
    def source_type(self) -> SourceType: ...

    @abstractmethod
    def is_available(self) -> bool: ...  # 是否已配置可用

    @abstractmethod
    async def search(self, query: str, **kwargs) -> list[DataPoint]: ...

# ===== 具体数据源实现 =====

class TianYanchaSource(DataSource):
    """
    天眼查：融资/工商变更/股东结构/法律风险/知识产权
    - 接入方式：天眼查开放平台 API（api.tianyancha.com/open/）
    - 成本：免费（有调用频率限制）
    - 覆盖维度：融资历史、工商变更、司法风险、知识产权、招投标
    - 对应红线：G1 核实渠道（融资/工商数据必须可核实）
    """
    BASE_URL = "https://open-api.tianyancha.com"
    # 实现：HTTP 调用 → 解析 JSON → 映射为 DataPoint 列表

class SiteSearchSource(DataSource):
    """
    Site: 限定搜索 —— 通过 DuckDuckGo site: 操作符搜索特定平台
    - 覆盖平台：
      · 小红书 (xiaohongshu.com / xiaohongshu.com/explore)
      · 知乎 (zhihu.com)
      · 黑猫投诉 (ts.21cn.pro)
      · 微信公众号（通过搜狗微信搜索 weixin.sogou.com）
    - 成本：零（复用 DuckDuckGo）
    - 局限性：只能获取公开可索引的内容，非全量
    - 对应红线：G6 中文互联网优先
    """
    # 实现：构建 "query site:domain" → 调用 Searcher.site_search() → 抓取正文

class ThirdPartyApiSource(DataSource):
    """
    第三方数据 API 占位 —— 小红书/抖音深度数据
    - 默认关闭（ENABLE_THIRD_PARTY_API=False）
    - 可选服务商：典枢数据（~2元/页）、Octoparse Social Feed 等
    - 用途：获取完整笔记详情/评论/互动数据（site: 搜索拿不到的）
    - 对应红线：P0 场景增强（舆情分析需要完整评论数据）
    """
    # 实现：占位 stub，返回空列表 + log warning

class ChineseSourceManager:
    """
    中文数据源统一调度器 —— 对外暴露的唯一入口
    """
    def __init__(self):
        self._sources: list[DataSource] = [
            TianYanchaSource(),
            SiteSearchSource(),
            ThirdPartyApiSource(),  # 默认不启用
        ]

    def search(self, query: str, source_types: list[SourceType] | None = None) -> list[DataPoint]:
        """根据指定类型（或全部可用）搜索中文数据源"""
        ...

    def search_company(self, company_name: str) -> list[DataPoint]:
        """公司维度的综合搜索（天眼查 + 社媒 + 招聘）"""
        ...
```

**MVP 接入优先级**：

| 数据源 | W2 状态 | 说明 |
|---|---|---|
| 天眼查 API | ✅ 必接 | 免费、结构化、G1 硬要求 |
| site: 小红书 | ✅ 必接 | 零成本、G6 要求 |
| site: 知乎 | ✅ 必接 | 零成本、G6 要求 |
| site: 黑猫投诉 | ✅ 必接 | 零成本、05 few-shot 引用 |
| site: 公众号（搜狗） | ✅ 必接 | 零成本、G1 核实渠道 |
| 第三方 API（小红书/抖音深度） | 🔌 占位 | 接口预留，默认关闭 |
| 七麦数据 | 🔌 按需 | 仅竞品有 App 时启用 |

**验收**：
```python
from app.chinese_sources import ChineseSourceManager

mgr = ChineseSourceManager()
# 天眼查搜索
results = mgr.search_company("字节跳动")
assert any(r.source_type == SourceType.COMMERCIAL_TIANYANCHA for r in results)

# site: 限定搜索
results = mgr.search("Notion 差评", [SourceType.SOCIAL_XIAOHONGSHU, SourceType.SOCIAL_ZHIHU])
assert len(results) >= 0  # 可能为空（取决于搜索结果），但不报错

# 第三方 API 关闭时不报错
results = mgr.search("xxx", [SourceType.SOCIAL_DOUYIN])  # 返回空列表，不 crash
```

#### Task 2.5 — 【新增】招聘数据 `recruitment_source.py`（0.5d）

**新建文件**：`app/recruitment_source.py`

> **对应 10 审核报告的「缺失 3」。**
> **依据：04 §四第5条（"招出海运营=要出海"）+ 05 §5 监控维度（招聘动向）+ 业界 CI 工具标配。**

具体内容：
```python
@dataclass
class RecruitmentData(DataPoint):
    position: str        # 职位名称
    department: str      # 部门
    company: str         # 公司名
    date: str | None     # 发布日期
    skills_hint: str | None  # 技能线索（反推技术栈/方向）
    source_url: str | None

class RecruitmentSource:
    """
    招聘数据聚合器
    数据来源（按优先级）：
    1. DuckDuckGo site: 限定搜索（zhipin.com / lagou.com / liepin.com）
    2. 公司官网招聘页（careers 页面定向抓取）
    输出用于反推战略方向（04 §四第5条）
    """

    def search_company(self, company_name: str, max_results: int = 10) -> list[RecruitmentData]:
        """搜索某公司的招聘信息"""
        # 1. site: 招聘网站搜索
        # 2. 官网 careers 页面抓取
        # 3. 去重合并 → RecruitmentData 列表

    def analyze_strategy_signals(self, data: list[RecruitmentData]) -> dict:
        """从招聘数据提炼战略信号"""
        # 返回：{expanding_depts: [], new_skills: [], geo_expansion: bool, ...}
```

**验收**：
```python
from app.recruitment_source import RecruitmentSource
results = RecruitmentSource().search_company("Notion")
assert isinstance(results, list)
# 不强求一定有结果（招聘网站可能有反爬），但接口必须稳定
```

#### Task 2.6 — 【新增】场景感知搜索策略 `scene_search.py`（0.5d）

**新建文件**：`app/scene_search.py`

> **对应 10 审核报告的「缺失 4」。**
> **问题：09 v1 的「场景感知搜索」只做了关键词拼接，实际 4 个场景需要差异化的数据源组合/权重/时效偏置。**

具体内容：

```python
@dataclass
class SceneSearchConfig:
    """单个场景的搜索策略配置"""
    scene: str
    keywords_templates: list[str]     # 关键词模板（{竞品} / {品类} 为变量）
    source_types: list[SourceType]    # 该场景启用的数据源
    weight: dict[str, float]          # 权重调整 {source_type: multiplier}
    freshness_bias: int | None = None  # 时效偏置（天），None = 不偏置
    special_actions: list[str] = field(default_factory=list)  # 特殊操作

# ===== 4 个场景的差异化策略 =====

SCENE_STRATEGIES: dict[str, SceneSearchConfig] = {
    "battle_card": SceneSearchConfig(
        scene="battle_card",
        keywords_templates=[
            "{竞品} vs {品类}",
            "{竞品} 弱点 评价",
            "{竞品} 差评",
            "{竞品} 对比 缺点",
            "{竞品} 替代",
        ],
        source_types=[
            SourceType.GENERAL_WEB,
            SourceType.SOCIAL_XIAOHONGSHU,   # 用户真实评价
            SourceType.SOCIAL_ZHIHU,          # 专业讨论
            SourceType.COMPLAINT_HEIMA,       # 投诉 = 进攻情报窗口
        ],
        weight={"sentiment_negative": 2.0},   # 差评/投诉权重加倍（找攻击面）
        freshness_bias=None,
    ),

    "pricing": SceneSearchConfig(
        scene="pricing",
        keywords_templates=[
            "{竞品} pricing 定价 价格方案",
            "{竞品} 降价 涨价 调价",
            "{竞品} 价格 方案 对比",
            "{竞品} 收费 标准",
        ],
        source_types=[
            SourceType.GENERAL_WEB,
            SourceType.OFFICIAL_PRICING,      # 官网价格页优先
            SourceType.SOCIAL_ZHIHU,           # 用户对定价的吐槽
        ],
        weight={"official_pricing_page": 3.0},
        freshness_bias=30,                     # 最近 30 天的定价信息更相关
        special_actions=["direct_fetch_pricing_page"],  # 直接抓取价格页
    ),

    "weekly": SceneSearchConfig(
        scene="weekly",
        keywords_templates=[
            "{竞品} 最新 news 2026",
            "{竞品} 融资 投资",
            "{竞品} 招聘 扩招",
            "{竞品} 产品更新 上线 新功能",
            "{竞品} 合作 战略",
        ],
        source_types=[
            SourceType.GENERAL_WEB,
            SourceType.WEIXIN_GZH,             # 官方公告
            SourceType.COMMERCIAL_TIANYANCHA,  # 融资/工商
            SourceType.RECRUITMENT,            # 招聘动向
            SourceType.OFFICIAL_CHANGELOG,     # 产品更新
        ],
        freshness_bias=7,                      # 最近 7 天权重高
    ),

    "discovery": SceneSearchConfig(
        scene="discovery",
        keywords_templates=[
            "{品类} 竞品 替代品 similar to",
            "{品类} 品牌 排行 推荐",
            "{品类} startup 初创公司",
            "{品类} 新玩家 入局",
        ],
        source_types=[
            SourceType.GENERAL_WEB,
            SourceType.COMMERCIAL_TIANYANCHA,   # 找公司
        ],
        diversity_requirement=True,            # 必须返回间接/降维/跨界类型
    ),
}

class SceneSearchEngine:
    """
    场景感知搜索引擎 —— 对外暴露的统一搜索入口
    复用 Searcher + ChineseSourceManager + RecruitmentSource 的能力
    """

    def __init__(self):
        self.searcher = Searcher()
        self.cn_sources = ChineseSourceManager()
        self.recruitment = RecruitmentSource()

    def search(self, scene: str, query: str, target: str | None = None) -> list[DataPoint]:
        """
        场景感知搜索主方法

        1. 从 SCENE_STRATEGIES 取出该场景的策略配置
        2. 渲染关键词模板（{竞品} → target, {品类} → 从 query 提取）
        3. 并发调用各数据源（通用搜索 + 中文数据源 + 招聘）
        4. 按权重排序 + 去重
        5. 返回统一的 DataPoint 列表
        """
        config = SCENE_STRATEGIES[scene]
        # ... 编排逻辑
```

**验收**：
```python
from app.scene_search import SceneSearchEngine

engine = SceneSearchEngine()

# battle_card 场景：应包含社媒差评 + 投诉
result = engine.search("battle_card", "客户拿 Notion 压我们", target="Notion")
source_types = set(dp.source_type for dp in result)
assert SourceType.GENERAL_WEB in source_types
# 如果天眼查/site:搜索有结果，应包含中文数据源

# pricing 场景：应触发官网价格页抓取
result = engine.search("pricing", "Notion 定价贵吗", target="Notion")
assert any(dp.source_type == SourceType.OFFICIAL_PRICING for dp in result) or \
       any("pricing" in (dp.url or "").lower() for dp in result)
```

#### Task 2.7 — 数据模型 `models.py`（0.5d）

**新建文件**：`app/models.py`（原 Task 2.4，扩展以支持多源数据）

具体内容：
```python
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime
from .chinese_sources import DataPoint, SourceType  # 复用统一数据格式

class Source(BaseModel):
    url: str
    title: str
    snippet: str           # 引用的原文片段
    source_type: SourceType = SourceType.GENERAL_WEB
    source_display: str = ""  # 显示名称：「网页搜索」「小红书」「天眼查」等
    relevance: str = "high"  # high / medium / low

class AnalysisResult(BaseModel):
    scene: str
    query: str
    analysis: str          # LLM 生成的分析文本
    sources: list[Source] = []
    data_points: list[DataPoint] = []  # 原始数据点（调试/审计用）
    confidence: str = "medium"  # high / medium / low
    ts: datetime = Field(default_factory=datetime.now)

    # 覆盖率指标（用于质量评估）
    coverage_summary: dict = Field(default_factory=dict)
    # 例：{"general_web": 3, "xiaohongshu": 1, "tianyancha": 1, "total": 5}

class CompareResult(BaseModel):
    targets: list[str]     # 对比的竞品列表
    matrix: dict           # 维度 → {竞品: 值}
    insights: list[str]    # 差异化洞察
    sources: list[Source]
```

**验收**：Pydantic 校验通过，`AnalysisResult(analysis="test")` 不报错，支持 `DataPoint` 作为原始数据附件

#### Task 2.8 — 引擎改造 `engine.py`（1.5d）【核心】

**改动文件**：`app/engine.py`（重大改造）

**改造前**（当前）：
```
用户 query → 加载 prompt → 直接丢 LLM → 返回纯文本
```

**改造后**（目标）：
```
用户 query
  → 1. 场景感知搜索（SceneSearchEngine：多源并发搜索）
  → 2. 内容抓取（Fetcher：Top N URL → 正文提取 + 定价页定向提取）
  → 3. 上下文组装（把多源 DataPoint 拼成结构化 reference 材料）
  → 4. Prompt 注入（system prompt + scene prompt + 参考资料区块）
  → 5. LLM 分析（基于真实数据的分析，而非"编"）
  → 6. 结果封装（AnalysisResult：analysis + sources + data_points + confidence + coverage）
  → 7. 返回结构化结果
```

**新的 `analyze()` 签名**：
```python
def analyze(scene: str, query: str, target: str | None = None) -> AnalysisResult:
    # 1. 场景感知多源搜索
    search_engine = SceneSearchEngine()
    data_points = search_engine.search(scene, query, target=target)

    # 2. 内容抓取（取有 URL 的 Top N 个 DataPoint）
    urls = [dp.url for dp in data_points if dp.url][:settings.fetch_max_urls]
    pages = Fetcher().fetch(urls)

    # 3. 组装上下文（结构化参考资料）
    context = build_structured_context(data_points, pages)
    # context 格式：
    # ## 参考资料（N 条来源）
    # ### 1. [Notion 定价方案](url) — 来源：官网
    # > 定价摘要...
    #
    # ### 2. [Notion 差评合集](url) — 来源：知乎
    # > 用户评价摘要...

    # 4. 调 LLM（prompt 里注入参考材料）
    system_prompt = _read("system.txt")
    scene_prompt = _read(SCENES[scene])
    enhanced_user_msg = f"{query}\n\n## 参考资料来源（{len(data_points)} 条）\n{context}"

    resp = client.chat.completions.create(
        model=settings.model,
        temperature=0.4,
        messages=[
            {"role": "system", "content": system_prompt + "\n\n" + scene_prompt},
            {"role": "user", "content": enhanced_user_msg},
        ],
    )

    # 5. 封装结果
    return AnalysisResult(
        scene=scene,
        query=query,
        analysis=resp.choices[0].message.content,
        sources=[Source(
            url=dp.url or "",
            title=dp.title,
            snippet=dp.content[:200],
            source_type=dp.source_type,
            source_display=dp.source_name,
        ) for dp in data_points if dp.content],
        data_points=data_points,
        confidence=_assess_confidence(data_points, pages),
        coverage_summary=_summarize_coverage(data_points),
    )
```

**Prompt 改造要点**（不改 `validation/prompts/*.txt` 文件，在注入时追加指令）：
- 在 user message 末尾追加 `## 参考资料来源` 区块（结构化，标注每条来源类型）
- 追加系统级指令：
  > 「以下是基于实时搜索获取的参考资料，你的分析必须基于这些材料：
  > 1. 标注信息来源（如 [来源：天眼查][来源：小红书]）
  > 2. 对无法验证的信息明确标注"待核实"
  > 3. 优先使用中文互联网来源的数据（G6 红线）
  > 4. 融资/工商数据必须引用天眼查等核实渠道（G1 红线）」

**优雅降级策略**：
```
搜索成功 + 有数据 → 正常多源分析
搜索成功但无中文数据 → 分析 + 警告「中文数据源暂无结果，以下部分待核实」
搜索完全失败 → 回退到纯 LLM 模式 + 标注「⚠️ 未能获取实时数据，以下基于模型知识（待核实）」
单数据源失败 → 跳过该源，用剩余数据继续（不因一个源失败而整体失败）
```

**验收**：
- `analyze("battle_card", "客户拿 Notion 压我们", target="Notion")` 返回 `AnalysisResult`
- `result.sources` 非空（至少 1 条搜索结果）
- `result.coverage_summary` 显示参与了哪些数据源类别
- `result.analysis` 包含具体数据/名称/来源引用，而非泛泛而谈
- 搜索失败时优雅降级（回退到纯 LLM 模式，不 crash）

#### Task 2.9 — Bot 适配 + 清理废弃文件（0.5d）

**改动文件**：`bot.py`, `webhook_server.py`, 删除 `main.py`/`feishu.py`/`sessions.py`

具体内容：
- [ ] `bot.py._async_analyze()` 适配新签名：`analyze()` 现在返回 `AnalysisResult` 而非 `str`
  - 展示逻辑：先显示分析正文，再附上来源链接列表（按来源类型分组显示）
  - 格式示例：
    ```
    {analysis 正文}

    📚 数据来源（N 条）
    🔵 通用搜索（3 条）
      · [标题](url)
    🟠 小红书（1 条）
      · [标题](url)
    🟢 天眼查（1 条）
      · [标题](url)

    ✅ 置信度：高/中/低
    ```
- [ ] `webhook_server.py/api_analyze` 适配：返回值从 `{scene, result: str}` 变成 `AnalysisResult.json()`
- [ ] **删除** `app/main.py`、`app/feishu.py`、`app/sessions.py`（已废弃）
- [ ] 更新 `backend/README.md` 废弃文件说明

**验收**：飞书群 @bot 触发分析 → 收到带来源链接的回复（按类型分组）；`/api/analyze` 返回含 `sources` + `coverage_summary` 的 JSON

#### W2 交付物

| 交付物 | 说明 | 对应缺口 |
|---|---|---|
| `app/searcher.py` | DuckDuckGo 通用搜索层 + site: 限定搜索 | 基础能力 |
| `app/fetcher.py` | 网页正文抓取 + **定价页定向提取** | 04 P0 定价监控 |
| `app/chinese_sources.py` | **中文数据源层：天眼查 / site:搜索 / 第三方API占位** | **G6 + G1 红线** |
| `app/recruitment_source.py` | **招聘数据聚合器** | **04 §四第5条** |
| `app/scene_search.py` | **场景感知搜索策略引擎（4 场景差异化编排）** | **05 四场景真实需求** |
| `app/models.py` | 结构化数据模型（含 SourceType/DataPoint/Coverage） | 多源基础 |
| `app/engine.py`（改造） | 多源搜索→抓取→LLM→带来源+覆盖率的结构化结果 | 核心跃升 |
| `app/config.py`（扩展） | 中文数据源/招聘/日志配置 | 基础设施 |
| `app/logger.py` | 统一日志 | 可观测性 |
| `bot.py`（适配） | 展示带来源+分组的分析结果 | 用户可见 |
| 废弃文件清理 | 删除 main.py/feishu.py/sessions.py | 代码卫生 |

#### W2 验收标准（含红线合规检查）

- [ ] `analyze("battle_card", "客户拿飞书文档压我们", target="飞书文档")` 返回带 ≥1 条 source 的结果
- [ ] **G6 合规**：`coverage_summary` 中包含中文数据源（天眼查/site:搜索至少 1 类有结果）
- [ ] **G1 合规**：涉及融资/工商信息的分析必须引用天眼查等结构化数据源
- [ ] 分析质量：结果中包含具体产品名/功能名/价格/来源引用，非泛泛空话
- [ ] 飞书群实测：@bot → 收到带 📚 分组来源 的回复
- [ ] 优雅降级：搜索失败/单源失败时不 crash，回退到纯 LLM + 明确标注
- [ ] **数据源覆盖率 ≥ 50%**（21 项中至少覆盖 11 项的实际查询能力，含 site: 间接覆盖）

---

### W3：飞书卡片渲染 + 对比分析 + 会话管理

**目标**：从 text 回复升级为富交互卡片；支持多竞品对比；会话上下文连续。

#### Task 3.1 — 飞书卡片渲染器 `card_renderer.py`（2d）

**新建文件**：`app/card_renderer.py`

**飞书卡片格式**：JSON Card（飞书 Interactive Card），支持：
- 标题 + 带标签栏（Markdown）
- 分栏布局（column_set）
- 可折叠区块（div/collapse）
- 按钮（button action）
- 链接（href）

**需要实现的卡片模板**：

**(A) 分析卡片**（对应 battle_card / pricing / discovery）：
```
┌─────────────────────────────────────┐
│ 🔍 竞品分析：Notion                  │
│ ─────────────────────────────────── │
│                                     │
│ 💡 核心洞察                          │
│ ┌─────────────────────────────────┐ │
│ │ Notion AI 正在降低协作门槛...    │ │
│ │ 建议你从垂直场景切入差异化...     │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ▼ 详细分析（可折叠）                  │
│ ┌─────────────────────────────────┐ │
│ │ 🏢 公司：融资$275M · 估值$10B    │ │
│ │ 📦 产品：AI+数据库一体化          │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 📚 数据来源                          │
│ 🔵 通用搜索（3）🟠 小红书（1）🟢 天眼查（1）│
│ · Crunchbase - Notion 公司概况      │
│ · 小红书 - 用户真实评价汇总          │
│ · 天眼查 - 最新融资轮次              │
│                                     │
│ [⚔️ 对比其他竞品] [📄 存为文档]       │
└─────────────────────────────────────┘
```

**(B) 对比卡片**（对应 `/compare` 或「和 XX 对比」）：
```
┌─────────────────────────────────────┐
│ ⚔️ Notion vs 飞书文档 vs Wolai       │
│ ─────────────────────────────────── │
│                                     │
│ 📊 对比矩阵                          │
│ ┌──────┬────────┬──────┬──────┐     │
│ │ 维度  │ Notion │ 飞书  │Wolai │     │
│ ├──────┼────────┼──────┼──────┤     │
│ │ 定价  │ $10起  │ 免费  │ ¥99  │     │
│ │ AI   │ ✅强   │ ✅强  │ ⚠️弱  │     │
│ │ 移动端│ ⚠️弱   │ ✅强  │ ⚠️弱  │     │
│ └──────┴────────┴──────┴──────┘     │
│                                     │
│ 💡 差异化启示                        │
│ 「做 Notion 数据库没做的垂直场景...」 │
│                                     │
│ [+加入更多竞品] [📄 存为文档]         │
└─────────────────────────────────────┘
```

**(C) 周报卡片**（对应 weekly）：
```
┌─────────────────────────────────────┐
│ 📋 竞品周报 | 2026-07-21 ~ 07-27     │
│ ─────────────────────────────────── │
│                                     │
│ 🔴 重要动态                          │
│ 1. Notion 发布 AI 写作助手 2.0      │
│ 2. 飞书文档上线智能表格              │
│ 3. Wolai 完成 A 轮融资              │
│                                     │
│ 🟡 值得关注                          │
│ · ...                              │
│                                     │
│ 💡 应对建议                          │
│ · ...                              │
│                                     │
│ [查看详情] [历史周报]                 │
└─────────────────────────────────────┘
```

**核心接口**：
```python
class CardRenderer:
    def render_analysis(result: AnalysisResult) -> dict   # → 飞书 Card JSON
    def render_compare(compare: CompareResult) -> dict    # → 飞书 Card JSON
    def render_weekly(result: AnalysisResult) -> dict     # → 飞书 Card JSON
    def render_text_fallback(text: str) -> dict           # → 纯文本 Card（降级）
```

**来源展示增强**（vs v1 的改进）：卡片中的来源区域按 `source_type` 分色显示，让用户一眼看出数据来自哪里。

**验收**：`CardRenderer().render_analysis(sample_result)` 输出合法飞书 Card JSON；在飞书群里正确渲染

#### Task 3.2 — Bot 卡片发送 + 对比分析（1.5d）

**改动文件**：`bot.py`, `webhook_server.py`

具体内容：
- [ ] `bot.py._reply()` 升级：支持 `msg_type="interactive"`（卡片）和 `"text"`（纯文本降级）
- [ ] 新增 `/compare A B [C...]` 命令解析 → 调用多次 `analyze` + 差异对比 LLM 调用
- [ ] `handle_message()` 记住最近分析的竞品（sessions），支持「和 XX 对比」省略主竞品
- [ ] 对比逻辑：
  ```
  对每个竞品分别 analyze → 收集各维度数据 → LLM 生成差异矩阵 + 启示
  ```

**验收**：群里发 `/compare Notion 飞书文档` → 收到对比卡片；发「和 Wolai 对比」→ 自动带上次主竞品

#### Task 3.3 — 会话管理重写 `sessions.py`（1d）

**重写文件**：`app/sessions.py`

具体内容：
- [ ] 从内存字典升级为 **SQLite 持久化**
- [ ] Schema：
  ```sql
  CREATE TABLE sessions (
      chat_id TEXT PRIMARY KEY,
      last_scene TEXT,
      last_query TEXT,
      last_targets TEXT,      -- JSON array: 最近分析的竞品列表
      updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
  );
  CREATE TABLE analysis_history (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      chat_id TEXT,
      scene TEXT,
      query TEXT,
      result_json TEXT,       -- AnalysisResult JSON
      coverage_json TEXT,     -- 覆盖率快照（用于审计数据源质量）
      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
  );
  ```
- [ ] 接口：
  ```python
  class SessionManager:
      def get_or_create(chat_id: str) -> Session
      def update_last_analysis(chat_id: str, scene: str, query: str, targets: list[str])
      def save_result(chat_id: str, result: AnalysisResult)
      def get_history(chat_id: str, limit: int = 10) -> list[AnalysisResult]
  ```
- [ ] 自动清理：30 天未活跃的 session

**验收**：重启服务后 session 不丢失；`get_history(chat_id)` 能查到之前的分析记录

#### Task 3.4 — Webhook 服务加固（0.5d）

**改动文件**：`webhook_server.py`

具体内容：
- [ ] 加请求日志中间件（method/path/status_code/耗时）
- [ ] 全局异常处理器（500 时返回友好错误，不暴露堆栈）
- [ ] 简单速率限制（同一 chat_id 1 分钟最多 3 次，防刷）
- [ ] CORS 中间件（`/api/*` 路由需要）

**验收**：故意触发异常 → 返回 500 + `{"error": "..."}` 而非 HTML 错误页；快速连续请求 → 429

#### W3 交付物

| 交付物 | 说明 |
|---|---|
| `app/card_renderer.py` | 飞书卡片渲染器（3 种卡片模板 + 来源分色） |
| `app/sessions.py`（重写） | SQLite 持久化会话管理（含覆盖率审计） |
| `bot.py`（升级） | 卡片发送 + 对比命令 + 上下文记忆 |
| `webhook_server.py`（加固） | 日志/限流/异常处理/CORS |

#### W3 验收标准
- [ ] 飞书群收到富交互卡片（非纯文本），可展开/收起
- [ ] 卡片中数据来源区域按类型分色显示（🔵通用 / 🟠社媒 / 🟢商业数据）
- [ ] `/compare Notion 飞书文档` 返回对比矩阵卡片
- [ ] 重启服务后「和 XX 对比」仍能记住上次的主竞品
- [ ] 分析历史可在会话中查询（含覆盖率快照）

---

### W4：监控雷达 + 定时周报 + 打磨内测

**目标**：从「被动问答」升级为「主动推送」——竞品一有动作就推群。

#### Task 4.1 — 监控雷达数据模型（0.5d）

**改动文件**：新建 `app/monitor.py`，扩展 `data/bot.db` schema

具体内容：
- [ ] 新增表：
  ```sql
  CREATE TABLE monitors (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      chat_id TEXT NOT NULL,          -- 推送到哪个群
      target_name TEXT NOT NULL,      -- 监控的竞品名
      scene TEXT DEFAULT 'weekly',    -- 监控类型
      keywords TEXT,                  -- 额外关键词（JSON array）
      source_types TEXT,              -- 监控的数据源类型（JSON array）
      is_active INTEGER DEFAULT 1,
      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
      last_check_at TIMESTAMP,        -- 上次检查时间
      last_snapshot TEXT,             -- 上次快照 JSON（用于 diff）
      UNIQUE(chat_id, target_name)
  );

  CREATE TABLE monitor_events (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      monitor_id INTEGER NOT NULL,
      event_type TEXT,                -- price_change / product_update / funding / hiring / sentiment
      event_summary TEXT,             -- 变化摘要
      event_detail TEXT,              -- 详细内容
      source_url TEXT,
      source_type TEXT,               -- 变化发现自哪个数据源
      detected_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
      FOREIGN KEY (monitor_id) REFERENCES monitors(id)
  );
  ```
- [ ] CRUD 接口：
  ```python
  class MonitorManager:
      def add_monitor(chat_id, target_name, scene, keywords, source_types) -> Monitor
      def remove_monitor(monitor_id)
      def list_monitors(chat_id) -> list[Monitor]
      def check_all_due() -> list[Monitor]  # 检查所有到期监控
  ```

**验收**：`add_monitor("oc_xxx", "Notion", "weekly", [], ["tianyancha", "general_web"])` 成功写入 DB

#### Task 4.2 — 变化检测引擎（1.5d）

**新建/改动文件**：`app/detector.py`

具体内容：
- [ ] 定期搜索逻辑：
  ```
  对每个 active monitor:
    1. 用 target_name + keywords 多源搜索（复用 SceneSearchEngine）
    2. 抓取 Top URL（复用 Fetcher）
    3. 与上次快照做 diff（文本相似度 / 关键实体变化 / 定价数值对比）
    4. 若变化超过阈值 → 生成 monitor_event（标注 source_type）
  ```
- [ ] 变化类型识别（用 LLM 分类）：
  - `price_change` — 定价调整（04 P0 核心维度）
  - `product_update` — 产品功能变更
  - `funding` — 融资/投资动态（天眼查驱动）
  - `hiring` — 招聘动向（暗示战略重点转移）
  - `sentiment` — 舆情变化（差评激增/好评爆发，社媒驱动）
- [ ] 去重：同一天内同类事件合并
- [ ] **定价变化专项检测**：对官网价格页做数值 diff（04 P0）

**验收**：手动触发检测 → 发现模拟变化 → 生成 event 记录（含 source_type）

#### Task 4.3 — 定时任务 + 主动推送（1d）

**改动文件**：新建 `app/scheduler.py`, `bot.py`

具体内容：
- [ ] 调度器（用 `apscheduler` 或简单 `threading.Timer` 循环）：
  ```python
  class Scheduler:
      def start():
          """启动后台定时任务"""
          # 每 6 小时检查一次监控（开发期可缩短为 30 分钟测试）
          # 每周一 9:00 推送周报
      def check_monitors():
          """检查所有到期监控 → 有变化就推群"""
      def push_weekly(chat_id: str):
          """推送周报到指定群"""
  ```
- [ ] 推送逻辑：
  - 有变化 → 组装事件卡片 → `bot._reply(card_json)` 推群
  - 周报 → 调 `analyze("weekly", ...)` → 渲染周报卡片 → 推群
- [ ] Bot 新增命令：
  - `/monitor add Notion` — 添加监控
  - `/monitor list` — 查看当前监控列表
  - `/monitor remove 1` — 移除监控
  - `/report` — 手动触发周报

**验收**：`/monitor add Notion` → 成功添加；定时触发 → 群里收到变化推送（含来源标注）

#### Task 4.4 — 内测打磨（1d）

**涉及文件**：全部

具体内容：
- [ ] 错误消息优化：用户友好的中文提示，不含堆栈
- [ ] 边界情况处理：
  - 搜索无结果 → 「未找到足够信息，基于已有知识为您分析（以下内容待核实）」
  - LLM 超时 → 30s 超时兜底，返回部分结果 + 重试提示
  - 飞书 API 限流 → 排队重试（exponential backoff）
  - 单数据源失败 → 跳过该源，用剩余数据继续（W2 已实现的基础上的体验优化）
- [ ] `.env.example` 补全所有配置项 + 注释
- [ ] `README.md` 重写：完整的部署/配置/使用文档
- [ ] 性能基准：单次分析端到端 < 20s（W2 因多源搜索增加了耗时，从 15s 放宽到 20s）

**验收**：断网情况下 bot 不 crash；各种异常都有友好降级

#### Task 4.5 — 部署方案（0.5d）

**新建文件**：`deploy/` 或文档章节

具体内容：
- [ ] 开发环境一键启动脚本（`start.bat` / `start.sh`）：
  ```bash
  # start.bat
  cd backend
  call .venv\Scripts\activate
  start uvicorn webhook_server:app --host 0.0.0.0 --port 8011
  start ngrok http 8011
  ```
- [ ] 生产部署选项文档：
  - **轻量**：VPS + systemd + nginx 反代（月费 ¥30–50）
  - **推荐**：Railway / Fly.io（自动 HTTPS + CDN，按量付费）
  - **企业**：腾讯云/阿里云 ECS + CLB
- [ ] Dockerfile（可选，预留）：

**验收**：`start.bat` 双击即可在本地跑起完整服务

#### W4 交付物

| 交付物 | 说明 |
|---|---|
| `app/monitor.py` | 监控雷达 CRUD（含 source_types 配置） |
| `app/detector.py` | 变化检测引擎（多源 diff + 定价专项） |
| `app/scheduler.py` | 定时任务 + 主动推送 |
| `deploy/start.bat` | 一键启动脚本 |
| 完整 README | 部署/配置/使用文档 |

#### W4 验收标准
- [ ] `/monitor add Notion` → 添加成功 → 定时检测到变化 → 推群（含来源标注）
- [ ] 周一早上 9 点自动收到竞品周报
- [ ] `start.bat` 一键启动全链路
- [ ] 断网/超时/API 限流/单源失败等边界情况均有友好降级

---

## 4. 依赖关系图

```
W2 (多源搜索+引擎改造) — 9 个 Task
  ├─ Task 2.1 基础设施（config/logger/deps）──→ 无依赖，最先做
  │
  ├─ Task 2.2 通用搜索 searcher.py ──────┐
  ├─ Task 2.3 抓取层 fetcher.py ─────────┤
  ├─ Task 2.4 中文数据源 chinese_sources ─┼→ Task 2.6 场景感知搜索 scene_search
  ├─ Task 2.5 招聘数据 recruitment_source ┤        ↓
  └─ Task 2.7 数据模型 models.py ────────┘→ Task 2.8 引擎改造 engine.py（核心）
                                                      ↓
                                                Task 2.9 Bot 适配 + 清理

W3 (卡片+对比+会话) — 4 个 Task
  ├─ Task 3.1 card_renderer.py ──→ 依赖 W2 的 models.py
  ├─ Task 3.2 Bot 卡片 + 对比 ──→ 依赖 3.1 + W2 engine
  ├─ Task 3.3 sessions.py 重写 ──→ 可与 3.1 并行
  └─ Task 3.4 Webhook 加固 ──→ 无依赖，可随时做

W4 (监控+周报+打磨) — 5 个 Task
  ├─ Task 4.1 监控数据模型 ──→ 依赖 W3 sessions（共用 DB）
  ├─ Task 4.2 变化检测引擎 ──→ 依赖 4.1 + W2 searcher/fetcher/chinese_sources
  ├─ Task 4.3 定时任务 + 推送 ──→ 依赖 4.2 + W3 card_renderer
  ├─ Task 4.4 内测打磨 ──→ 依赖前面全部
  └─ Task 4.5 部署方案 ──→ 依赖 4.4
```

---

## 5. 风险与应对（v2 更新）

| 风险 | 影响 | 概率 | 应对 |
|---|---|---|---|
| DuckDuckGo 搜索质量不稳 | 分析结果偏浅 | 中 | **多源互补**：中文数据源填补 DDG 在中文内容的不足 |
| 网页抓取反爬（403/JS 渲染） | 拿不到正文 | 中 | User-Agent 轮换 + Jina Reader fallback |
| **天眼查 API 额度用完/限流** | 结构化商业数据不可用 | 中 | **优雅降级**：标记「天眼查暂时不可用，融资/工商数据待核实」+ site: 搜索补充 |
| **中文 site: 搜索结果稀少** | 社媒数据不足 | 中-高 | **分级降级**：有则用、无则标注、不 block 主流程；后续接第三方 API 补齐 |
| LLM 仍编造数据（搜索有但没读进上下文） | 质量红线 | 中 | Prompt 强约束「必须基于参考资料」+ P0 验收回归 + **覆盖率审计**（每次分析记录用了哪些源） |
| 飞书卡片审核不通过 | 无法发送卡片 | 低 | 先用 text 模式跑通，卡片逐步迭代 |
| 单次分析耗时 >20s（多源搜索叠加） | 用户体验差 | 低 | **多源并行搜索**（async gather）+ 流式回复（先发"🔍 正在从 N 个数据源搜集信息…"） |
| 定时任务进程挂掉 | 监控停摆 | 中 | 进程守护（supervisor/systemd）+ 健康检查告警 |
| **第三方数据 API 成本超预期** | 运营成本 | 低 | **默认关闭**，按需开启；用量监控 + 月预算告警 |

---

## 6. 不做的事（V2+）

| 不做 | 原因 | 何时重新评估 |
|---|---|---|
| 自研爬虫集群 | 成本高、法律风险、维护重 | 日分析量 > 1000 时 |
| 独立 Web 仪表盘 | SMB 用飞书群就够了 | 有多个企业客户要求时 |
| 复杂 SSO/权限系统 | MVP 不需要 | 有团队付费客户时 |
| 多语言（非中英） | SMB 目标市场在中美 | 拓展东南亚/欧洲时 |
| 移动 App | 飞书移动端就是入口 | 用户强烈要求独立 App 时 |
| **微博/B站/脉脉深掘** | P2 优先级；MVP 先覆盖 Top 5（小红书/抖音/公众号/知乎/黑猫） | MVP 验证后有明确需求时 |
| **广告素材库（巨量引擎等）** | P2；需要广告平台账号或第三方服务 | 有客户明确提出时 |
| **Sensor Tower / App Annie** | 付费较贵（$79+/月）；MVP 用七麦免费版即可 | 需要海外 App 数据时 |

---

## 7. 数据源覆盖路线图

```
W2 结束（v2 目标）：~70%（14/21）
  ✅ 通用搜索（DuckDuckGo）
  ✅ 网页正文提取（trafilatura）
  ✅ 天眼查（API）
  ✅ site: 小红书 / 知乎 / 黑猫 / 公众号（搜狗）
  ✅ 招聘数据（site: 搜索 + 官网招聘页）
  ✅ 官网价格页（定向抓取）
  ✅ 官网 changelog（定向抓取）
  ⚠️ 抖音（仅 site: 有限覆盖）
  ⚠️ 七麦（按需接入）
  🔌 第三方 API（占位，默认关闭）
  ❌ 微博 / B站 / 脉脉（P2，V2）
  ❌ 广告素材库（P2，V2）
  ❌ 电商平台比价（消费品专用，V2）

W3-W4 结束：~80%（17/21）
  + App Store 数据（七麦，按需）
  + 定价变化 diff 检测
  + 监控事件多源标注
  ❌ 仍缺：微博/B站/脉脉/广告/电商（均为 P2）

V2 目标：90%+（19/21+）
  + 第三方 API 正式接入（小红书/抖音深度数据）
  + 微博/B站/脉脉（如有需求）
```

---

## 8. 实施节奏建议

| 建议 | 理由 |
|---|---|
| **每个 Task 完成后 commit**，不要攒到周末 | 方便回滚 + code review |
| **每完成一个周就飞书群实测一轮** | 早发现体验问题 |
| **W2 最优先**——这是质量跃升的核心 | 没有多源搜索/抓取，卡片再花也没用 |
| **保持 `validation/prompts/*.txt` 唯一真相** | 绝不在 engine/card 里硬编码提示词 |
| **W2 Task 2.4（中文数据源）是关键路径** | 这是 v2 vs v1 的最大差异，也是通过验收的关键 |
| **先跑通天眼查 + site: 搜索，再考虑第三方 API** | 降低外部依赖风险，快速验证核心假设 |
| **每个 analyze() 调用都记录覆盖率快照** | 用于后续审计数据源效果，指导 V2 优化 |

---

## 附录：v1 → v2 变更清单

| 变更项 | v1 | v2 | 原因 |
|---|---|---|---|
| 依据文件 | 2 个（07 + 02） | **7 个**（+01/04/05/06 + 代码现状） | 审核发现遗漏关键约束 |
| W2 Task 数 | 6 | **9**（+3） | 补入中文数据源/招聘/场景感知搜索 |
| 数据源覆盖率目标 | 未明确 | **W2 ≥ 50%，最终 ≥ 90%** | 量化验收标准 |
| 中文数据层 | 无 | **chinese_sources.py** | G6 红线合规 |
| 招聘数据 | 无 | **recruitment_source.py** | 04 §四 + 业界标配 |
| 场景感知搜索 | 简单关键词拼接 | **scene_search.py（4 场景差异化编排）** | 05 四场景真实需求 |
| 数据模型 | 简单 Source/AnalysisResult | **SourceType 枚举 + DataPoint + CoverageSummary** | 多源归一化 |
| 引擎输入 | 纯文本 query | **多源 DataPoint 列表** | 质量跃升核心 |
| 引擎输出 | analysis + sources | **+ data_points + coverage_summary + confidence** | 可审计 |
| 优雅降级 | 简单回退 | **分级降级（逐源失败/中文缺失/全失败）** | 生产可靠性 |
| 定价页抓取 | 通用抓取 | **定价页定向提取（结构化）** | 04 P0 |
| 风险项 | 6 | **9**（+3 项中文数据相关风险） | 新增能力的风险面 |
| 目录结构 | 12 个文件 | **16 个文件**（+4 新模块） | 功能扩展 |

---

## 附录B：项目全貌（W1–W7+）

```
W1  ✅ 已完成（2026-07-24）     预研骨架 + MVP Gate
    交付：webhook_server / engine(bot) / config + 飞书端到端 + 4场景P0验收

W2  🔲 当前阶段                 多源搜索引擎接入 + 引擎改造
    目标：从「纯LLM编报告」→「基于真实多源数据分析」
    工期：~7d | Task：9个

W3  📋 待细化                  飞书卡片渲染 + 对比分析 + 会话管理
    目标：text → 富交互卡片 + 多竞品对比 + SQLite会话
    工期：~5d | Task：4个（骨架已有，执行细节W2结束后出）

W4  📋 待细化                  监控雷达 + 定时周报 + 打磨内测
    目标：被动问答 → 主动推送 + 生产可用
    工期：~5d | Task：5个（骨架已有，执行细节W3结束后出）
    ────────────────────────────── MVP 内测版分界线 ──────────────────────────────

W5  📋 小规模真实用户验证       2-3 个 SMB 试用 + 反馈收集
    关键问题：
    · 用户真的在飞书群里用吗？还是想要独立 Web 入口？
    · 哪些数据源最有用？哪些是噪音？
    · 分析质量够不够支撑付费决策？
    · 定价敏感度测试（免费/¥99/¥299/自定义？）
    输出：用户行为数据 + PMF 验证/否定信号 + 迭代方向

W6  📋 基于反馈迭代 + 商业化准备
    可能方向（取决于 W5 发现）：
    · 接入更多第三方 API（小红书/抖音深度数据）
    · 补入 P2 数据源（微博/B站/广告素材库）
    · 多租户架构（多企业支持 + 数据隔离）
    · 用量计费 / 套餐设计 / 试用转正流程
    · 部署方案落地（VPS or Railway or 腾讯云）
    · if PMF 未验证 → pivot 方向评估

W7+ 📋 正式发布 + 获客
    取决于 W5-W6 的发现，不提前规划细节
```

### 滚动计划节奏

```
当前动作：细化 W2 执行手册 → 开干
W2 结束时：飞书群实测验证 → 基于真实经验细化 W3 执行手册
W3 结束时：卡片交互验证 → 细化 W4 执行手册
W4 结束时：MVP 内测版交付 → 制定 W5 用户验证计划
W5 结束时：PMF 判断 → 决定 W6 是迭代还是 pivot
```

---

## 附录C：v3 修订摘要（2026-07-25）

> 基于 `13-项目计划批判性审视（反驳者_市场_PM三视角）.md` + `14-竞品体验对比报告.md` 的实测发现修订。
> **v2.1 原文保留作历史参考，开发执行以本附录为准。**

### C.1 修订背景

- `13` 审视发现 3 个致命假设：① MVP Gate 是假闭环（验红线不验洞察）② 中文护城河技术不成立（天眼查按次计费、DDG 中文索引弱）③ 最大风险（付费意愿）排在最后
- `14` 对比实验实测：**裸 DeepSeek 竞品分析质量已够 SMB 用**，本产品唯一硬差异化 = 监控推送（裸模型无持久化/定时/diff）
- 用户决策（2026-07-25）：不做 concierge 预验证（先开发完再验证）；G6 降级；监控提前；定价重做；竞品对比（已完成，见 `14`）

### C.2 五个变更点

| # | 变更 | v2.1 | v3 |
|---|---|---|---|
| 1 | G6 中文互联网优先 | 红线（阻塞 Gate） | **降级为目标**（不阻塞，做占位） |
| 2 | 监控推送 | W4 才做 | **提前到 W2**（核心差异化尽早验证） |
| 3 | 中文数据源层 | W2 必接（天眼查+site:） | **降为占位接口**（默认关闭，W4/W5 按需启用） |
| 4 | W2 核心 | 多源搜索 + 引擎改造 | **监控推送最小版 + 引擎轻改造** |
| 5 | W2 验收门槛 | 数据源覆盖率 ≥50% + G6 合规 | 覆盖率 ≥30% + **监控推群可用**（G6 改目标） |

### C.3 新 W2 任务清单（v3，替代 v2.1 的 9 个 Task）

**W2 目标变更**：从「多源搜索质量跃升」改为「**监控推送最小可用 + 引擎轻改造**」——尽早把唯一硬差异化做出来验证留存假设。

| Task | 内容 | v2.1 对应 | 变化 |
|---|---|---|---|
| 2.1 | 基础设施加固（config/logger/deps） | 原 2.1 | 不变 |
| 2.2 | 通用搜索 searcher.py（**仅 DuckDuckGo，砍 site: 中文源**） | 原 2.2 | 简化 |
| 2.3 | 抓取 fetcher.py（trafilatura，**砍定价页定向提取移 W4**） | 原 2.3 | 简化 |
| 2.4 | **【新核心】监控雷达最小版** monitor.py + scheduler.py（CRUD + 定时搜索 + 推群） | 原 W4 Task 4.1+4.3 提前 | **提前** |
| 2.5 | **【新核心】变化检测最小版** detector.py（简单文本/关键词 diff，不做类型识别） | 原 W4 Task 4.2 提前简化 | **提前简化** |
| 2.6 | 数据模型 models.py | 原 2.7 | 不变 |
| 2.7 | 引擎轻改造 engine.py（搜索→组装→LLM，**中文源占位**） | 原 2.8 简化 | 简化 |
| 2.8 | 中文数据源占位 chinese_sources.py（**接口预留，默认关闭**） | 原 2.4 降级 | **降为占位** |
| 2.9 | Bot 适配 + **监控命令**（/monitor add/list/remove）+ 清理废弃文件 | 原 2.9 + 监控命令 | 加监控命令 |

**依赖关系（v3）**：
```
2.1 基础设施
  ├─ 2.2 通用搜索 ──────┐
  ├─ 2.3 抓取 ──────────┼→ 2.7 引擎轻改造 ──→ 2.9 Bot适配+监控命令
  ├─ 2.6 数据模型 ──────┤         ↑
  ├─ 2.8 中文源占位 ────┘    2.4 监控雷达 + 2.5 变化检测（复用 2.2/2.7）
```

### C.4 W2 验收标准（v3）

- [ ] `analyze()` 返回带来源的结果（通用 web 搜索即可，**不强求中文源**）
- [ ] **`/monitor add 竞品X` → 添加成功 → 定时搜索 → 有变化推群**（核心验收，唯一硬差异化）
- [ ] **G6 改为目标非红线**：中文数据源有则用、无则标注「待核实」，不阻塞 Gate
- [ ] 优雅降级：搜索失败回退纯 LLM + 标注
- [ ] 数据源覆盖率 ≥ 30%（v2.1 是 50%，因中文源降级下调）

### C.5 W4 调整（v3）

W4 从「监控+周报+打磨」改为「**中文数据源增强 + 变化检测深化 + 打磨**」：
- Task 4.1 中文数据源从占位升级为实接（天眼查按需付费 + site: 尽力）——**是否实接取决于 W5 用户验证是否在乎来源引用**（`14` 启示 2：实测证明没有它质量也够）
- Task 4.2 变化类型识别（定价/产品/融资/招聘）+ 定价专项 diff
- Task 4.3 周报定时推送
- Task 4.4 打磨 + 部署

### C.6 定价模型重做（详见 04 §五 v2）

- 免费层：**不送监控**（最贵功能），送每月 3 次单次分析
- 监控是付费功能（¥99/月起）
- 毛利底线 ≥ 60%（达不到就砍数据源或涨价）

### C.7 不变的约定

- 提示词唯一入口 `validation/prompts/*.txt`
- 风险测试用未见过的竞品
- `.env` gitignore，API Key 不入库
- 提交规范（README §10）
- 滚动计划（W2 结束再细化 W3）
