# 17 - W3 滚动计划（飞书卡片 + 对比 + 会话）

> **v3 横幅**：本计划基于 `09` §779-958（W3：飞书卡片渲染 + 对比分析 + 会话管理），按 v3 现实修正。
> **监控已在 W2 提前完成**（monitor.py/detector.py + 后台调度），故 W3 不含监控逻辑，只做卡片/对比/会话/加固。
> 设计来源：`09` §779-958；W2 已交付：`models.py`(AnalysisResult/CompareResult/Source) / `engine.py`(analyze 返回结构化) / `bot.py`(`/monitor`+`push`) / `webhook_server.py`(lifespan 调度)。
> 模式：滚动计划（B）——只细化当前阶段（W3），W4 等 W3 验收后再出执行手册。

---

## 一、W3 目标（v3 修正版）

把 text 回复升级为**富交互卡片**；支持**多竞品对比**；会话上下文**连续且持久化**。

| 维度 | v2.1 原设计 | v3 修正 |
|---|---|---|
| 卡片来源分色 | 🔵通用 / 🟠社媒 / 🟢商业数据 三色 | **当前仅 🔵通用**（v3 中文源占位未实接）；渲染时按实际 `source_type` 动态显示，🟠🟢 待 W4/W5 接中文源后出现，**不硬编码假数据** |
| 监控 | W3 不涉及 | W2 已交付，本阶段不重复 |
| sessions | 重写 SQLite | W2 清理的 `sessions.py` 是废弃骨架，W3 按新 Schema **重建**（非改） |

---

## 二、前置依赖（W2 已就绪，W3 直接消费）

- `app/models.py`：`AnalysisResult`(scene/query/analysis/sources/confidence/coverage_summary/note) · `CompareResult`(targets/matrix/insights/sources) · `Source`(url/title/snippet/source_type/source_display/relevance)
- `app/engine.py`：`analyze(scene, query) -> AnalysisResult`（已结构化返回）
- `app/bot.py`：`LarkBot` 已有 `push(chat_id, text)`；`handle_message` 场景路由在 W2 落地
- `app/config.py`：`db_path` 已配（SQLite 路径，W2 监控已用）
- `backend/.gitignore`：已排除 `data/`（运行时 SQLite）

---

## 三、Task 3.1–3.4（按真实代码边界）

### Task 3.1 — 飞书卡片渲染器 `card_renderer.py`（2d）
**新建** `app/card_renderer.py`，输出飞书 Interactive Card JSON。

**接口**（对齐 `models.py`，不新增字段）：
```python
class CardRenderer:
    def render_analysis(self, result: AnalysisResult) -> dict   # 分析卡片
    def render_compare(self, compare: CompareResult) -> dict    # 对比卡片
    def render_weekly(self, result: AnalysisResult) -> dict     # 周报卡片（字段复用 analysis + sources）
    def render_text_fallback(self, text: str) -> dict           # 纯文本卡片降级
```

**来源展示（v3 修正）**：来源区按 `Source.source_type` 聚合计数，仅渲染**实际存在的类型**（当前只有 `GENERAL`→🔵通用）。**禁止**在卡片里写「🟠小红书 / 🟢天眼查」假数据——v3 中文源未实接前这些计数恒为 0。

**验收（本地，不需飞书）**：
```bash
cd backend
.\.venv\Scripts\python.exe -c "from app.card_renderer import CardRenderer; from app.models import AnalysisResult, Source; r=AnalysisResult(analysis='x', sources=[Source(url='u', title='t', source_display='通用搜索')]); c=CardRenderer().render_analysis(r); print('card keys:', list(c.keys()))"
```
→ 输出合法飞书 Card 结构（含 `msg_type=interactive` / `card` 节点）；飞书群实际渲染待用户本地实测。

### Task 3.2 — Bot 卡片发送 + 对比命令（1.5d）
**改** `bot.py` / `webhook_server.py`：
- [ ] `_reply` / `push` 支持 `msg_type="interactive"`（卡片）与 `"text"`（降级）
- [ ] 新增 `/compare A B [C...]`：循环 `analyze` 各竞品 → LLM 生成差异矩阵 → 封装 `CompareResult` → `render_compare`
- [ ] 会话记忆：记住最近主竞品，支持「和 XX 对比」省略主竞品（依赖 3.3 SessionManager）
- [ ] 分析完成后调用 `render_analysis` 而非裸 `res.analysis`

**验收（本地）**：构造两个 `AnalysisResult` 跑 `render_compare` 出合法 JSON；完整 `@` 交互待飞书实测。

### Task 3.3 — 会话管理重建 `sessions.py`（1d）
**新建（重建）** `app/sessions.py`，SQLite 持久化（W2 清理的是旧废弃骨架，此处按 `09` §895-930 Schema 重建）：
```sql
CREATE TABLE sessions (chat_id TEXT PRIMARY KEY, last_scene TEXT, last_query TEXT, last_targets TEXT, updated_at TIMESTAMP);
CREATE TABLE analysis_history (id INTEGER PRIMARY KEY AUTOINCREMENT, chat_id TEXT, scene TEXT, query TEXT, result_json TEXT, coverage_json TEXT, created_at TIMESTAMP);
```
接口：`get_or_create` / `update_last_analysis` / `save_result` / `get_history(limit=10)`；30 天未活跃自动清理。
**验收**：重启服务后 session 不丢；`get_history` 能查到历史（复用 W2 的 `db_path`）。

### Task 3.4 — Webhook 服务加固（0.5d）
**改** `webhook_server.py`：
- [ ] 请求日志中间件（method/path/status/耗时）
- [ ] 全局异常处理器（500 → `{"error": "..."}` 不暴露堆栈）
- [ ] 速率限制（同 chat_id 1 分钟 ≤3 次）
- [ ] CORS 中间件（`/api/*` 需要）

**验收（本地）**：故意触发异常 → 返回 JSON 500；快速连发 → 429。

---

## 四、W3 交付物 / 验收标准

| 交付物 | 说明 |
|---|---|
| `app/card_renderer.py` | 3 种卡片模板 + 来源动态分色（仅实际类型） |
| `app/sessions.py`（重建） | SQLite 持久化会话 + 覆盖率审计 |
| `bot.py`（升级） | 卡片发送 + `/compare` + 上下文记忆 |
| `webhook_server.py`（加固） | 日志/限流/异常/CORS |

**验收标准**：
- [ ] 卡片为富交互（非纯文本），可展开/收起
- [ ] 来源区只显示实际有的类型（当前仅 🔵通用），无假数据
- [ ] `/compare A B` 返回对比矩阵卡片
- [ ] 重启后仍记得上次主竞品
- [ ] 分析历史可查（含 coverage 快照）

---

## 五、验证方法（分层）

| 层 | 方法 | 谁跑 |
|---|---|---|
| 代码层 | 本文件 §三 各 Task 的本地 python 片段（无需飞书/网络） | 我（本地冒烟） |
| 卡片渲染 | `render_*` 输出 JSON 结构校验 | 我 + 用户复查 |
| 飞书集成 | 群里发 `/compare`、看卡片渲染、重启服务验证记忆 | 用户本地实测（需飞书 + ngrok） |

---

## 六、W3 → W4 交接

- W3 验收后，基于真实卡片交互经验细化 `W4`（定时周报自动进群、监控变化类型识别、中文源实接依 W5 验证决定）。
- W4 的监控部分在 W2 已做最小版，W4 主要补**变化类型识别** + **周报定时进群** + **打磨内测**。
- 风险：W3 卡片渲染依赖飞书 Card Schema，需用户本地实测确认渲染无误（沙箱无法代跑）。
