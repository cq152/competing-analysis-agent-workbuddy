# 07 · 自建后端技术方案（预研骨架）

> 状态：**预研 / Gate 前准备**。正式开发待 `06` §4.5 验收 Gate 全过 + 满足 `03` §七升级判定。
> 依据：`02-产品设计文档.md` 的架构参考；产品形态采用 Aily 阶段已验证的 SMB 场景（`04`/`05`），**非另起炉灶做 02 的"战略报告"**。

---

## 1. 与 `02` 文档的关系

- `02` 是「战略分析师 / 三层深度报告」的架构参考：飞书 Webhook + FastAPI + 分析引擎（SearchAgent/FetchAgent/AnalyzeAgent/CardRenderer）+ 卡片渲染。
- 本项目主线已定为 SMB「雷达 + 军师」（`04`/`05`）。后端是其**工程化升级**——把 Aily 里验证过质量的 4 个场景（battle_card / pricing / weekly / discovery）+ 监控雷达，升级为带富交互、持续监控、可对外 API 的真产品。
- **互补而非替代**：Aily 先低成本的验证分析质量；后端补 Aily 做不到的事——对比矩阵富交互、存为飞书文档、持续监控、定时任务、Web/自有 API。

## 2. 核心设计原则

1. **提示词唯一真相 = `validation/prompts/*.txt`**：后端 `engine` 直接加载，不重复存放（守 `README §10` 纪律，杜绝飞书/后端漂移）。改提示词只改 `validation/`，后端自动吃到。
2. **LLM 用 OpenAI 兼容协议**：DeepSeek / Moonshot / 通义 / 智谱可一行切换（`config` 读 `.env`）。
3. **最小可运行优先**：先打通 消息 → 意图 → 分析 → 回复 主链路；卡片渲染、搜索抓取、监控雷达按周迭代。

## 3. 架构

```
飞书开放平台
   │  im.message.receive_v1
   ▼
FastAPI (app.main)
   ├─ /webhook/feishu  → URL 验证 + 事件解析
   ├─ 意图解析 (parse_intent)
   ├─ 会话状态 (sessions)
   └─ 分析引擎 (engine)
          │  加载 validation/prompts/*.txt + 调 LLM
          ▼
       结果 → 卡片渲染 (feishu) → 飞书回复
            （规划）监控雷达(定时) → 变化检测 → 主动推群
```

## 4. 目录结构（已落地骨架）

```
backend/
  app/
    __init__.py
    main.py        # /health · /webhook/feishu · /api/analyze
    config.py      # 配置（读 .env）
    engine.py      # 分析引擎：加载 prompts + 调 LLM
    feishu.py      # 飞书 webhook 验证 + 卡片发送 stub
    sessions.py    # 会话状态（内存版）
  requirements.txt
  .env.example
  README.md
```

## 5. 接口契约

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/health` | 存活检查，返回可用场景列表 |
| POST | `/api/analyze` | `{scene, query, session_id?}` → `{scene, result}`，真调 LLM |
| POST | `/webhook/feishu` | 飞书 URL 验证（返回 `challenge`）/ 事件接收占位 |
| _规划_ | `/api/compare` | 2–5 竞品对比矩阵 |
| _规划_ | `/api/monitor` | 监控雷达增删 + 变化检测 |

## 6. 数据模型（骨架）

- `Session`：`session_id → {scene, query, ts}`，内存字典（生产换 Redis/SQLite）。
- `Report`：`{scene, result, sources?, ts}`。

## 7. 技术栈

FastAPI + Uvicorn + OpenAI SDK（兼容） + DeepSeek（默认） + SQLite（生产存储） + Railway/Vercel（部署）。

## 8. 实施步骤（待 Gate 后正式投入）

| 周 | 目标 | 对应骨架 |
|---|---|---|
| W1 | webhook 收发 + 意图解析 + `/api/analyze` 接 engine | main / feishu / sessions / engine ✅ 已预研 |
| W2 | 分析引擎深化：接搜索/抓取，替代纯 LLM 合成 | engine 扩展 |
| W3 | 卡片渲染 + 对比 + 展开收起 + 存为飞书文档 | feishu 卡片 |
| W4 | 监控雷达 + 定时周报 + 打磨内测 | /api/monitor + 定时任务 |

## 9. 与 Aily 的取舍

- **Aily 做不到、后端补**：对比矩阵富交互、存为飞书文档、持续监控与变化检测、定时任务、Web/自有 API、团队协作。
- **Aily 阶段已验证的提示词直接复用**：`engine` 加载 `validation/prompts`，零返工。

## 10. 当前骨架状态

- ✅ 已实现：`/health`、`/api/analyze`（真调 LLM 产出 battle_card 等）、`/webhook/feishu`（challenge 验证）、`sessions` 内存、`engine` 复用 `validation/prompts`。
- ⬜ 未实现：飞书真实收发、卡片渲染、搜索/抓取接入、监控雷达、SQLite。
- ✅ 已验证：`uvicorn` 起服 → `curl /health`=200 → `curl /api/analyze` 真跑出 battle_card 结果。

---

*本文件为路线 B 的预研与最小骨架，不阻塞当前 Aily 验收主线。Gate 过后再投入 W2–W4。*
