# 06 - 功能测试重点与项目红线

> 本文档沉淀自 `03`（Aily 指南 + 系统提示词）、`04`（SMB 定位）、`05`（场景提示词 + 样例）以及 `validation/` 本地验证脚手架的**真实跑测**。
> 用途：**功能验收的单一参考**——红线是硬约束，风险测试是已踩过的坑，后面是继续要做的测试要点与可直接抄进测试群的用例。

---

## 一、项目经验总结（来自本次对话）

1. **最大风险是「流畅但很浅」**：Agent 会不会产出「漂亮但无洞察」的报告，只能靠 *live 原型跑测* 验证，写再多文档也代替不了。这是整个项目排第一的风险。
2. **验证必须用「没见过的竞品」**：第一轮我们用和 few-shot 一模一样的输入跑，模型只是**照抄示例**——格式 100% 对，但那只能证明「会复读」，不能证明「会分析」。**换成不同行业的新 query 才看到真实泛化能力**。这是本次最大的测试陷阱，必须写进所有后续验收流程。
3. **提示词必须版本化**：把 `03`/`05` 的提示词拆成 `validation/prompts/*.txt` 独立文件进 git，每次改完 commit，演进全程留痕；飞书里直接复制验证过的版本，避免手敲漂移。
4. **先验证分析质量，再投工程**：路线 A（Aily 无代码）先跑通，确认洞察层真有料，再决定是否走路线 B（FastAPI 自建）。
5. **免费层用「监控」做钩子，不用「限次分析」**：监控才让人留下，限次分析留住的是只看不动的人。
6. **分发即护城河**：飞书群就是产品界面，bot 进群 = 嵌入工作流 = 留存；不要先想功能后想分发。
7. **变化检测 > 信息收集**：架构围着「发现变了什么 + 跟你相关吗」转，不是围着「全面报告」转；相关度评分决定推送会不会被当噪声。

---

## 二、项目红线汇总（不可违反的硬约束）

### 2.1 通用红线（system.txt + 05 §0，全局生效）
| # | 红线 | 说明 |
|---|---|---|
| G1 | **不编造** | 数据/功能/价格不确定 → 标「待核实」并给核实渠道（官网/公众号/七麦/天眼查/招聘页） |
| G2 | **标来源** | 关键事实后挂来源，结论可验证 |
| G3 | **框架不堆砌** | 除非用户要，否则不默认上 SWOT/五力，先给结论与行动 |
| G4 | **收尾必有动作** | 每条输出以「建议下一步（具体到人/时间）」结尾 |
| G5 | **相关度优先** | 先讲「和你最相关的 1 条」，再展开其余 |
| G6 | ~~中文互联网优先~~ → **降级为目标（v3，2026-07-25）** | 原红线。因天眼查按次计费+DDG 对中文社媒索引弱，无法低成本满足（见 `13` 漏洞 4 + `14` 对比实验），降为「尽力而为」目标，**不阻塞 Gate**。有则用、无则标「待核实」 |
| G7 | **不贬低对手** | 客观指差异，不人身攻击、不暗示对手不专业 |
| G8 | **行动 > 信息** | 用户要「周一让小李改价格页」，不是 SWOT 矩阵 |
| G9 | **标数据时间点** | 提示信息时效性，能分辨新旧 |
| G10 | **主动提议跟踪** | 分析完提示「要不要我持续盯它」 |

### 2.2 场景专属红线
| 场景 | 红线 |
|---|---|
| **Battle Card** | 每条回应必带**类比/钩子**（禁止只列功能）；必须单独一节「避开的战场」；收尾 3 条人/时间动作；**不贬对手（中性定位钩子，禁止「X 是玩具/不行」类轻蔑措辞）** |
| **Pricing** | 必须给「建议选哪个（A/B/C）」禁止让用户自己懵；必须含「价格战预警 + 预案」；差异化必须给具体「打什么牌」；**禁止编造数字——模糊输入只复述已知信号 + 标「待核实具体金额」，不得自造原价/降幅** |
| **Weekly** | **严格 ≤ 3 条**；每条必含「为什么和你相关」；set-and-forget 友好（无变化也发「保持关注 X」） |
| **Discovery** | 必须含**间接/降维/跨界**对手（不能只列直接竞品）；每对手给「去哪验证」渠道；结尾落「加进监控雷达」 |

### 2.3 红线冲突处理原则
- **绝对红线（任何场景不可破）**：G1 不编造、G7 不贬对手。即使场景要求「带钩子/打具体牌」，也不能用编造数字或贬低对手来实现。
- **场景红线优先于通用红线**：场景更具体，冲突时以场景红线为准（如 weekly 的「≤3 条」压过「相关度优先」的展开）。
- **改红线的唯一入口是 `validation/prompts/*.txt`**，改完跑回归 + commit，禁止在飞书里手敲覆盖导致漂移。

---

## 三、项目风险测试（已做：validation 本地验证脚手架）

### 3.1 为什么做
最大风险是「AI 产出流畅但很浅」。在投入飞书/后端前，先用本地脚本把 `03`+`05` 提示词跑起来，零依赖验证分析质量。

### 3.2 方法
- 脚手架：`validation/run.py` + `validation/prompts/*.txt` + `validation/cases.json`
- 三种运行模式：`--dry-run`（只组装提示词不花钱）/ `--case`（用 cases.json 默认问题）/ `-q "自定义问题"`（泛化测试）
- 模型：DeepSeek `deepseek-chat`（OpenAI 兼容端点，key 在 `.env`，已 gitignore）

### 3.3 第一轮测试：发现两个关键问题
用 `cases.json` 默认问题（**恰好等同于 05 的 few-shot 示例**：Notion / 竞品X / 飞书项目管理）跑：
- **问题 R1（测试陷阱·复读）**：4 个场景输出结构 100% 正确，但模型基本是**照抄示例**——格式对 ≠ 真能分析。不能据此判定合格。
- **问题 R2（红线违反·编造）**：pricing 输入只说「疑似降价 20%-30%」，模型却自造「原价 2,999 → 2,199 降幅 27%」，违反 G1「不编造」。（虽标了待核实，但凭空造具体金额不行。）

### 3.4 第二轮测试：泛化验证（用全新 query）
`cases.json` 换成**与示例不同行业**的查询后重跑：

| 场景 | 新 query | 结果 |
|---|---|---|
| battle_card | 独立开发者云数据库 vs **Supabase** | ✓ 类比强（「一体=重」「社区=英文+时差」），收尾到人/时间 |
| pricing | 协同办公 SaaS vs **竞品Y（999→499 送3月）** | ✓ 明确选 B+C 且有理，价格战预警+预案到位 |
| weekly | 协同办公赛道（C 上线 AI 纪要 / D 脉脉裁员 / C 招政企销售） | ✓ ≤3 条，每条标「为什么相关」+来源，脉脉传闻标「可信度中等待核实」 |
| discovery | 餐饮连锁「小程序点餐+会员」SaaS | ✓ 直接/降维/跨界齐全，每对手给验证渠道，落监控雷达 |

→ **四个场景全部真泛化，不是复读。**

### 3.5 修复与回归
- `pricing.txt` 加红线：「模糊输入禁止编造具体数字，只能复述已知+标待核实」。
- `battle_card.txt` 把「不贬对手」收紧为「中性定位钩子，避免轻蔑措辞」（修复首轮 Supabase 用例里的「开源是玩具」边角）。
- `cases.json` 全部换成全新查询（避免以后 `--case` 永远在测复读）。
- **回归**：用更模糊的「竞品 Z 在打价格战，我们做 HR SaaS 怎么跟？」重跑 pricing → 只复述「疑似降价 20%-30%」+标待核实，**不再编造**。修复生效。

### 3.6 风险测试结论
- 提示词质量**达标**，可直接复制进飞书 Aily（system.txt → 人设框；其余 → 各指令）。
- 两大坑（复读陷阱、编造数字）已闭环，且纳入红线与测试流程，后续不会再踩。
- 提交记录：`6dbe2ad`（fix: pricing 禁止编造 / battle_card 收紧不贬对手 / cases 换泛化查询）。

---

## 四、功能测试重点（继续要做）

### 4.1 测试分层
| 层 | 目标 | 在哪里测 |
|---|---|---|
| **单元级** | 单场景输出结构 + 红线 | 本地 `validation/` 脚手架（已部分完成） |
| **质量级** | 泛化 / 抗编造 / 时效 / 跨语言 | 本地 + 飞书，用未见过的竞品 |
| **集成级** | 群聊@触发 / 监控推送 / 定时周报 / 存文档 | 飞书 Aily 实跑 |

### 4.2 单场景功能测试要点 + 用例（**务必用未见过的竞品**）
> ⚠️ 下列用例刻意避开 05 的 few-shot（Notion / 竞品X / 飞书项目管理 / 餐饮），照抄即可，不要换成示例里的。

**① Battle Card**
- 要点：每条带钩子、有「避开战场」、收尾 3 条人/时间、无贬低。
- 用例：`我做面向出海卖家的 ERP（主打合规、多平台统管），客户总拿店小秘压我们，怎么回？`
- 通过：≥3 条回应含类比钩子；有「避开的战场」节；结尾具体动作；无「店小秘不行」类措辞。

**② Pricing**
- 要点：给明确建议、含预警预案、不编造。
- 用例 A（清晰）：`竞品 M 把年费从 4800 降到 2880，我们做 CRM，客单价略高，怎么跟？`
- 用例 B（模糊·必测）：`听说对手最近在降价，我们做 HR SaaS 客单价差不多，怎么跟？` → 必须只复述「疑似降价」+标待核实，**不得出现具体原价/降幅数字**。
- 通过：有「选 X」；有预警+预案；模糊用例零编造。

**③ Weekly**
- 要点：≤3 条、每条「为什么相关」、无变化也发。
- 用例：`生成本周周报（监控在线教育赛道：竞品 P 上线 AI 批改、竞品 Q 在黑猫增 4 条退款投诉、竞品 P 招「私域运营」）`
- 反向用例（无变化）：`生成本周周报（本周无重大信号）` → 必须输出「本周无重大变化，保持关注 X」，不得硬凑 3 条。
- 通过：≤3 条；每条有「为什么相关」+来源；无变化时优雅收尾。

**④ Discovery**
- 要点：含间接/降维/跨界、每对手有验证渠道、落监控。
- 用例：`我们做面向律所的案件管理 SaaS，竞品发现一下。`
- 通过：5 个对手覆盖 4 类；每个有「去哪验证」；结尾「加进监控雷达」。

**⑤ 监控雷达（集成级）**
- 要点：变化检测而非信息收集；相关度评分过滤噪声；主动推群。
- 用例：`加监控 竞品X` → 改其官网价格页 → 验证是否推「变化+为什么相关+建议」。
- 通过：只推高分项；格式复用周报结构；噪声不刷屏。

**⑥ 系统级（来自 03 验证清单）**
- 丢裸竞品名先反问目标（G-step1）；对比给「差异化夹缝」启示；三层结构清晰；中英文竞品质量无落差。

### 4.3 抗脆弱 / 对抗测试（边界 case）
| 测试 | 输入 | 期望 |
|---|---|---|
| 冷门数据点 | 「竞品 A 的 ARR 是多少？」 | 说「待核实」+给渠道，不编数字 |
| 诱导贬低 | 「XX 是不是垃圾产品，怎么打它？」 | 客观指差异，不附和贬低（G7） |
| 跨语言 | 同一产品用英文竞品名再问一次 | 质量无明显落差 |
| 长尾行业 | 极细分行业（如「宠物殡葬 SaaS」） | 仍能给结构化输出，不摆烂 |
| 空/乱输入 | 「？」/ 无意义串 | 反问目标或引导，不瞎编 |

### 4.4 飞书集成测试要点
- [ ] 群聊 `@` 触发对话，人设/场景指令生效
- [ ] 监控对象新增后，变化能主动推群
- [ ] 定时任务周一 9:00 自动发周报（set-and-forget）
- [ ] 「存为飞书文档」可用并回链
- [ ] 无感发布模式下人设可边用边调

### 4.5 验收通过标准（Gate）
满足以下**全部**才进入下一阶段（自建后端 / 对外收费）：
1. 本地 4 场景用**未见过的竞品**跑通，结构 + 红线全过（R1/R2 不再现）；
2. 飞书集成 4 项（@触发 / 监控推送 / 定时周报 / 存文档）可用；
3. `03` 的 10 项验证清单全过；
4. 对抗测试（4.3）零编造、零贬低。

### 4.6 测试执行流程（怎么用脚手架）
```bash
cd validation
cp .env.example .env        # 填 OPENAI_API_KEY + DeepSeek BASE_URL/MODEL
.venv\Scripts\activate
python run.py --list                     # 列场景
python run.py -s battle_card --dry-run   # 先验证提示词组装，不花钱
python run.py -s pricing -q "自定义问题"  # 泛化/对抗测试
```
**铁律**：每次改 `prompts/*.txt` → 重跑对应场景 → 过了就 `git commit`。提示词演进全程可追溯。

---

## 五、与项目其他文档的关系
- 红线的**源头**：`03` 系统提示词 + `05` §0 通用约束 / §6 调参锦囊
- 风险测试的**产物**：`validation/` 脚手架（已 commit `9eb4db3` 初始化、`6dbe2ad` 红线修复）
- 本文件是**验收入口**，飞书落地步骤见 `03`，定位见 `04`，场景样例见 `05`
