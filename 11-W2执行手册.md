# W2 执行手册：监控雷达 + 结构化分析引擎（v3 版）

> 日期：2026-07-25 | 阶段：**W2 — 监控优先 + 引擎轻改造**
> 总蓝图：`09-W2W4正式开发实施计划.md` 附录C（v3 任务清单，唯一权威）
> 前置：W1 MVP Gate 已过（2026-07-24，4 个 P0 全 PASS）
> 模式：滚动计划 —— 本文档是 W2 的执行级细节，W3/W4 保持骨架不动
> 代码事实来源：`backend/app/*.py`（已提交 `af8a4a4` 核心 + `017b164` 收尾）

> ⚠️ **v3 执行标准（2026-07-25 定稿，替代全部 v2.1 描述）**
> 本手册与 `09` 附录C v3 完全一致。与 v2.1 的关键差异（也是本手册唯一正确口径）：
> ① **监控推送（Task 2.4 / 2.5）提前为 W2 核心差异化** —— 裸大模型无持久化 / 定时 / diff，这是付费理由；
> ② **中文数据源降为 Task 2.8 占位**（默认关闭，W4 实接天眼查 / site:）；
> ③ **site: 中文社媒搜索、定价页提取均在 W2 砍掉**（移 W4，避免 over-build）；
> ④ **G6 红线降级为目标**（不阻塞 Gate），覆盖率门槛 50% → 30%（v3 不强求中文源）；
> ⑤ 模型统一 `deepseek-v4-pro`（`deepseek-chat` 已失效）。
> ❗ 本文件旧版（v2.1 全文 + 完整 TianYancha / site: / SceneSearchEngine / RecruitmentSource 代码）已作废，请勿照抄。

---

## 1. W2 目标与验收 Gate（v3）

### 目标（一句话）
从「纯 LLM 编报告」升级为「**真实搜索接地 + 结构化结果**」，并落地「**监控雷达**」这一裸大模型做不到的硬差异化（持久化 + 定时 + diff + 推群）。

### 最终验收 Gate（v3，全部通过才算 W2 完成）

| # | 验收项 | 对应 | 验证方法 |
|---|---|---|---|
| M1 | `/monitor add/list/remove` 在飞书群可用；后台定时轮询随服务启动 | 监控核心 | 飞书实测 + 日志见「监控雷达后台轮询已启动」 |
| M2 | `analyze(scene, query)` 返回 `AnalysisResult`，真实搜索时 `sources` ≥ 1 | 数据接地 | 端到端冒烟（已验证：DDG 3 条 + 1778 字带引用分析） |
| M3 | 搜索 / 抓取失败 → 不 crash，回退纯 LLM + `note` 标「待核实」 | 生产可靠 | 断网 / 限流触发测试 |
| M4 | pricing / battle_card 等场景红线完好（模型主动标「待核实」，不编造数字） | G1/G6 目标 | 红线抽测（已验证：pricing 主动标注「价格待核实」） |
| M5 | 中文源占位关闭时 `general_web` 覆盖即可；含中文源后总覆盖 ≥ 30%（目标非硬线） | 量化目标 | 日志 / `coverage_summary` |

> **G6 已降级为目标**（v3 共识，见 `09` 附录C + `06` G6 标注）：没有中文源时不阻塞 W2，但 `analyze()` 仍通过通用搜索接地并要求模型标注「待核实」。

---

## 2. Task 拆解与执行顺序（v3：2.1–2.9）

### 依赖关系（执行顺序）

```
第 1 步：Task 2.1（基础设施）──→ 无依赖，最先做 ↓
                                    │
         ┌──────────────────────────┼──────────────────────────┐
         ↓                          ↓                          ↓
    Task 2.2                   Task 2.3                    Task 2.4
    (通用搜索 ddgs)            (抓取 trafilatura)          (监控 monitor)
         │                          │                          │
         ↓                          ↓                          ↓
    Task 2.5（变化检测 detector）────────────┤                  │
         │                          │                          │
         ↓                          ↓                          ↓
    Task 2.6（数据模型 models）─────────────┴──────────────────┘
         │
         ↓
    Task 2.7（引擎轻改造 engine）────── 核心路径，依赖 2.2+2.3+2.6
         │
         ↓
    Task 2.8（中文源占位 chinese_sources）── 接口预留，默认关闭
         │
         ↓
    Task 2.9（Bot 监控命令 + 清理废弃文件）── 最后做
```

### 并行机会
- **2.2 / 2.3 / 2.4 / 2.5** 互相不依赖，可并行；2.6（数据模型）先定接口。
- 实际提交：核心 `af8a4a4`（2.1–2.5 + 2.9 命令部分）→ 收尾 `017b164`（2.6–2.9）。

---

## 3. 各 Task 详细执行指南（v3，对照真实代码）

### Task 2.1 — 基础设施加固（0.5d）✅ 已完成

**改动清单**（真实落地，非 v2.1 设计）：

| 文件 | 操作 | 关键内容 |
|---|---|---|
| `config.py` | 改写 | pydantic_settings；LLM 用 `api_key/base_url/model` 别名 `OPENAI_*`（兼容 W1 engine）；新增 `search_*` / `fetch_*` / `monitor_enabled` / `monitor_interval_minutes` |
| `logger.py` | 新建 | 幂等日志处理器（重复调用不产生重复 handler） |
| `requirements.txt` | 追加 | `ddgs>=6.0` / `trafilatura>=1.8` / `httpx>=0.27` / `pydantic-settings>=2.0` |
| `.env.example` | 追加 | `OPENAI_MODEL=deepseek-v4-pro` / `SEARCH_MAX_RESULTS` / `FETCH_*` / `MONITOR_*` |

> ⚠️ `config.py` 中 `search_engine: str = "duckduckgo"` 字段**保留但不被消费**（searcher 直接调 `ddgs`，见 2.2）。不要据此写 `if settings.search_engine == ...` 分支。

**验证**（已通过）：`config` 读取 / `logger` 幂等 / `engine` 兼容 / `webhook_server` 启动路径未破坏。

---

### Task 2.2 — 通用搜索层 `searcher.py`（v3 简化）✅ 已完成

**v3 调整**：砍掉 site: 中文社媒搜索与来源分类（中文源降占位）。仅保留通用 DuckDuckGo 搜索。

```python
# app/searcher.py（v3 简化版核心结构）
from ddgs import DDGS                      # 原 duckduckgo-search 已废弃，8.x 默认 backend 被限流
from app.config import settings
from app.logger import log

class SearchSource(Enum):
    GENERAL = "general_web"                # v3：仅通用网页

@dataclass
class SearchResult:
    url: str
    title: str
    snippet: str
    domain: str
    source: SearchSource = SearchSource.GENERAL
    relevance_score: float = 0.0

class Searcher:
    def __init__(self, timeout=10, region="wt-wt", backend="lite"):
        self._ddgs = DDGS(timeout=timeout)   # backend="lite" 实测可用；"auto"/"html" 被限流返回 0
    def search(self, query, max_results=None, region=None) -> list[SearchResult]:
        # 失败返回 [] 并记日志（不抛异常，避免拖垮 analyze）
        raw = self._ddgs.text(query=query, max_results=..., region=..., backend=self._backend)
        ...
```

**关键 API**：`Searcher().search(query, max_results=settings.search_max_results)` → `list[SearchResult]`。
**验证**（已通过）：`Searcher().search("AI 项目管理工具", 3)` 返回 3 条；失败场景返回 `[]` 不崩。

---

### Task 2.3 — 抓取层 `fetcher.py`（v3 简化）✅ 已完成

**v3 调整**：砍掉定价页定向提取（移 W4），仅保留通用正文抽取 + 并发抓取 + 失败降级。修 trafilatura 2.x API 漂移。

```python
# app/fetcher.py（v3 简化版核心结构）
import trafilatura
from app.config import settings

@dataclass
class FetchedPage:
    url: str; title: str; text: str
    publish_date: Optional[str] = None; word_count: int = 0

class Fetcher:
    async def fetch_one(self, url) -> Optional[FetchedPage]:
        # trafilatura 2.x 正确用法：
        extracted = trafilatura.extract(html, include_comments=False,
            include_tables=True, favor_precision=True, output_format="txt")  # ⚠️ "txt" 非 "text"
        # 标题/日期用 extract_metadata（2.x 已移除 extract_title/extract_date）
        meta = trafilatura.extract_metadata(html)
        title = meta.title or "" if meta else ""
        ...
    async def fetch(self, urls, max_pages=3) -> list[FetchedPage]:
        # asyncio.gather 并发，返回成功的页（顺序不限）
```

**关键 API**：`await Fetcher().fetch(urls)` → `list[FetchedPage]`（url/title/text/...）。
**验证**（已通过）：`fetch(["https://www.notion.so/product"])` 成功；trafilatura 2.x 漂移已修。

---

### Task 2.4 — 监控雷达 `monitor.py`（v3 核心）✅ 已完成

**v3 核心差异化**：加竞品 → 定时搜索 → 变化检测 → 有变化 `bot.push` 推群。LLM 分析不在本模块（属于按需 `/api/analyze`）。

```python
# app/monitor.py（v3 最小版核心结构）
from app.config import settings
from app.logger import log

@dataclass
class Monitor:
    id: int; competitor: str; scene: str; chat_id: str
    created_at: str; last_check_at: Optional[str] = None; last_signature: str = ""

class MonitorStore:                       # SQLite 持久化
    def __init__(self, db_path=settings.db_path): ...   # 建表 monitors(...)
    def add(self, competitor, chat_id, scene="discovery") -> int
    def list(self, chat_id) -> list[dict]
    def get_all(self) -> list[Monitor]
    def remove(self, mid, chat_id) -> bool
    def update_check(self, mid, signature) -> None

class MonitorService:
    def __init__(self, store, searcher, fetcher, bot): ...
    async def check_one(self, m) -> bool:  # 搜索→detect_changes→有变化 push+update
    async def run_all(self) -> int:        # 轮询全部，返回变化项数

# 单例（避免循环导入：monitor 不顶层 import bot）
def init_monitor_service(bot) -> MonitorService: ...
def get_monitor_service(bot=None) -> MonitorService: ...
```

**关键 API**：`svc.store.add(competitor, chat_id)` / `get_monitor_service(bot).run_all()`。
**注意**：`MonitorStore` 有 `list()` 方法，类内若写 `list[Monitor]` 注解会冲突 → 文件顶部加 `from __future__ import annotations`（已加）。
**验证**（已通过）：mock bot + 真实搜索 sanity；`webhook_server` 导入 + 真实 bot 初始化 + 路由齐全。

---

### Task 2.5 — 变化检测 `detector.py`（v3 核心）✅ 已完成

**v3 最小版**：集合 diff，不做类型识别（定价/产品/融资分类留 W4）。

```python
# app/detector.py
@dataclass
class ChangeSet:
    added: list[str] = field(default_factory=list)
    removed: list[str] = field(default_factory=list)
    has_change: bool = False

def detect_changes(old_items: list[str], new_items: list[str]) -> ChangeSet:
    old_set, new_set = set(old_items), set(new_items)
    added = [x for x in new_items if x not in old_set]
    removed = [x for x in old_items if x not in new_set]
    return ChangeSet(added=added, removed=removed, has_change=bool(added or removed))
```

**监控签名**：`monitor.py` 用 `"\n".join([f"{r.title} ({r.domain})" ...])` 作 `last_signature`，下次 `detect_changes` 比差异。

---

### Task 2.6 — 数据模型 `models.py` ✅ 已完成

**自包含 Pydantic**（不依赖 `chinese_sources`，避免循环导入）。

```python
# app/models.py
class Source(BaseModel):
    url: str = ""; title: str = ""; snippet: str = ""
    source_type: str = "general_web"      # general_web / chinese_social / tianyancha / third_party
    source_display: str = ""              # 「网页搜索」「天眼查」...
    relevance: str = "high"               # high / medium / low

class AnalysisResult(BaseModel):
    scene: str = ""; query: str = ""
    analysis: str                         # LLM 生成文本
    sources: list[Source] = Field(default_factory=list)
    confidence: str = "medium"
    ts: datetime = Field(default_factory=datetime.now)
    coverage_summary: dict = Field(default_factory=dict)   # {"general_web": n, "total": n}
    note: str = ""                        # 优雅降级备注

class CompareResult(BaseModel):          # 预留，v3 未启用
    targets: list[str] = ...; matrix: dict = ...; insights: list[str] = ...; sources: list[Source] = ...
```

> `scene` / `query` 给默认空串（修掉 09 验收要求 `AnalysisResult(analysis="test")` 必填报错）。

---

### Task 2.7 — 引擎轻改造 `engine.py`（W1 核心防护）✅ 已完成

**保留 W1 已验证部分不动**：`validation/prompts/*.txt` 加载方式、`model`、`temperature=0.4`、messages 结构、`analyze(scene, query)` 场景路由。**仅追加**：通用搜索 → 抓取 → 上下文组装 → 注入 → 封装 `AnalysisResult`。

```python
# app/engine.py（v3 轻改造核心结构）
def _gather_context(scene, query) -> tuple[list[Source], str]:
    from . import fetcher
    from .searcher import Searcher        # search 是实例方法，必须 Searcher().search(...)
    try:
        results = Searcher().search(query, max_results=settings.search_max_results)
    except Exception:
        return [], "⚠️ 未能获取实时数据，以下基于模型知识（待核实）"
    if not results:
        return [], "⚠️ 实时搜索无结果，以下基于模型知识（待核实）"
    urls = [r.url for r in results][:settings.fetch_max_urls]
    pages = asyncio.run(fetcher.fetch(urls)) if urls else []
    # 组装 "## 参考资料来源（N 条）" 块
    ...

def analyze(scene, query, target=None) -> AnalysisResult:
    if scene not in SCENES: raise ValueError(...)
    system_prompt = _read("system.txt")
    scene_prompt = _read(SCENES[scene])
    sources, context = _gather_context(scene, query)
    note = context if context.startswith("⚠️") else ""
    user_content = query if note else f"{query}\n\n{context}\n\n（请基于参考资料分析，标来源；无法验证标「待核实」）"
    client = OpenAI(api_key=settings.api_key, base_url=settings.base_url)
    analysis = ""
    for _ in range(2):                    # 偶发空 completion 重试一次
        resp = client.chat.completions.create(model=settings.model, temperature=0.4, messages=[...])
        analysis = resp.choices[0].message.content or ""
        if analysis.strip(): break
    return AnalysisResult(scene=scene, query=query, analysis=analysis,
                          sources=sources, confidence="medium",
                          coverage_summary={"general_web": len(sources), "total": len(sources)},
                          note=note)
```

**设计约束（保护 W1 红线立场）**：
- 不注入 v2.1 的 G6/G1 硬性指令（「优先中文源」「天眼查必引」）—— v3 已降级为目标，避免改变红线立场。
- 搜索 / 抓取失败 → 优雅降级为纯 LLM + `note` 标「待核实」，不整体失败。
- 🐛 **已修关键 bug**：原误写 `searcher.search(...)`（当模块函数调）→ `AttributeError` 被 try/except 吞掉、搜索永远失败。应为 `Searcher().search(...)`（`search` 是实例方法）。

**同步调用点**（否则 `push` 收 model 会崩）：
- `bot.py`：`res = analyze(...)`；`text = res.analysis if hasattr(res,"analysis") else str(res)`
- `webhook_server.py` `/api/analyze`：`return {"scene": req.scene, **res.model_dump()}`

**验证**（已通过）：真实 DDG 搜索 3 条 + DeepSeek 1778 字带引用分析；pricing 红线完好（模型主动标「待核实」）。

---

### Task 2.8 — 中文数据源占位 `chinese_sources.py` ✅ 已完成

**v3 占位接口**：默认关闭，W4 实接。不做真实 API 调用。

```python
# app/chinese_sources.py（v3 占位）
class SourceType(str, Enum):
    GENERAL_WEB = "general_web"
    CHINESE_SOCIAL = "chinese_social"     # 小红书/知乎/微博等（占位）
    TIANYANCHA = "tianyancha"             # 工商/融资（占位）
    THIRD_PARTY = "third_party"           # 七麦等第三方（占位）

@dataclass
class DataPoint:
    title: str; content: str = ""; url: str = ""
    source_type: SourceType = SourceType.GENERAL_WEB
    source_name: str = ""

class ChineseSourceManager:
    def __init__(self, enable: bool = False): self._enable = enable
    def search(self, scene, query, target=None) -> list[DataPoint]:
        if not self._enable: return []     # 默认关闭
        raise NotImplementedError("中文数据源 v3 为占位，实接见 09 附录C.C.5（W4 Task 4.1）")
```

**关键 API**：`ChineseSourceManager().search(...)` 默认返回 `[]`；`enable=True` 抛 `NotImplementedError` 指回 W4。

---

### Task 2.9 — Bot 监控命令 + 清理废弃文件 ✅ 已完成

**bot.py 改动**：
- 新增 `push(self, chat_id, text) -> bool`：用 `receive_id_type="chat_id"` 主动向群推消息（监控雷达用）。
- 新增 `_handle_monitor(self, msg, text)`：处理 `/monitor add <竞品>` / `list` / `remove <ID>`。
- `handle_message`：`if clean.startswith("/monitor"): self._handle_monitor(msg, clean); return`（在场景路由前）。

**webhook_server.py 改动**：
- `lifespan(app)`：`if settings.monitor_enabled: svc = monitor_mod.init_monitor_service(bot); asyncio.create_task(_scheduler_loop(svc))`。
- `_scheduler_loop(svc)`：`while True: await svc.run_all(); await asyncio.sleep(settings.monitor_interval_minutes * 60)`。
- 修正顶部历史说明：`app/main.py` / `app/feishu.py` / `app/sessions.py` 已删除，功能迁至本文件 + `bot.py`。

**清理**：`git rm backend/app/main.py` / `app/feishu.py` / `app/sessions.py`（无活跃 import）。

**监控命令用法（飞书群）**：
```
/monitor add 飞书        → ✅ 已加入监控 #1：飞书（定时搜索，有变化推群）
/monitor list            → 📡 本群监控列表：#1 飞书（场景：discovery）
/monitor remove 1        → ✅ 已删除
```

---

## 4. 排错（v3 已知坑）

| 现象 | 根因 | 解决 |
|---|---|---|
| `searcher.search` 返回 0 条 | 用了 `ddgs` 的 `auto`/`html` backend（被限流） | 用 `backend="lite"`（已在 `Searcher.__init__` 默认） |
| `ModuleNotFoundError: duckduckgo_search` | 包已废弃 | 换 `ddgs`（`from ddgs import DDGS`） |
| `trafilatura` 报 `output_format="text"` 非法 | 2.x 改为 `txt` | `output_format="txt"` |
| `module 'trafilatura' has no attribute 'extract_title'` | 2.x 移除 | 用 `trafilatura.extract_metadata(html)` 取 `.title`/`.date` |
| `analyze()` 返回空 `analysis` 且 `sources=0` | 误写 `searcher.search(...)` 当模块函数 → AttributeError 被吞 | 改 `Searcher().search(...)`（实例方法） |
| LLM 偶发返回空 | 模型冷启动 / 空 completion | `engine.py` 已加重试一次（`range(2)`） |
| `TypeError: 'function' object is not subscriptable` | `MonitorStore.list` 方法与 `list[Monitor]` 注解冲突 | 文件顶部 `from __future__ import annotations` |
| `deepseek-chat` 调不通 | 模型名失效 | 统一 `deepseek-v4-pro`（`config.py` 默认值 + `.env.example` 已改） |

---

## 5. W2 → W3 交接

**W2 交付物**（已提交 `af8a4a4` + `017b164`）：监控雷达（唯一硬差异化）+ 结构化分析引擎（真实搜索接地）+ 优雅降级 + 中文源占位接口。

**W3 候选方向**（滚动计划，待细化）：
- 监控「变化类型识别」（定价/产品/融资/招聘分类，目前 detect_changes 只做集合 diff）。
- 周报自动进群（P0 #4，set-and-forget，复用 monitor 调度）。
- 中文数据源实接（W4 Task 4.1，依 W5 用户验证是否在乎来源引用决定投入）。
- 飞书 Interactive Card 渲染（替代纯文本回复）。

**下次验证建议**：本地飞书群 `/monitor add <竞品>` 实测推群链路，确认留存假设；重跑 `06` 4 场景红线用例（P0 不回退）。
