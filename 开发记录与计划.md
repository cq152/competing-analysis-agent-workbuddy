# 开发记录与计划

> **本文档定位**：项目全程「滚动开发记录」，按阶段（W2 / W3 / W4 …）分章。
> - **详细层**：每个阶段的具体计划、执行手册、审计、验证与遗留，都在对应章节展开（取代原来散落的 `09`/`11`/`15`/`16`/`17` 平级文档，防「每问一文档」反模式）。
> - **摘要层**：`README.md` 是单一事实来源与总验收报告，只列「做了什么 + 验证结果 + commit + 遗留结论」，不重复细节。
> - **维护纪律**：阶段有进展/决策时，先在 `README` 同步结论，再回本文补详节；W3/W4 推进只新增顶层 `## Wx ·` 章节，不再改名、不再新建平级 NN 文档。
> - **内容结构化约定**：每个阶段统一 4 节（§1 计划 / §2 执行手册 / §3 审计 / §4 验证与遗留），模板见文末「阶段内容结构化模板」。

---

## W2 · 监控优先核心（v3，2026-07-25 定稿）

### 1. 计划（Plan）

#### 1.1 v3 核心决策（2026-07-25 定稿，替代 v2.1）
源自 `13` 批判性审视 + `14` 竞品对比实验，与 `06` G6 降级 / `04` 定价 v2 / `README` 自洽：

| # | 决策 | 说明 |
|---|---|---|
| 1 | **监控推送提前为 W2 核心** | `14` 实测：裸 DeepSeek 竞品分析质量已够 SMB 用，**唯一硬差异化 = 监控推送**（裸模型无持久化/定时/diff） |
| 2 | **G6 红线降级为目标** | 天眼查按次计费 + DDG 中文社媒索引弱，无法低成本满足；有中文源则用、无则标「待核实」，不阻塞 Gate |
| 3 | **中文数据源降占位** | `chinese_sources.py` 默认关闭，W4 实接；W2 不 over-build |
| 4 | **定价重做** | 免费层不送监控、送 3 次分析；毛利 ≥60%（见 `04` §五 v2） |
| 5 | **不做 concierge 预验证** | 用户决定先开发完再找 SMB 验证（无渠道） |
| 6 | **模型统一 `deepseek-v4-pro`** | `deepseek-chat` 已失效 |

#### 1.2 任务清单（v3：2.1–2.9，全部完成）
| Task | 内容 | commit | 状态 |
|---|---|---|---|
| 2.1 | 基础设施（config/logger/requirements/.env.example） | `af8a4a4` | ✅ |
| 2.2 | 通用搜索 `searcher.py`（砍 site: 中文源，ddgs lite） | `af8a4a4` | ✅ |
| 2.3 | 抓取 `fetcher.py`（修 trafilatura 2.x + 砍定价提取） | `af8a4a4` | ✅ |
| 2.4 | 监控雷达 `monitor.py`（SQLite + 后台调度推群） | `af8a4a4` | ✅ |
| 2.5 | 变化检测 `detector.py`（集合 diff） | `af8a4a4` | ✅ |
| 2.6 | 数据模型 `models.py`（Source/AnalysisResult/CompareResult） | `017b164` | ✅ |
| 2.7 | 引擎轻改造 `engine.py`（搜索→抓取→注入→结构化 + 优雅降级） | `017b164` | ✅ |
| 2.8 | 中文源占位 `chinese_sources.py` | `017b164` | ✅ |
| 2.9 | Bot 监控命令 + 清理废弃文件 | `017b164` | ✅ |

#### 1.3 W2 验收 Gate（v3）
| # | 验收项 | 验证方法 | 状态 |
|---|---|---|---|
| M1 | `/monitor add/list/remove` 在飞书群可用；后台定时随服务启动 | 飞书实测 + 日志 | ⏳ 待用户本地实测 |
| M2 | `analyze()` 返回 `AnalysisResult`，真实搜索 `sources`≥1 | 端到端冒烟（DDG 3 条 + 1778 字） | ✅ 代码层 PASS |
| M3 | 搜索/抓取失败 → 不 crash，回退纯 LLM + 标「待核实」 | 断网/限流测试 | ✅ 代码层 PASS |
| M4 | pricing/battle_card 红线完好（主动标「待核实」，不编造） | 红线抽测 | ✅ 代码层 PASS |
| M5 | 中文源关闭时 general_web 覆盖即可；含中文源后总覆盖≥30%（目标） | coverage_summary | ✅ 目标非硬线 |

### 2. 执行手册（Execution）

> ⚠️ **v3 执行标准（替代全部 v2.1 描述）**：监控提前为 W2 核心；中文源降占位；site: 中文搜索/定价提取砍掉；G6 降级为目标；模型 `deepseek-v4-pro`。旧 v2.1 全文（TianYancha/site:/SceneSearchEngine）已作废。

#### 2.1 各 Task 执行指南（对照真实代码）

**Task 2.1 — 基础设施** ✅
| 文件 | 操作 | 关键内容 |
|---|---|---|
| `config.py` | 改写 | pydantic_settings；`api_key/base_url/model` 别名 `OPENAI_*`；新增 `search_*`/`fetch_*`/`monitor_enabled`/`monitor_interval_minutes` |
| `logger.py` | 新建 | 幂等日志处理器 |
| `requirements.txt` | 追加 | `ddgs>=6.0` / `trafilatura>=1.8` / `httpx>=0.27` / `pydantic-settings>=2.0` |
| `.env.example` | 追加 | `OPENAI_MODEL=deepseek-v4-pro` / `MONITOR_*` |

> ⚠️ `config.py` 中 `search_engine: str = "duckduckgo"` 字段**保留但不被消费**（searcher 直接调 `ddgs`）。

**Task 2.2 — `searcher.py`（v3 简化）** ✅
```python
from ddgs import DDGS                      # 原 duckduckgo-search 已废弃，8.x 默认 backend 被限流
class SearchSource(Enum):
    GENERAL = "general_web"                # v3：仅通用网页
@dataclass
class SearchResult:
    url: str; title: str; snippet: str; domain: str
    source: SearchSource = SearchSource.GENERAL; relevance_score: float = 0.0
class Searcher:
    def __init__(self, timeout=10, region="wt-wt", backend="lite"):
        self._ddgs = DDGS(timeout=timeout)   # backend="lite" 实测可用；"auto"/"html" 被限流返回 0
    def search(self, query, max_results=None, region=None) -> list[SearchResult]:
        # 失败返回 [] 并记日志（不抛异常）
        ...
```
API：`Searcher().search(query, max_results=settings.search_max_results)` → `list[SearchResult]`。

**Task 2.3 — `fetcher.py`（v3 简化）** ✅
```python
import trafilatura
@dataclass
class FetchedPage:
    url: str; title: str; text: str
    publish_date: Optional[str] = None; word_count: int = 0
class Fetcher:
    async def fetch_one(self, url) -> Optional[FetchedPage]:
        extracted = trafilatura.extract(html, include_comments=False,
            include_tables=True, favor_precision=True, output_format="txt")  # ⚠️ "txt" 非 "text"
        meta = trafilatura.extract_metadata(html)   # 2.x 已移除 extract_title/extract_date
        title = meta.title or "" if meta else ""
    async def fetch(self, urls, max_pages=3) -> list[FetchedPage]:
        # asyncio.gather 并发
```
API：`await Fetcher().fetch(urls)` → `list[FetchedPage]`。

**Task 2.4 — `monitor.py`（v3 核心）** ✅
```python
@dataclass
class Monitor:
    id: int; competitor: str; scene: str; chat_id: str
    created_at: str; last_check_at: Optional[str] = None; last_signature: str = ""
class MonitorStore:                       # SQLite 持久化
    def add(self, competitor, chat_id, scene="discovery") -> int
    def list(self, chat_id) -> list[dict]
    def get_all(self) -> list[Monitor]
    def remove(self, mid, chat_id) -> bool
    def update_check(self, mid, signature) -> None
class MonitorService:
    async def check_one(self, m) -> bool      # 搜索→detect_changes→有变化 push+update
    async def run_all(self) -> int            # 轮询全部，返回变化项数
def init_monitor_service(bot) -> MonitorService: ...
def get_monitor_service(bot=None) -> MonitorService: ...
```
> ⚠️ `MonitorStore` 有 `list()` 方法，类内写 `list[Monitor]` 注解冲突 → 顶部加 `from __future__ import annotations`（已加）。

**Task 2.5 — `detector.py`** ✅ 集合 diff，不做类型识别（留 W4）：
```python
@dataclass
class ChangeSet:
    added: list[str] = field(default_factory=list)
    removed: list[str] = field(default_factory=list)
    has_change: bool = False
def detect_changes(old_items, new_items) -> ChangeSet: ...
```

**Task 2.6 — `models.py`** ✅ 自包含 Pydantic（不依赖 chinese_sources）：
```python
class Source(BaseModel):
    url: str=""; title: str=""; snippet: str=""
    source_type: str="general_web"; source_display: str=""; relevance: str="high"
class AnalysisResult(BaseModel):
    scene: str=""; query: str=""; analysis: str
    sources: list[Source] = Field(default_factory=list)
    confidence: str="medium"; ts: datetime = Field(default_factory=datetime.now)
    coverage_summary: dict = Field(default_factory=dict); note: str=""
class CompareResult(BaseModel):    # 预留，v3 未启用
    targets: list[str]; matrix: dict; insights: list[str]; sources: list[Source]
```
> `scene`/`query` 给默认空串（修掉 `AnalysisResult(analysis="test")` 必填报错）。

**Task 2.7 — `engine.py`（W1 核心防护）** ✅
保留 W1 已验证部分（`validation/prompts/*.txt` 加载/`model`/`temperature=0.4`/messages 结构/`analyze(scene,query)` 路由）不动；**仅追加**搜索→抓取→上下文组装→注入→封装 `AnalysisResult`。
```python
def _gather_context(scene, query) -> tuple[list[Source], str]:
    from . import fetcher
    from .searcher import Searcher        # search 是实例方法，必须 Searcher().search(...)
    try:
        results = Searcher().search(query, max_results=settings.search_max_results)
    except Exception:
        return [], "⚠️ 未能获取实时数据，以下基于模型知识（待核实）"
    if not results:
        return [], "⚠️ 实时搜索无结果，以下基于模型知识（待核实）"
    urls = [r.url for r in results][:settings.fetch_max_urls]
    pages = asyncio.run(fetcher.fetch(urls)) if urls else []
    # 组装 "## 参考资料来源（N 条）" 块
def analyze(scene, query, target=None) -> AnalysisResult:
    if scene not in SCENES: raise ValueError(...)
    ...
    for _ in range(2):                    # 偶发空 completion 重试一次
        resp = client.chat.completions.create(model=settings.model, temperature=0.4, messages=[...])
        analysis = resp.choices[0].message.content or ""
        if analysis.strip(): break
    return AnalysisResult(scene=scene, query=query, analysis=analysis, sources=sources, ...)
```
设计约束：不注入 v2.1 的 G6/G1 硬性指令（避免改变红线立场）；失败优雅降级。
🐛 已修关键 bug：误写 `searcher.search(...)`（当模块函数）→ `AttributeError` 被吞、搜索永远失败。应为 `Searcher().search(...)`。
同步调用点：`bot.py` 取 `res.analysis`；`webhook_server.py` `/api/analyze` 返回 `res.model_dump()`。

**Task 2.8 — `chinese_sources.py`** ✅ 占位接口：
```python
class SourceType(str, Enum):
    GENERAL_WEB="general_web"; CHINESE_SOCIAL="chinese_social"
    TIANYANCHA="tianyancha"; THIRD_PARTY="third_party"
@dataclass
class DataPoint:
    title: str; content: str=""; url: str=""
    source_type: SourceType=SourceType.GENERAL_WEB; source_name: str=""
class ChineseSourceManager:
    def __init__(self, enable: bool=False): self._enable=enable
    def search(self, scene, query, target=None) -> list[DataPoint]:
        if not self._enable: return []     # 默认关闭
        raise NotImplementedError("中文数据源 v3 为占位，实接见 W4")
```

**Task 2.9 — Bot 监控命令 + 清理** ✅
- `bot.py`：新增 `push(chat_id, text)`（主动推群）+ `_handle_monitor()` 处理 `/monitor add|list|remove`；`handle_message` 在场景路由前拦截 `/monitor` 开头。
- `webhook_server.py`：`lifespan` 中 `if settings.monitor_enabled: init_monitor_service(bot); asyncio.create_task(_scheduler_loop(svc))`；`_scheduler_loop` 每 `monitor_interval_minutes*60` 秒跑 `run_all()`。
- 清理：`git rm backend/app/main.py` / `app/feishu.py` / `app/sessions.py`（无活跃 import）。

监控命令用法：
```
/monitor add 飞书    → ✅ 已加入监控 #1：飞书（定时搜索，有变化推群）
/monitor list        → 📡 本群监控列表：#1 飞书（场景：discovery）
/monitor remove 1    → ✅ 已删除
```

#### 2.2 排错（v3 已知坑）
| 现象 | 根因 | 解决 |
|---|---|---|
| `searcher.search` 返回 0 条 | `ddgs` 的 `auto`/`html` backend 被限流 | 用 `backend="lite"` |
| `ModuleNotFoundError: duckduckgo_search` | 包已废弃 | 换 `ddgs` |
| `trafilatura` `output_format="text"` 非法 | 2.x 改为 `txt` | `output_format="txt"` |
| `module 'trafilatura' has no attribute 'extract_title'` | 2.x 移除 | 用 `extract_metadata(html)` |
| `analyze()` 返回空 `analysis` 且 `sources=0` | 误写 `searcher.search(...)` 当模块函数 → AttributeError 被吞 | 改 `Searcher().search(...)` |
| LLM 偶发返回空 | 冷启动空 completion | `engine.py` 重试一次 |
| `TypeError: 'function' object is not subscriptable` | `MonitorStore.list` 与 `list[Monitor]` 注解冲突 | 顶部 `from __future__ import annotations` |
| `deepseek-chat` 调不通 | 模型名失效 | 统一 `deepseek-v4-pro` |

#### 2.3 W2 → W3 交接
W2 交付物：监控雷达 + 结构化分析引擎 + 优雅降级 + 中文源占位。
W3 候选：变化类型识别（定价/产品/融资/招聘分类）/ 周报自动进群 / 中文源实接（依用户验证决定）/ 飞书 Interactive Card 渲染。
下次验证建议：本地飞书群 `/monitor add <竞品>` 实测推群；重跑 `06` 4 场景红线用例防回归。

### 3. 审计与决策闭环（Audit）

#### 3.1 审计发现（2026-07-25 扫描）
| 严重度 | 问题 | 结论 |
|---|---|---|
| 🔴 高 | **计划版本错位**：执行态（代码+记忆+11手册）停在 v2.1，权威文档（09/06/04/14/README）全为 v3 | 全盘切 v3，不再参考 11 旧 Task 编号 |
| 🟠 中 | **代码 over-build**：`searcher.py` 写了 site: 中文社媒分类；`fetcher.py` 写了定价提取且 BROKEN（trafilatura 2.x 漂移） | 按 v3 简化（砍 site:/砍定价/修 API） |
| 🟡 低 | **文档引用漂移**：09/11 写 `duckduckgo-search`（实际 `ddgs`）；`config.py` 有死字段 `search_engine`；README §7 旧"W1–W4"描述 | 已对齐（见 3.3） |

#### 3.2 待用户确认 4 项（已全部确认执行）
1. 是否以 v3 为执行标准 → **是**
2. searcher/fetcher 怎么处理 → **按 v3 简化**
3. v3 第一优先级是否 = 监控(2.4)+检测(2.5) → **是**
4. 记忆"方向决策"段是否按 v3 覆盖 → **是**（该段已加"已废弃，以 v3 为准"横幅）

#### 3.3 审计项执行结果（全部闭环）
**代码侧**（commit `af8a4a4` + `017b164`）：2.2 searcher 砍 site: / 2.3 fetcher 修 API+砍定价 / 2.4 monitor / 2.5 detector / 2.6 models / 2.7 engine / 2.8 chinese_sources / 2.9 清理 / `.env.example`+`config.py` 默认 model → `deepseek-v4-pro`。
**文档侧**（commit `ffca813` + `cc7d9d8`）：11 重写 v3 准确版 / 09 `duckduckgo-search`→`ddgs`+移除 tianyancha 依赖 / README §7 旧描述改 W2 完成 / 15 自身闭环 / 16 验证落库。
**残留（非阻塞，留 W4）**：`config.py` 死字段 `search_engine` 未清；tianyancha 仍作 W4 占位概念描述。

### 4. 验证与遗留（Verification & Gaps）

#### 4.1 验证范围与分层
| 层 | 本记录覆盖 | 不在本记录 |
|---|---|---|
| 代码层（模块加载/接口契约） | ✅ 全覆盖 | — |
| 引擎层（端到端 analyze） | ✅ discovery + pricing 冒烟 | 4 场景全量泛化（沿用 W1） |
| 红线回归 | ✅ pricing 编造数字回归 | 全部 4 场景（建议飞书实测复测） |
| 飞书集成层 | ❌ | MR-01 / FS-*（**待用户本地实测**） |

> ⚠️ W2 验证是**本地代码验证**，不等同于飞书群实测。监控推群（MR-01）必须用户本地起服务 + ngrok + 飞书群 `@` 才能验。

#### 4.2 复现命令
```bash
cd backend
.\.venv\Scripts\python.exe -c "import webhook_server; print('routes:', [r.path for r in webhook_server.app.routes if hasattr(r,'path')])"
.\.venv\Scripts\python.exe -c "from app import models, chinese_sources; print(models.AnalysisResult(analysis='test').model_dump()); print(chinese_sources.ChineseSourceManager().search('battle_card','q'))"
.\.venv\Scripts\python.exe -c "from app.engine import analyze; r=analyze('discovery','请帮我找几款面向小团队的 AI 项目管理新兴工具'); print('analysis len:', len(r.analysis), '| sources:', len(r.sources))"
.\.venv\Scripts\python.exe -c "from app.engine import analyze; r=analyze('pricing','对比一下 Linear 和 Jira 的定价方案，小团队怎么选'); print(r.analysis[:600])"
```
> 需 `backend/.env` 有 Key；网络不通时走优雅降级（sources=0 + 标「待核实」），仍验证结构正确。

#### 4.3 验证结果
**代码层（全绿）**：全模块 import OK；webhook 路由齐全（`/webhook/event` `/api/analyze` `/health`）；`AnalysisResult(analysis="test")` 构造成功；`ChineseSourceManager().search()` 默认 `[]`、`enable=True` 抛 `NotImplementedError`；`analyze("nope","q")` 抛 `ValueError`；删除废弃文件后 import 正常。

**引擎层（真实端到端）**：
| 场景 | 结果 | 状态 |
|---|---|---|
| discovery | 返回 AnalysisResult；**真实 DDG 3 条**；analysis 1778 字带真实引用；coverage 标「通用搜索 3」 | ✅ |
| pricing（红线回归） | 主动标「参考资料无定价信息、价格待核实」，零编造 | ✅ G1 合规 |

**验证中发现的 Bug（已修，commit `017b164`）**：
| Bug | 根因 | 修复 |
|---|---|---|
| 搜索静默降级 | `engine.py` 误写 `searcher.search(...)` 当模块函数 → AttributeError 被吞 | 改 `Searcher().search(...)` |
| 偶发空响应 | 模型冷启动空 completion | `analyze()` 重试一次 |

#### 4.4 结论与限制
- W2 九个 Task 代码层全 PASS；W1 红线未被 2.7 破坏（G1 合规）。
- **唯一硬差异化（监控推群 MR-01）仍待用户本地飞书实测（用户已确认后续手动测试闭环，2026-07-25）**。
- `06-测试记录表.csv` 已追加 W2 代码层 PASS 行（W2-IMP/E2E/RED/RBAC/CLN）；飞书集成项仍「待测」。

---

## W3 · 飞书卡片 + 对比 + 会话（详细滚动计划，2026-07-25 re-plan 展开）

> ⚠️ **W3 前置门禁（Re-plan Gate，用户 2026-07-25 提出）**：进入 W3 编码前须确认 W2 验收状态——
> - ✅ W2 **代码层已闭环**：`W2-IMP / E2E / RED / RBAC / CLN` 全 PASS（本地冒烟，commit `017b164`）。
> - ⚠️ W2 **飞书集成层仍待测**（MVP 核心差异化，AI 代跑不了）：`MR-01` 监控推群、`FS-01` 群聊@、`FS-02` 定时周报、`FS-03` 存文档、`FS-04` 无感调教 在 `06-测试记录表.csv` 均为「待测」——**不得虚报 PASS**。
> - 🚦 **W3.1 提交门禁（用户 2026-07-25 11:20 决策，已于当日满足）**：先等 W2 飞书集成**本地实测闭环**（MR-01 `/monitor` 推群成功）后，再提交 W3.1（`card_renderer.py` + `bot.py`）；W3 代码暂留本地，不提交、不推送。✅ **门禁已满足（2026-07-25 经 ngrok 公网实测：MR-01/FS-01/FS-02 PASS）**，W3.1 已随本次提交合入。W3 推进顺序仍为 3.1→3.2→3.3→3.4。

### 1. 计划（Plan）

**目标**：text 回复 → 富交互卡片；多竞品对比；会话上下文连续持久化。
**v3 修正**：① 监控已在 W2 提前，W3 不含监控；② 卡片来源分色降级为**仅 🔵通用**（中文源未实接前不写假数据）。

#### 1.1 任务清单（W3：3.1–3.4）

**3.1 `card_renderer.py` — 飞书卡片渲染** ✅ 已提交（2026-07-25，含 `bot.py` push_card + mention 剥离 bugfix）
- **步骤**：① 新建 `backend/app/card_renderer.py`，输入 `AnalysisResult`/`CompareResult`，输出飞书 Interactive Card JSON 体（不含外层 msg_type）；② 场景→配色映射（battle_card=orange / pricing=red / discovery=green / weekly=blue）；③ 来源分色 `_source_badge`（当前仅 🔵通用，W4 中文源实接后自动扩展）；④ 超长正文截断保护（≤18000 字）；⑤ 降级备注（note）橙字渲染；⑥ `bot.py` 加 `push_card()` + `_async_analyze` 改为卡片优先、失败回退纯文本（不破坏 W1 文本链路）。
- **验收**：`render_analysis_card(sample)` 输出合法 Card JSON（已本地单测：5 元素、来源块含 🔵通用+链接、降级块、pricing 模板=red、超长截断均 PASS）；`bot.py` 可 import、`push_card` 存在。
- **依赖**：`models.py`（AnalysisResult/Source）。无外部依赖。
- **状态**：**已提交**（2026-07-25，随 W3.1 提交；离线冒烟 5 项全绿 + mention 剥离 bugfix 一并合入）；门禁（W2 飞书实测闭环）已满足。

**3.2 Bot 卡片发送 + `/compare` 对比** ✅ 已实现（2026-07-25，待提交；经 Re-plan Gate 确认后写码）
- **步骤**：
  1. `validation/prompts/compare.txt`（**新增，提示词唯一入口**）：竞品对比专用系统/场景指令——要求输出「对比维度 + 各维度逐家评估 + 3-5 条关键洞察」，严守红线 G1（不编造、引用缺失标「待核实」）+ G3（不堆砌空框架）。
  2. `engine.compare(targets: list[str], query: str = "") -> CompareResult`：合并 targets 做通用搜索→抓取上下文；用 `compare.txt` + 上下文调 LLM；结构化填充 `CompareResult`（`targets` / `analysis` 叙事 / `matrix` / `insights` / `sources` / `note` 降级）。
  3. `models.CompareResult` 增加 `analysis: str = ""` 字段（叙事正文，与 `AnalysisResult` 对齐，便于卡片渲染）。
  4. `card_renderer.render_compare_card` 改造：顶部先渲染 `analysis` 叙事（同 `render_analysis_card` 正文+来源+降级+页脚），矩阵表与洞察保留（`matrix` 空则降级提示）。
  5. `bot.py` 命令路由：在 `parse_command` 前加 `/compare` 分支 → `_handle_compare(msg, text)`；解析 `text[len("/compare"):]` 空格分词得 targets；<2 个回用法提示；≥2 个则线程异步 `_async_compare` 调 `engine.compare` 后 `render_card` 卡片优先推送（复用 3.1 `push_card`）。
  6. 「和 XX 对比」自然语义 + 上次主竞品记忆（轻量 in-memory：`_last_compare: dict[chat_id, str]` 存主竞品；命中「对比 / 和…对比」且缺主竞品时自动补；**3.3 `sessions.py` 将其持久化到 SQLite**）。
- **验收**：
  - 群里 `/compare 飞书 钉钉` → 收到「⚖️ 对比：飞书 vs 钉钉」蓝色对比卡（叙事+矩阵/洞察+来源）。
  - `和 钉钉 对比`（该群已有主竞品）→ 自动补主竞品成双目标对比。
  - `/compare` 单目标或无目标 → 回用法提示、不崩。
  - **红线回归**：compare 场景 G1 不编造（引用缺失标待核实）、G3 不堆空框架（用**全新未见竞品**跑，非 `05` few-shot）。
  - **不回归**：battle_card / pricing / weekly / monitor 仍正常。
- **依赖**：3.1（`push_card` + `render_compare_card` 接口已就绪）；`engine.analyze` 上下文管线可复用。
- **风险**：中——对比引擎为新增能力，须先做 W2 红线回归（G1/G3）；`compare.txt` 提示词为新增唯一入口（Aily 已弃用，仅 `validation/prompts` 为准）。
- **状态**：✅ 已实现并验证（离线冒烟 6 项全绿 + 真实 E2E 推群 + 红线回归 PASS）；待提交。

**3.3 `sessions.py` 重建 — SQLite 会话持久化**
- **步骤**：① 重建 `backend/app/sessions.py`（W2 清理时删除，W3 重建）：`sessions` + `analysis_history` 表；② `get_history(chat_id)` / `save_analysis(...)`；③ bot 分析后自动存历史。
- **验收**：重启后 session 不丢；`get_history` 可查；不破坏 W1 已验证链路。
- **依赖**：`config.db_path`。
- **风险**：中——重建需确认 W2 清理时无其他活跃引用（已确认 `git rm` 时无 import 报错）。
- **状态**：✅ 已实现并验证并已提交（`137098a`，2026-07-25）。离线冒烟 `_smoke33.py` 全绿：临时 db 主竞品记忆往返 + 分析历史往返 + 模拟重启新实例读同 db 主竞品不丢 + bot 已移除 `_last_compare` 改用 `get_session_store()`；webhook_server import 正常、routes 齐全；已重启 uvicorn 加载新代码（原 PID 29752 停，新进程 PID 5012 监听 8011）。

**3.4 webhook 加固 + 监控体验修复**（2026-07-25 用户扩范围决策）

> 用户反馈"竞品雷达消息太多有垃圾感"，查 db 发现 6 个重复"飞书"监控项 + 首次必推 + 30min 轮询叠加。原 3.4 仅 webhook 层加固，现扩范围纳入监控体验修复。

**3.4.1 监控体验修复（产品层，优先）**
- ① **去重**：`MonitorStore.add` 改 `INSERT OR IGNORE` + 唯一索引 `(chat_id, competitor)`；重复添加返回已有 ID + 提示"已存在"。
- ② **首次静默期**：`check_one` 检测 `last_signature == ""` 时，只写 baseline、不推群（返回 False）；下一轮才正常 diff。
- ③ **频率上限**：`Monitor` 加 `last_push_at` 字段；`check_one` 推送前检查距上次推送 < 24h 则跳过（记 log，不推）。
- ④ **清理历史重复**：迁移逻辑——启动时对同 `(chat_id, competitor)` 的重复项保留最早一条、删除其余。

**3.4.2 webhook 层加固（原计划）**
- ① **日志中间件**：记录每个请求 method/path/status/耗时。
- ② **限流**：简单内存令牌桶，同 IP 连发触发 429（开发期阈值宽松，如 30 req/min）。
- ③ **统一异常**：未捕获异常返回 500 JSON `{"code":1,"msg":"internal error"}`（不泄露堆栈）。
- ④ **CORS**：允许外部系统调用 `/api/analyze`（按需开）。

**验收标准**
- 监控：重复 `/monitor add 飞书` 返回"已存在"不新增；新监控首检不推群；24h 内同竞品同群最多 1 条告警。
- webhook：异常返 500 JSON；连发返 429；日志可见请求记录。
- 离线冒烟 + 不破坏 W2/W3.1-3.3 已验证链路。

**依赖**：`monitor.py` / `webhook_server.py` / `config.py`（均已有）。
**风险**：低-中。去重改表结构需迁移现有 db（启动时自动）。
**门禁前置**：W2 飞书已闭环，3.1-3.3 已落地，用户已确认扩范围。属代码层，无飞书集成待测缺口。

#### 1.2 接口骨架（3.1 已实现）
```python
class CardRenderer:
    def render_analysis(self, result: AnalysisResult) -> dict
    def render_compare(self, compare: CompareResult) -> dict
    def render_weekly(self, result: AnalysisResult) -> dict
    def render_text_fallback(self, text: str) -> dict
```

### 2. 执行手册（Execution）

#### 2.1 3.2 `/compare` 实现细节（2026-07-25 落地）
- **提示词唯一入口**：`validation/prompts/compare.txt`（新增）。要求模型**仅输出一个 ```json 围栏块**（`narrative` / `matrix` / `insights`），严守 G1（不编造、引用缺失标「待核实」）+ G3（不堆空框架）。Aily 已弃用，不另存。
- **引擎 `engine.compare(targets, query="")`**（`backend/app/engine.py`）：合并 targets 做 `_gather_context`（复用 W2 搜索/抓取管线）→ 拼 `system.txt`+`compare.txt`+上下文 → 调 LLM（temperature=0.4，空 completion 重试一次）→ `_extract_json_block` 正则抽 ```json 块 → `json.loads` 填充 `CompareResult`。JSON 解析失败则整段作为 `analysis` 兜底（不整体失败）。
- **模型 `CompareResult`** 增加 `analysis: str` + `note: str` 字段（与 `AnalysisResult` 对齐），支撑卡片叙事与降级渲染。
- **渲染 `render_compare_card`** 改造：顶部 `analysis` 叙事（≤18000 截断）→ 维度矩阵 markdown 表（空则「暂无维度数据」）→ 关键洞察 → 来源🔵通用 → 降级备注。
- **命令路由 `bot.py`**：`handle_message` 在 `parse_command` 前加 `/compare` 分支 → `_handle_compare`（空格分词得 targets，<2 回用法提示）；并加「和 X 对比 / 对比 X」自然语义分支 → `_handle_compare_phrase`（提取 X、补 `_last_compare[chat_id]` 主竞品）。`_async_compare` 复用 W3.1 `push_card` 卡片优先、失败回退文本。
- **排错表**：

| 现象 | 根因 | 解决 |
|---|---|---|
| `/compare` 被当成 battle_card | 旧 3.1 只在 scene 别名里路由，无 /compare 分支 | 新增 `/compare` 显式分支（先于 parse_command） |
| 卡片只有叙事无矩阵 | 模型未输出合法 ```json 块 | `_extract_json_block` 失败兜底整段为 narrative；可在 compare.txt 强调「仅输出 json 块」 |
| 「和 X 对比」误触发 | 文本含「对比」两字 | 加正则 `^(和\s+\S+\s*对比|对比\s+\S+)$` 精确匹配，普通对话不进对比分支 |

### 3. 审计（Audit）
- **决策确认（用户 2026-07-25）**：① 对比引擎用「单次 LLM 调用 + 新 compare.txt」（非多次 analyze 收敛）；② 「和 X 对比」记忆 3.2 先做 in-memory，3.3 `sessions.py` 持久化。
- **执行结果**：离线冒烟 6 项全绿（渲染/分发/JSON 解析/路由分支/降级）；真实 E2E（`/compare 飞书 钉钉` + 「和 钉钉 对比」）经进程内 bot + 真实飞书 API 推群 200，无异常；红线回归（Pitch vs Gamma）模型诚实标「参考资料未含 Pitch、需核实」，无编造金额。
- **残留（已闭环）**：`_last_compare` 进程内 dict 重启丢失 → 3.3 `sessions.py` 已接管持久化（主竞品记忆 + 分析历史跨重启不丢）。

### 4. 验证与遗留（Verification & Gaps）
- **验证分层**：离线冒烟（`_smoke32.py` 6 项）→ 真实 E2E（进程内 bot + 真实飞书推群）→ 红线回归（新颖竞品直接调 `engine.compare`）。
- **关联 `06-测试记录表.csv`**：`W3-CP-01`（/compare 推卡 PASS）、`W3-CP-02`（和X对比记忆 PASS）、`W3-CP-03`（红线回归 PASS）。
- **关联 `06-测试记录表.csv`（3.3）**：`W3-SE-01`（sessions 持久化代码层 PASS）。
- **3.3 已落库（2026-07-25）**：`sessions.py` 重建完成（SQLite `sessions`+`analysis_history` 表），`get_primary/set_primary/save_analysis/get_history` + 单例 `get_session_store()`；bot 移除 `_last_compare` in-memory，主竞品记忆 + 分析历史跨重启持久化。离线冒烟全绿，已重启 uvicorn 加载新代码。
- **遗留 → 3.4**：webhook 加固（日志中间件/限流/统一 500/CORS）。
- **状态**：✅ 3.2 已提交（`12a2a4e`）+ 3.3 已提交（`137098a`，代码层全绿）+ 3.4 已实现验证（待提交）。

---

## W4 · 变化识别 + 周报定时 + 打磨部署（v3 修正）

> **v3 修正**：监控 CRUD（4.1）与定时推送（4.3 部分）**已在 W2 提前做最小版**，W4 实际剩：变化**类型识别** / 周报**自动进群** / 内测打磨 / 部署。
> **目标**：从被动问答升级为主动推送（竞品一有动作就推群）。

### 1. 计划（Plan）

#### 1.1 任务清单（W4：4.2–4.5）
| Task | 内容（v3 修正后） | 验收 |
|---|---|---|
| 4.2 变化检测引擎 | LLM 分类变化类型（price_change/product_update/funding/hiring/sentiment）+ 去重 + 定价数值 diff | 模拟变化 → 生成 event（含 source_type） |
| 4.3 周报定时进群 | Scheduler 周一 9:00 推周报；`/report` 手动触发 | 到点自动收周报 |
| 4.4 内测打磨 | 友好降级（断网/超时/限流/单源失败）；`.env.example` 补全；README 部署文档 | 各种异常有友好降级 |
| 4.5 部署 | `start.bat`（uvicorn + ngrok）；VPS/Railway 文档；Dockerfile 可选 | 一键启动全链路 |

#### 1.2 W4 验收标准
`/monitor add` → 定时检测变化推群（含来源标注）；周一 9 点自动周报；`start.bat` 一键启动；边界情况友好降级。

### 2. 执行手册（Execution）
> 待 W4 开工填充。

### 3. 审计（Audit）
> 待 W4 开工填充。

### 4. 验证与遗留（Verification & Gaps）
> 待 W4 开工填充。

> 详细设计（卡片 ASCII 模板、SQL schema、调度器代码）待 W3/W4 开工时在本文档对应章节展开，不另建文件。

---

## 附：阶段内容结构化模板

每个阶段章节严格按以下 4 节组织，便于长期维护与跨阶段对齐：

### 1. 计划（Plan）
- **目标 / Scope**
- **关键决策**（vN 决策表，如有）
- **任务清单**：`| Task | 内容 | commit | 状态 |`
- **验收 Gate**：`| # | 验收项 | 验证方法 | 状态 |`

### 2. 执行手册（Execution）
- **各 Task 实现指南**：`| 文件 | 操作 | 关键内容 |`（含关键代码片段）
- **排错表**：`| 现象 | 根因 | 解决 |`
- **阶段交接**：交付物 / 下阶段候选

### 3. 审计（Audit）
- **审计发现**：`| 严重度 | 问题 | 结论 |`
- **决策确认**：待确认项 → 结论
- **执行结果**：代码侧 / 文档侧 / 残留（非阻塞项）

### 4. 验证与遗留（Verification & Gaps）
- **验证分层**：`| 层 | 覆盖 | 不在本记录 |`
- **复现命令**（代码块）
- **验证结果**：代码层 / 引擎层 / 红线
- **结论与限制**：含飞书集成等需手动闭环项 + 关联 `06-测试记录表.csv` / commit
