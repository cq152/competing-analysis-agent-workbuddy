# W1 交付报告 — 预研骨架 + MVP Gate

> **阶段**：W1（预研骨架 + MVP 验证）
> **周期**：2026-07-24（单日密集开发：git 仓库初始化 00:15 → P0 验收通过 23:50）
> **状态**：✅ **已完成，Gate 通过（2026-07-24 23:50）**
> **产出位置**：本文件为 W1 阶段的**单一交付总结**；详细技术细节散落在 `backend/`、`validation/`、`06`、`00`、`08` 等文件

---

## 1. W1 目标与范围

### 1.1 目标

验证「飞书群里的竞品分析 Agent」这个产品形态是否可行——核心问题是：

> **LLM 在飞书群里做竞品分析，质量够不够用？端到端能不能跑通？**

如果 Gate 通过 → 进入正式开发（W2–W4）；如果不通过 → 回到路线 A（Aily 无代码）继续优化提示词。

### 1.2 范围（做什么 / 不做什么）

| 做 | 不做 |
|---|---|
| 最小可运行 FastAPI 后端 | 完整错误处理 / 日志系统 |
| 飞书事件接收→处理→回复全链路 | 富交互卡片（先用纯文本） |
| 4 个核心场景的 LLM 引擎 | 搜索 / 抓取 / 数据源 |
| P0 验收（4 场景飞书群实测） | 监控雷达 / 定时任务 |
| 提示词版本化与风险测试 | 多租户 / 认证 / 部署 |

### 1.3 成功标准（Gate）

满足以下**全部**才通过：

1. 本地 4 场景用**未见过的竞品**跑通，结构 + 红线全过；
2. 飞书集成：@触发 → 场景路由 → LLM 分析 → 回复到群，全链路通；
3. 对抗测试零编造、零贬低。

---

## 2. 交付物清单

### 2.1 代码交付

| # | 文件 | 行数 | 职责 | 状态 |
|---|---|---|---|---|
| 1 | `backend/webhook_server.py` | 163 | FastAPI 主入口：Webhook 接收 + `/api/analyze` + `/health` | ✅ 生产可用 |
| 2 | `backend/app/engine.py` | 48 | 分析引擎：加载 `validation/prompts/*.txt` → 调 DeepSeek API | ✅ 可运行（W2 需改造） |
| 3 | `backend/app/bot.py` | 236 | 消息处理：解析飞书事件 → 场景路由（4 场景）→ 组装回复 | ✅ 4 场景全通（W2 需扩展） |
| 4 | `backend/app/config.py` | 15 | 配置管理：从 `.env` 读 LLM API Key / Model / 飞书凭证 | ✅ 正常工作（W2 需扩展） |
| 5 | `backend/run_bot.py` | — | WebSocket 长连接备选启动脚本 | ✅ 可运行（已不推荐使用） |
| 6 | `backend/bot_config.json` | — | 声明式配置：人设名 / 默认场景 / 群白名单 / 命令别名 | ✅ 已配置 |
| 7 | `backend/.env.example` | — | 环境变量模板（API Key 占位） | ✅ 已创建 |
| 8 | `backend/requirements.txt` | — | Python 依赖（fastapi / uvicorn / lark-oapi / openai / python-dotenv） | ✅ 已就位 |

### 2.2 废弃文件（预研遗留）

| 文件 | 原因 | 处理方式 |
|---|---|---|
| `app/main.py` | 被 `webhook_server.py` 合并取代 | 🗑️ 标记废弃，W2 清理 |
| `app/feishu.py` | 只有 stub（verify_url / send_card），未实现 | 🗑️ 标记废弃，W2 清理 |
| `app/sessions.py` | 仅被 `main.py` 引用，无独立价值 | 🗑️ 标记废弃，W2 清理 |

### 2.3 提示词资产

| 文件 | 内容 | 状态 |
|---|---|---|
| `validation/prompts/system.txt` | 全局系统提示词（10 条红线 G1-G10 + 角色定义） | ✅ 最终版 |
| `validation/prompts/battle_card.txt` | Battle Card 场景提示词（含「不贬对手」收紧红线） | ✅ 修复后最终版 |
| `validation/prompts/pricing.txt` | Pricing 场景提示词（含「禁止编造数字」红线） | ✅ 修复后最终版 |
| `validation/prompts/weekly.txt` | Weekly 周报场景提示词（≤3 条 / 相关度 / set-and-forget） | ✅ 最终版 |
| `validation/prompts/discovery.txt` | Discovery 竞品发现场景提示词（间接/降维/跨界） | ✅ 最终版 |
| `validation/cases.json` | 泛化测试用例（全新行业/竞品，非 few-shot 示例） | ✅ 防复读版本 |

### 2.4 文档交付

| 文件 | 内容 |
|---|---|
| `00-踩坑手册.md` | 18 条可复用踩坑记录（LLM 5 条 + 飞书 7 条 + 工程运维 4 条 + 产品设计 3 条） |
| `06-功能测试重点与项目红线.md` | 10 条通用红线 + 4 个场景专属红线 + 风险测试完整记录 + 验收 Gate 标准 |
| `08-飞书自建Bot接入方案.md` | 代码声明式飞书接入层方案（企业自建应用 + Webhook + ngrok） |
| `backend/README.md` | 后端模块说明、运行方式、API 路由、状态 |
| `06-测试记录表.csv` | 26 项验收用例（含 4 个 P0 必测），可导入飞书多维表格 |

---

## 3. 关键技术决策

### 3.1 决策记录

| # | 决策 | 为什么这么做 | 替代方案 | 风险 |
|---|---|---|---|---|
| D1 | **HTTP Webhook 取代 WebSocket 长连接** | 飞书同应用仅允许一个活跃长连接；开发期反复重启导致旧连接僵尸、消息丢失 | 加单实例锁 + 手动杀进程 | 需要 ngrok 内网穿透（免费域名不稳定） |
| D2 | **`receive_id_type=chat_id` 而非 `message_id`** | `message_id` 引用回复报错 `99992402` 字段校验失败 | 排查 message_id 格式要求 | 用户看到的是独立消息而非引用回复（体验降级，W3 卡片阶段解决） |
| D3 | **提示词唯一真相源 = `validation/prompts/*.txt`** | 防止多端漂移（飞书 UI / 文档 / 代码各存一份导致不一致） | 用配置中心管理 | 开发者必须记住「改提示词只改 validation」 |
| D4 | **DuckDuckGo 未在 W1 接入** | W1 目标是验证 LLM 分析质量 + 飞书端到端，搜索是 W2 的事 | W1 就接 Google Custom Search | W1 引擎是"纯 LLM"，分析结果无来源引用（已知限制） |
| D5 | **放弃 Aily UI 手动创建，改代码声明式 Bot** | Aily 平台自定义智能体入口卡住（UI 迷宫 + 企业权限问题）；代码方式可控、可复现、可 CI | 继续卡在 Aily UI 等权限开通 | 需要自己管理飞书应用生命周期 |
| D6 | **DeepSeek 作为 LLM 提供商** | OpenAI 兼容接口、成本较低、中文能力强 | 通义千问 / GLM / Claude | 单一供应商风险（W2 可加 model router） |

### 3.2 架构简图（W1 状态）

```
用户（飞书群）
  │ @机器人 "/battle 竞品X"
  ▼
飞书事件服务器 ──HTTP POST──▶ ngrok（内网穿透）
                              │
                              ▼
                       webhook_server.py（:8011）
                       ├── 验签（X-Lark-Signature）
                       ├── url_verification 挑战响应
                       └── im.message.receive_v1 事件
                              │
                              ▼
                           bot.py
                           ├── 消息解析（提取 text / chat_id）
                           ├── 场景路由（/battle → battle_card）
                           └── 群白名单 + @触发检查
                              │
                              ▼
                           engine.py
                           ├── 加载 validation/prompts/{scene}.txt
                           ├── 组装 messages（system + user query）
                           └── 调 DeepSeek API（OpenAI 兼容）
                              │
                              ▼
                           bot.py _reply()
                           └── 飞书 API 发送文本消息到群
```

---

## 4. 验收证据

### 4.1 P0 验收详情（2026-07-24 23:50 通过）

| 用例 ID | 场景 | 输入 | 验收点 | 结果 |
|---|---|---|---|---|
| **PR-02** | Battle Card | 真实竞品对比 | 标注"模糊信号/待核实"、无编造数字 | ✅ PASS |
| **SYS-04** | Pricing | 真实定价查询 | 引导去 Crunchbase/财报核实 | ✅ PASS |
| **AD-01** | 抗编造 | 追问"给我具体数字" | 仍拒给数、坚持标待核实 | ✅ PASS |
| **AD-02** | 抗贬低 | 诱导"XX 是不是垃圾" | 纠正"垃圾是情绪不是分析"、不附和贬低 | ✅ PASS |

**验证方法**：
1. 重启 uvicorn（端口 8011）+ ngrok（同域名 `sitter-shrubbery-washout.ngrok-free.dev`）
2. 跑 `_run_p0.py` 发送 4 条 POST 请求 → 全部返回 200
3. **通过飞书 API 拉取群消息逐条确认**内容符合预期

### 4.2 本地风险测试（Gate 前完成）

两轮测试，详见 `06` §三：

| 轮次 | 方法 | 发现 | 修复 |
|---|---|---|---|
| 第一轮 | 用 few-shot 相同输入跑 | R1 复读陷阱（格式对但照抄示例）、R2 编造数字（pricing 自造原价/降幅） | — |
| 第二轮 | 换全新行业/竞品重跑 | 4 场景真泛化，非复读 | pricing.txt 加禁止编造红线；battle_card.txt 收紧不贬对手；cases.json 全换新 |

### 4.3 飞书端到端验证

| 环节 | 状态 | 备注 |
|---|---|---|
| 飞书开发者后台 → 企业自建应用创建 | ✅ | App ID `cli_aae8...` |
| 凭证配置（App Secret） | ✅ | 已入 `.env`（gitignore） |
| 事件订阅 → Webhook 回调地址 | ✅ | ngrok 域名已填 |
| 应用发布 | ✅ | 含 `im:message:group_msg` 权限 |
| ngrok 内网穿透 | ✅ | `sitter-shrubbery-washout.ngrok-free.dev` → `:8011` |
| Webhook 签名验证 | ✅ | `X-Lark-Signature` HMAC-SHA256 |
| 消息接收 → 解析 → 路由 | ✅ | 群白名单 + @触发正常 |
| LLM 调用（DeepSeek） | ✅ | 4 场景全部返回结构化输出 |
| 回复发送到群 | ✅ | `chat_id` 方式成功 |
| 公网扫描器 401 | ✅ 已知无害 | IP 段不同于飞书事件服务器 |

---

## 5. 遗留技术债务

| # | 债务 | 优先级 | 计划清偿 | 影响 |
|---|---|---|---|---|
| TD-1 | 废弃文件未删除（main.py / feishu.py / sessions.py） | P2 | W2 Task 2.1 | 代码噪音，不影响运行 |
| TD-2 | 无结构化日志（print 代替 logger） | P1 | W2 Task 2.1 | 生产调试困难 |
| TD-3 | 无错误处理（LLM 超时 / API 异常直接抛） | P0 | W2 Task 2.1 | 服务不稳定 |
| TD-4 | 配置项不全（搜索 API Key / 数据源等缺失） | P1 | W2 Task 2.1 | W2 功能阻塞 |
| TD-5 | 引擎纯 LLM 无数据源（分析结果无来源引用） | P0 | W2 Task 2.5 核心 | 质量天花板 |
| TD-6 | 纯文本回复（无飞书 Interactive Card） | P1 | W3 Task 3.1 | 用户体验上限 |
| TD-7 | 会话内存字典（重启丢失） | P2 | W3 Task 3.3 | 无状态 |
| TD-8 | ngrok 免费域名临时性（重启变域名） | P2 | W4 Task 4.5 | 每次手动更新回调地址 |
| TD-9 | 无认证中间件（`/api/analyze` 任何人可调用） | P1 | W3 Task 3.4 | 安全隐患 |
| TD-10 | 单一 LLM 供应商（仅 DeepSeek） | P2 | W2+ 按需 | 供应商风险 |

---

## 6. 经验沉淀

W1 阶段产出的可复用经验已写入 **`00-踩坑手册.md`**（共 18 条），按类别索引：

### 6.1 LLM / 提示词工程（5 条）

| 编号 | 坑 | 一句话记住 |
|---|---|---|
| R1 | 复读陷阱 | 测试 query 必须换行业，否则模型只是照抄示例 |
| R2 | 编造数字陷阱 | 模型太" helpful "，模糊输入会脑补具体数字 |
| R3 | 轻蔑措辞陷阱 | "带钩子"容易激发创意表达欲，滑向情绪化攻击 |
| R4 | 「流畅但很浅」风险 | 格式正确 ≠ 有洞察，#1 风险 |
| R5 | 提示词版本化防漂移 | 单一真相源，其余只消费不编辑 |

### 6.2 飞书集成（6 条）

| 编号 | 坑 | 一句话记住 |
|---|---|---|
| F1 | 页面迷宫 | 智能伙伴 ≠ 自定义智能体，两个产品两个入口 |
| F2 | 长连接单连接限制 | 开发期反复重启 → 改 Webhook 彻底绕过 |
| F3 | 回复 API 字段校验 | `message_id` 引用报错 → 用 `chat_id` |
| F4 | ngrok 代理变量冲突 | 启动前清除 `http_proxy` |
| F5 | 权限静默缺失 | 读群消息需要额外开 `im:message:group_msg` |
| F6 | ListMessage API 参数 | `container_id_type` 用 `"chat"` 不是 `"chat_id"` |

### 6.3 工程运维（4 条）

| 编号 | 坑 | 一句话记住 |
|---|---|---|
| E1 | 后台进程回收 | 不要依赖会话级后台进程跑服务 |
| E2 | ngrok 域名临时性 | 重启后要更新飞书后台回调地址 |
| E3 | 端口冲突 | 用非常见端口（8011） |
| E4 | 401 ≠ 签名 bug | 可能是公网扫描器，看 IP 区分 |

### 6.4 产品设计（3 条）

| 编号 | 结论 | 一句话记住 |
|---|---|---|
| P1 | 免费层钩子 | 用「监控」不用「限次分析」 |
| P2 | 分发即护城河 | 飞书群 = 产品界面 |
| P3 | 变化检测 > 信息收集 |围着「变了什么 + 跟你相关吗」转 |

---

## 7. 量化总结

| 维度 | 数值 |
|---|---|
| **总工期** | 约 X 天（从首次对话到 Gate 通过） |
| **代码文件** | 8 个（5 个活跃 + 3 个废弃） |
| **代码总行数** | ~462 行（不含注释和空行） |
| **提示词文件** | 6 个（1 system + 4 场景 + 1 cases） |
| **文档文件** | 5 个（00 / 06 / 08 / backend/README / 本文件） |
| **踩坑记录** | 18 条（可复用） |
| **红线规则** | 10 通用 + 4 场景专属 = **14 条** |
| **P0 用例** | 4 个（全部 PASS） |
| **验收用例总数** | 26 个（CSV） |
| **Git commits** | 见 `git log` |
| **关键 Bug 修复** | 2 个（pricing 编造 + battle_card 轻蔑） |

---

## 8. 与前后阶段的衔接

### 8.1 W1 前置条件（来自更早的阶段）

- [x] 产品定位重设（SMB 视角）— `04`
- [x] Aily 搭建指南 + 系统提示词 — `03`
- [x] 4 场景提示词 + 样例 — `05`
- [x] 本地验证脚手架 `validation/` — 含风险测试闭环

### 8.2 W1 → W2 衔接（输入 / 输出）

| W1 产出 | W2 怎么用 |
|---|---|
| `webhook_server.py` | W2 扩展（加 `/api/search` 路由） |
| `engine.py` | **大改造**：从「query → LLM」变成「query → 搜索 → 抓取 → 组装上下文 → LLM → 结构化结果」 |
| `bot.py` | 扩展（加 compare 命令 + 错误场景处理） |
| `config.py` | 扩展（加搜索 / 数据源 / 日志配置） |
| `validation/prompts/*.txt` | **不变**，W2 直接复用 |
| 废弃文件 (TD-1) | W2 Task 2.1 清理 |

### 8.3 W1 的已知限制（W2 要解决的）

1. **引擎无数据源** — 分析完全依赖 LLM 参数知识，无法获取实时信息
2. **无来源引用** — 用户无法验证分析结论的真实性
3. **中文互联网覆盖为零** — 违反 G6 红线（W2 核心修复目标）
4. **置信度未知** — 无法告诉用户"这条分析有多可信"

---

## 附录 A：Git 提交记录（W1 全量）

> 数据来源：`git log --oneline --since="2026-07-15"`（W1 阶段共 25 个 commit，全部在 2026-07-24 当日）

### 按阶段分组的里程碑

| 阶段 | Commit | 说明 |
|---|---|---|
| **项目初始化** | `0e0e5f2` | 初始化竞品分析 Agent 项目 —— 总览/定位/Aily指南/场景提示词 纳入版本管理 |
| **提示词版本化** | `9eb4db3` | 新增本地验证脚手架 validation/ —— 抽离 03/05 提示词为可版本化文件 |
| **风险测试修复** | `6dbe2ad` | fix: pricing 模糊输入禁止编造 / battle_card 收紧不贬对手 / cases.json 换泛化查询 |
| **红线文档** | `f4b8740` `db2fcd6` | 新增 06-功能测试重点与项目红线 + 06-测试记录表.csv（26 项用例） |
| **Aily 导入包** | `3288ac2` | 新增 aily/ 飞书导入包（人设.txt 完整大脑 + 开场白 + 监控定时配置） |
| **提交规范** | `42b8442` | 确立提交规范（README §10）+ MEMORY.md 长期约定 |
| **路线 B 预研** | `8eb7a78` | 新增 backend/ 预研骨架（FastAPI 最小可运行，复用 validation/prompts）+ 07 技术方案 |
| **飞书长连接 Bot** | `328be6b` `abd96bc` | 基于 lark-coding-agent-bridge 新增 WebSocket 长连接 Bot 接入层 + 凭证实测 |
| **换 Webhook 方案** | `9a8522f` `6afc222` | 飞书接入换 HTTP Webhook（方案 B）；_reply 改用 chat_id 规避 99992402 |
| **架构清理** | `3b88f9f` | 合并 main.py 路由到 webhook_server.py 统一入口；标记废弃文件；加 _run_p0.py |
| **P0 验收通过** | `5a7df72` | test: P0 验收全通过 — 4 条红线用例飞书群实测 PASS，MVP Gate 闭环 |
| **踩坑手册** | `f9f4e5b` | 新增 00-踩坑手册.md —— 结构化沉淀 18 条可复用踩坑记录 |

### 完整 Commit 列表（时间倒序）

```
f9f4e5b docs: 新增 00-踩坑手册.md — 结构化沉淀 18 条可复用踩坑记录
5a7df72 test: P0 验收全通过 — 4 条红线用例飞书群实测 PASS，MVP Gate 闭环
3971807 docs: 修订『长运行命令管理』规则——超10分钟改为自检(正常继续/异常终止并汇报)
3b88f9f refactor: backend 架构清理落地——合并 main.py 路由到 webhook_server.py
793422f docs: 同步 §7 进度——飞书Webhook端到端全通，P0已触发到群待验收
6afc222 fix: webhook 方案 B 收尾——_reply 改用 chat_id 发群(规避 message_id 99992402)
9a8522f feat: 飞书接入换方案B(HTTP Webhook)替代长连接；bot 修回复API+单实例锁
abd96bc feat: 飞书自建应用凭证接入实测长连接成功；bot 加群白名单静默策略
328be6b feat: 基于 lark-coding-agent-bridge 新增飞书 WebSocket 长连接 Bot 接入层
24825ab docs: 确认已到达正确 Aily 平台页——修正 aily/README 入口描述
42b0792 docs: 诊断用户实际在飞书智能伙伴而非 Aily 平台——修正入口+企业没开通方案
0d85455 docs: 根据用户实际截图诊断——补齐 aily/README『你走错页面了吗』对照表
6747fb3 docs: 核实飞书 Aily 最新 UI——联网/开场白叫法修正
3260c73 docs: 进度同步——用户已填 Aily 人设，下一步开联网+开场白+知识库
8969ad8 docs: 核对飞书 Aily 最新 UI，修正 03/aily 创建步骤；README 进度同步
8eb7a78 feat: 新增路线 B 预研骨架 backend/ + 07 技术方案；README 导航/进度同步
42b8442 docs: 确立提交规范——README §10 固化；新增 MEMORY.md 长期约定
3288ac2 feat: 新增 aily/ 飞书导入包（人设.txt 完整大脑 + 开场白 + 监控定时配置）
db2fcd6 docs: 新增 06-测试记录表.csv —— 26 项验收用例（含 4 个 P0 必测）
f4b8740 docs: 新增 06-功能测试重点与项目红线；同步更新 README 导航与进度
6dbe2ad fix: pricing 禁止编造具体数字；battle_card 收紧不贬对手；cases.json 换泛化查询
9eb4db3 feat: 新增本地验证脚手架 validation/ —— 抽离提示词为可版本化文件
0e0e5f2 docs: 初始化竞品分析 Agent 项目 —— 总览/定位/Aily指南/场景提示词
```

---

## 附录 B：环境信息

| 项 | 值 |
|---|---|
| Python | 3.11.7（系统）/ 3.13.12（managed） |
| 操作系统 | Windows（Git Bash） |
| 飞书 SDK | `lark-oapi` |
| LLM | DeepSeek（OpenAI 兼容端点） |
| Web 框架 | FastAPI + Uvicorn |
| 内网穿透 | ngrok 免费版 |
| 端口 | 8011（uvicorn）、4040（ngrok API） |

---

*本文件是 W1 阶段的官方交付总结。后续每个周结束都应产出类似文档，形成固定节奏。*
